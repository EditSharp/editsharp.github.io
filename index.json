{
  "api/EditSharp.Audio.AudioTap.html": {
    "href": "api/EditSharp.Audio.AudioTap.html",
    "title": "Class AudioTap | EditSharp",
    "summary": "Class AudioTap Namespace EditSharp.Audio Assembly EditSharp.dll Well-known tap points besides node and channel ids. public static class AudioTap Inheritance object AudioTap Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Fields Master The master output, after every channel is summed. public static readonly Guid Master Field Value Guid"
  },
  "api/EditSharp.Audio.AudioTapBlock.html": {
    "href": "api/EditSharp.Audio.AudioTapBlock.html",
    "title": "Struct AudioTapBlock | EditSharp",
    "summary": "Struct AudioTapBlock Namespace EditSharp.Audio Assembly EditSharp.dll One block of audio seen at a tap: interleaved float samples (valid only during the callback), their format, and the timeline position of the block's first frame. public readonly record struct AudioTapBlock : IEquatable<AudioTapBlock> Implements IEquatable<AudioTapBlock> Inherited Members ValueType.Equals(object) ValueType.GetHashCode() ValueType.ToString() object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) Constructors AudioTapBlock(ReadOnlyMemory<float>, int, int, TimeSpan) One block of audio seen at a tap: interleaved float samples (valid only during the callback), their format, and the timeline position of the block's first frame. public AudioTapBlock(ReadOnlyMemory<float> Samples, int SampleRate, int Channels, TimeSpan Position) Parameters Samples ReadOnlyMemory<float> SampleRate int Channels int Position TimeSpan Properties Channels public int Channels { get; init; } Property Value int Position public TimeSpan Position { get; init; } Property Value TimeSpan SampleRate public int SampleRate { get; init; } Property Value int Samples public ReadOnlyMemory<float> Samples { get; init; } Property Value ReadOnlyMemory<float>"
  },
  "api/EditSharp.Audio.html": {
    "href": "api/EditSharp.Audio.html",
    "title": "Namespace EditSharp.Audio | EditSharp",
    "summary": "Namespace EditSharp.Audio Classes AudioTap Well-known tap points besides node and channel ids. Structs AudioTapBlock One block of audio seen at a tap: interleaved float samples (valid only during the callback), their format, and the timeline position of the block's first frame."
  },
  "api/EditSharp.Caching.Proxy.EsrpCompressionScheme.html": {
    "href": "api/EditSharp.Caching.Proxy.EsrpCompressionScheme.html",
    "title": "Enum EsrpCompressionScheme | EditSharp",
    "summary": "Enum EsrpCompressionScheme Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll Which lossless transform every stored frame's bytes went through; a file-wide choice recorded in the header. Applies to an IndexedDelta7 file's control-byte planes, never to its shared palette. See EsrpZstd. public enum EsrpCompressionScheme Fields None = 0 Raw bytes, no transform. Zstd = 1 Zstandard via ZstdSharp.Port (fully managed). Compresses each frame on its own, so random access stays O(1)."
  },
  "api/EditSharp.Caching.Proxy.EsrpPixelFormat.html": {
    "href": "api/EditSharp.Caching.Proxy.EsrpPixelFormat.html",
    "title": "Enum EsrpPixelFormat | EditSharp",
    "summary": "Enum EsrpPixelFormat Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll How a stored frame's pixels are laid out; file-wide, recorded in the header. public enum EsrpPixelFormat Fields IndexedDelta7 = 1 A 7-bit index/delta control byte per pixel against ONE palette shared by the whole file (built from a sampling pass before encoding). See IndexedDelta7Codec. Rgba8888 = 0 width * height * 4 interleaved RGBA8888 bytes; exactly what SourceDecoder produces."
  },
  "api/EditSharp.Caching.Proxy.ProxyCache.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyCache.html",
    "title": "Class ProxyCache | EditSharp",
    "summary": "Class ProxyCache Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll The one proxy per original media file; shared by scrubbing, playback and paused editing alike. An entry remembers its original (by content hash, so a renamed, moved or duplicated file finds the same proxy), its format, and how far it has been built. NOTHING BUILDS AUTOMATICALLY. A consumer calls BuildAsync (typically on import) and watches StatusChanged; everything that reads media only ever looks proxies up. Builds queue behind EditSharpConfig.MaxConcurrentProxyBuilds; concurrent requests for the same file share one build, which is cancelled only once every caller has cancelled. PARTIAL PROXIES ARE FIRST-CLASS: a proxy is readable from its start up to AvailableUpTo while it builds, stays readable if the build is interrupted (ProxyState.Partial), and the next BuildAsync resumes from there. Building a different format than the one on disk replaces it: the old proxy keeps serving reads until the new one completes, then is deleted. ON DISK (EditSharpConfig.ProxyDirectory/<hash[..2]>/), one stem per format (<hash>.delta7, .rgba, .dnxhr, .prores) so a replacement never collides with the proxy it replaces: <stem>.esrp an .esrp proxy (progress lives in its index) <stem>.mov.json a MOV proxy's sidecar (segments/progress) <stem>.seg<n>.mov, <stem>.mov its segments while building, its final file once complete public static class ProxyCache Inheritance object ProxyCache Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods BuildAsync(string, ProxyFormat?, IProgress<double>?, HardwareAccelerator, CancellationToken) Builds sourcePath's proxy in format (EditSharpConfig.ProxyFormat when null), resuming a partial one of the same format, and returns once it's complete. Returns at once if a complete proxy in that format already exists. Cancelling only detaches this caller; the build itself stops when no caller is left waiting on it, and what it wrote stays usable and resumable. public static Task<ProxyEntry> BuildAsync(string sourcePath, ProxyFormat? format = null, IProgress<double>? progress = null, HardwareAccelerator hwAccel = HardwareAccelerator.GPU, CancellationToken ct = default) Parameters sourcePath string format ProxyFormat? progress IProgress<double> hwAccel HardwareAccelerator ct CancellationToken Returns Task<ProxyEntry> GetStatusAsync(string, CancellationToken) Where sourcePath's proxy stands, looking at running builds first and then the disk. public static Task<ProxyStatus> GetStatusAsync(string sourcePath, CancellationToken ct = default) Parameters sourcePath string ct CancellationToken Returns Task<ProxyStatus> IsBuilding(string) Synchronous, memory-only: whether a build for sourcePath is queued or running in this process right now. public static bool IsBuilding(string sourcePath) Parameters sourcePath string Returns bool TryGetAsync(string, CancellationToken) sourcePath's proxy (complete or partial) if one is on disk, loading what's needed to find it. After this, TryGetEntry answers synchronously for the same unchanged file. public static Task<ProxyEntry?> TryGetAsync(string sourcePath, CancellationToken ct = default) Parameters sourcePath string ct CancellationToken Returns Task<ProxyEntry> TryGetEntry(string, out ProxyEntry) Synchronous, memory-only lookup for per-frame callers: the proxy for sourcePath if this process already knows it (via TryGetAsync, a status query, or a build). Never hashes and never reads the cache from disk; a miss only means \"not known yet\". public static bool TryGetEntry(string sourcePath, out ProxyEntry entry) Parameters sourcePath string entry ProxyEntry Returns bool Events StatusChanged Raised whenever a file's proxy status changes; queued, started, progressed (at most ~4 times a second per file), finished, failed or cancelled. Raised on a background thread. public static event EventHandler<ProxyStatusChangedEventArgs>? StatusChanged Event Type EventHandler<ProxyStatusChangedEventArgs>"
  },
  "api/EditSharp.Caching.Proxy.ProxyEntry.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyEntry.html",
    "title": "Class ProxyEntry | EditSharp",
    "summary": "Class ProxyEntry Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll One original's proxy on disk. Path is the .esrp file, or for a MOV proxy its sidecar (which lists the segments or the final file). Width/Height/FrameRate are the proxy's own. public sealed record ProxyEntry : IEquatable<ProxyEntry> Inheritance object ProxyEntry Implements IEquatable<ProxyEntry> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors ProxyEntry(string, ProxyFormat, string, int, int, double) One original's proxy on disk. Path is the .esrp file, or for a MOV proxy its sidecar (which lists the segments or the final file). Width/Height/FrameRate are the proxy's own. public ProxyEntry(string SourceHash, ProxyFormat Format, string Path, int Width, int Height, double FrameRate) Parameters SourceHash string Format ProxyFormat Path string Width int Height int FrameRate double Properties Format public ProxyFormat Format { get; init; } Property Value ProxyFormat FrameRate public double FrameRate { get; init; } Property Value double Height public int Height { get; init; } Property Value int IsEsrp public bool IsEsrp { get; } Property Value bool Path public string Path { get; init; } Property Value string SourceHash public string SourceHash { get; init; } Property Value string Width public int Width { get; init; } Property Value int"
  },
  "api/EditSharp.Caching.Proxy.ProxyFormat.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyFormat.html",
    "title": "Enum ProxyFormat | EditSharp",
    "summary": "Enum ProxyFormat Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll public enum ProxyFormat Fields DNxHR = 2 DNxHR in a MOV; all-intra, widely compatible; read through ffmpeg. EsrpDelta7 = 0 .esrp, 7-bit indexed against one shared palette. Smallest; lowest colour fidelity; no subprocess to read. EsrpRgba = 1 .esrp, full RGBA8888. Large; full fidelity at proxy size; no subprocess to read. ProRes = 3 ProRes in a MOV; all-intra, widely compatible; read through ffmpeg."
  },
  "api/EditSharp.Caching.Proxy.ProxyState.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyState.html",
    "title": "Enum ProxyState | EditSharp",
    "summary": "Enum ProxyState Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll public enum ProxyState Fields Building = 2 Complete = 4 Failed = 5 The last build failed. Anything it wrote before failing stays readable up to AvailableUpTo. NotCached = 0 Partial = 3 A build stopped part-way (cancelled, or the app exited). Readable up to AvailableUpTo; the next BuildAsync resumes it. Queued = 1 Waiting for a build slot (see EditSharpConfig.MaxConcurrentProxyBuilds)."
  },
  "api/EditSharp.Caching.Proxy.ProxyStatus.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyStatus.html",
    "title": "Struct ProxyStatus | EditSharp",
    "summary": "Struct ProxyStatus Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll Where a file's proxy stands. AvailableUpTo is how much of the original, from its start, can be read from the proxy right now; Progress is the same as a fraction of the whole. public readonly record struct ProxyStatus : IEquatable<ProxyStatus> Implements IEquatable<ProxyStatus> Inherited Members ValueType.Equals(object) ValueType.GetHashCode() ValueType.ToString() object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) Constructors ProxyStatus(ProxyState, TimeSpan, ProxyFormat?, double, Exception?) Where a file's proxy stands. AvailableUpTo is how much of the original, from its start, can be read from the proxy right now; Progress is the same as a fraction of the whole. public ProxyStatus(ProxyState State, TimeSpan AvailableUpTo, ProxyFormat? Format, double Progress, Exception? Error) Parameters State ProxyState AvailableUpTo TimeSpan Format ProxyFormat? Progress double Error Exception Properties AvailableUpTo public TimeSpan AvailableUpTo { get; init; } Property Value TimeSpan Error public Exception? Error { get; init; } Property Value Exception Format public ProxyFormat? Format { get; init; } Property Value ProxyFormat? NotCached public static ProxyStatus NotCached { get; } Property Value ProxyStatus Progress public double Progress { get; init; } Property Value double State public ProxyState State { get; init; } Property Value ProxyState"
  },
  "api/EditSharp.Caching.Proxy.ProxyStatusChangedEventArgs.html": {
    "href": "api/EditSharp.Caching.Proxy.ProxyStatusChangedEventArgs.html",
    "title": "Class ProxyStatusChangedEventArgs | EditSharp",
    "summary": "Class ProxyStatusChangedEventArgs Namespace EditSharp.Caching.Proxy Assembly EditSharp.dll public sealed class ProxyStatusChangedEventArgs : EventArgs Inheritance object EventArgs ProxyStatusChangedEventArgs Inherited Members EventArgs.Empty object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors ProxyStatusChangedEventArgs(string, ProxyStatus) public ProxyStatusChangedEventArgs(string sourcePath, ProxyStatus status) Parameters sourcePath string status ProxyStatus Properties SourcePath public string SourcePath { get; } Property Value string Status public ProxyStatus Status { get; } Property Value ProxyStatus"
  },
  "api/EditSharp.Caching.Proxy.html": {
    "href": "api/EditSharp.Caching.Proxy.html",
    "title": "Namespace EditSharp.Caching.Proxy | EditSharp",
    "summary": "Namespace EditSharp.Caching.Proxy Classes ProxyCache The one proxy per original media file; shared by scrubbing, playback and paused editing alike. An entry remembers its original (by content hash, so a renamed, moved or duplicated file finds the same proxy), its format, and how far it has been built. NOTHING BUILDS AUTOMATICALLY. A consumer calls BuildAsync (typically on import) and watches StatusChanged; everything that reads media only ever looks proxies up. Builds queue behind EditSharpConfig.MaxConcurrentProxyBuilds; concurrent requests for the same file share one build, which is cancelled only once every caller has cancelled. PARTIAL PROXIES ARE FIRST-CLASS: a proxy is readable from its start up to AvailableUpTo while it builds, stays readable if the build is interrupted (ProxyState.Partial), and the next BuildAsync resumes from there. Building a different format than the one on disk replaces it: the old proxy keeps serving reads until the new one completes, then is deleted. ON DISK (EditSharpConfig.ProxyDirectory/<hash[..2]>/), one stem per format (<hash>.delta7, .rgba, .dnxhr, .prores) so a replacement never collides with the proxy it replaces: <stem>.esrp an .esrp proxy (progress lives in its index) <stem>.mov.json a MOV proxy's sidecar (segments/progress) <stem>.seg<n>.mov, <stem>.mov its segments while building, its final file once complete ProxyEntry One original's proxy on disk. Path is the .esrp file, or for a MOV proxy its sidecar (which lists the segments or the final file). Width/Height/FrameRate are the proxy's own. ProxyStatusChangedEventArgs Structs ProxyStatus Where a file's proxy stands. AvailableUpTo is how much of the original, from its start, can be read from the proxy right now; Progress is the same as a fraction of the whole. Enums EsrpCompressionScheme Which lossless transform every stored frame's bytes went through; a file-wide choice recorded in the header. Applies to an IndexedDelta7 file's control-byte planes, never to its shared palette. See EsrpZstd. EsrpPixelFormat How a stored frame's pixels are laid out; file-wide, recorded in the header. ProxyFormat ProxyState"
  },
  "api/EditSharp.Components.Animatable-1.html": {
    "href": "api/EditSharp.Components.Animatable-1.html",
    "title": "Class Animatable<T> | EditSharp",
    "summary": "Class Animatable<T> Namespace EditSharp.Components Assembly EditSharp.dll public sealed class Animatable<T> : IAnimatable Type Parameters T Inheritance object Animatable<T> Implements IAnimatable Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors Animatable(T) public Animatable(T staticValue) Parameters staticValue T Properties IsAnimated True once the track has enough keyframes to override the static value. public bool IsAnimated { get; } Property Value bool Keyframes Every keyframe on this property, in time order; empty when it has no track. Times are content-relative, like everything else on a track. public IReadOnlyList<IKeyframe> Keyframes { get; } Property Value IReadOnlyList<IKeyframe> StaticValue public T StaticValue { get; set; } Property Value T Track public KeyframeTrack<T>? Track { get; } Property Value KeyframeTrack<T> ValueType The T in Animatable<T>. public Type ValueType { get; } Property Value Type Methods AttachTrack(KeyframeTrack<T>) Installs an already-constructed track — the escape hatch a KeyframeTrack<T> SUBCLASS (e.g. PositionTrack, which needs spatial handles beyond the plain track GetOrCreateTrack would build) needs to become this property's backing track. public void AttachTrack(KeyframeTrack<T> track) Parameters track KeyframeTrack<T> ClearKeyframes() Drops the track: no keyframes, the static value alone. public void ClearKeyframes() ClearTrack() Clears the track entirely, falling back to StaticValue — the schema doc leaves it open whether an empty track persists or is cleared to null; this is the explicit \"clear it\" path for a caller that wants that. public void ClearTrack() Duplicate() Deep copy. The track copies itself so a subclass (PositionTrack) survives as that subclass, spatial handles and all — rebuilding a plain KeyframeTrack here is what used to flatten a split fragment's motion path into straight lines. public Animatable<T> Duplicate() Returns Animatable<T> Evaluate(TimeSpan) public T Evaluate(TimeSpan clipRelativeTime) Parameters clipRelativeTime TimeSpan Returns T GetOrCreateTrack() Lazily creates the backing track on first use — a caller adding its first keyframe shouldn't have to separately new up a KeyframeTrack<T> first. public KeyframeTrack<T> GetOrCreateTrack() Returns KeyframeTrack<T> GetStaticValue() The static value, untyped — for editors that only know the descriptor. public object? GetStaticValue() Returns object RemoveKeyframeAt(TimeSpan) Removes the keyframe at exactly this time, if any. public bool RemoveKeyframeAt(TimeSpan time) Parameters time TimeSpan Returns bool SetKeyframe(TimeSpan, object?) Adds a keyframe at the time, or updates the one already there. Creates the track if there was none. public void SetKeyframe(TimeSpan time, object? value) Parameters time TimeSpan value object SetStaticValue(object?) Writes the static value from an untyped editor, coercing numbers and enums. public void SetStaticValue(object? value) Parameters value object ShiftKeyframes(TimeSpan) Moves every keyframe by amount. Keyframes are anchored to the content, so a head trim/extend shifts them all. public void ShiftKeyframes(TimeSpan amount) Parameters amount TimeSpan Operators implicit operator Animatable<T>(T) public static implicit operator Animatable<T>(T value) Parameters value T Returns Animatable<T>"
  },
  "api/EditSharp.Components.Channels.AudioChannel.html": {
    "href": "api/EditSharp.Components.Channels.AudioChannel.html",
    "title": "Class AudioChannel | EditSharp",
    "summary": "Class AudioChannel Namespace EditSharp.Components.Channels Assembly EditSharp.dll public sealed class AudioChannel : Channel Inheritance object Channel AudioChannel Inherited Members Channel.Id Channel.Name Channel.Clips Channel.Transitions Channel.Timeline Channel.Index Channel.MoveUp() Channel.MoveDown() Channel.End Channel.AddClip(Clip) Channel.RippleAddClip(Clip) Channel.RemoveRange(TimeSpan, TimeSpan) Channel.RippleRemoveRange(TimeSpan, TimeSpan) Channel.AddTransition(Transition) Channel.RemoveTransition(Transition) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Volume [Editable(\"Volume\", Min = 0, Max = 2, Step = 0.01)] public float Volume { get; set; } Property Value float Methods IsValidClipType(Clip) protected override bool IsValidClipType(Clip clip) Parameters clip Clip Returns bool"
  },
  "api/EditSharp.Components.Channels.Channel.html": {
    "href": "api/EditSharp.Components.Channels.Channel.html",
    "title": "Class Channel | EditSharp",
    "summary": "Class Channel Namespace EditSharp.Components.Channels Assembly EditSharp.dll A single row of clips. Enforces the one invariant that matters here: clips on a channel never overlap, except exactly two joined by a Transition may overlap for that transition's Duration. REWRITE note (\"clips are graphs\"): the old TimelineVideoClip/ TimelineAudioClip Clip subtypes are gone — an embedded nested Timeline is now just a TimelineVideoInputNode/TimelineAudioInputNode somewhere inside an ordinary VideoClip's/AudioClip's graph (see Clip.cs's own remarks). That means the cycle-check and Timeline.UsedBy bookkeeping that used to live in Timeline.AddTimelineClipCore (a SEPARATE add path from ordinary Channel.AddClip, reserved specifically for the old dedicated clip types) has moved here, into AddClipCore — the ONE place any clip newly enters a channel via the public Add path — generalized to scan whatever embed nodes a clip's graph happens to contain, of which there can now be any number (zero, one, or several, in a graph with multiple InputNodes). See Timeline.ValidateNoCycle/RegisterEmbeddedTimelines/NotifyClipDetached. public abstract class Channel Inheritance object Channel Derived AudioChannel VideoChannel Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Clips public IReadOnlyCollection<Clip> Clips { get; } Property Value IReadOnlyCollection<Clip> End public TimeSpan End { get; } Property Value TimeSpan Id Runtime identity (not saved), e.g. for tapping a channel's audio. public Guid Id { get; } Property Value Guid Index This channel's position among channels of its own kind — 0 is the bottom layer, matching VideoChannels'/AudioChannels' own index order (see Timeline's remarks on the video/audio split). -1 if this channel isn't currently placed on any Timeline. public int Index { get; } Property Value int Name [Editable(\"Name\")] public string Name { get; set; } Property Value string Timeline public Timeline? Timeline { get; } Property Value Timeline Transitions public IReadOnlyList<Transition> Transitions { get; } Property Value IReadOnlyList<Transition> Methods AddClip(Clip) public Clip AddClip(Clip clip) Parameters clip Clip Returns Clip AddTransition(Transition) FOUND IN THE FIELD, FIXED: this used to call the ordinary to.ExtendStart(achievableHalf) / from.ExtendEnd(achievableHalf) — both of which resolve conflicts via the normal Overwrite path, with NO exclusion. Since to.Start == from.End by construction (that's what \"adjacent\" means here), each extend's own Overwrite call found the OTHER transition partner sitting exactly in the region it was trying to claim, and trimmed it right back — extending to's head trimmed from's tail back to where it started, then extending from's tail pushed to's head back out to where IT started. Net effect: both clips ended up completely unchanged, transition.Duration was set to a value that no longer matched to.Start == from.End at all, and the very next trim/extend on either clip would silently delete this \"transition\" via ReconcileTransitionsFor's own consistency check — no crossfade region was ever actually created. Fix: call ExtendHead/ExtendTail directly (bypassing Clip's own ExtendStart/ExtendEnd wrappers) with each other passed as exclude, since a transition's whole point is to make exactly these two clips overlap — every OTHER neighbor on the channel still gets the normal overwrite treatment. public Transition AddTransition(Transition transition) Parameters transition Transition Returns Transition IsValidClipType(Clip) protected abstract bool IsValidClipType(Clip clip) Parameters clip Clip Returns bool MoveDown() Swaps this channel with the one directly below it. No-op at the bottom. public void MoveDown() MoveUp() Swaps this channel with the one directly above it (higher index — for VideoChannels that's the next layer drawn on top; AudioChannels' order carries no acoustic meaning, only UI listing order — see AudioMixer). No-op if this channel is already the topmost of its kind, or isn't placed on a Timeline. public void MoveUp() RemoveRange(TimeSpan, TimeSpan) Takes everything inside [start, end) off this channel: a clip wholly inside goes, one spanning an edge is trimmed to the edge, one spanning both is split around the range. Overwrite semantics with nothing placed in the hole. public void RemoveRange(TimeSpan start, TimeSpan end) Parameters start TimeSpan end TimeSpan RemoveTransition(Transition) public void RemoveTransition(Transition transition) Parameters transition Transition RippleAddClip(Clip) public Clip RippleAddClip(Clip clip) Parameters clip Clip Returns Clip RippleRemoveRange(TimeSpan, TimeSpan) RemoveRange, then closes the gap: everything that started at or after end moves earlier by the range's length. public void RippleRemoveRange(TimeSpan start, TimeSpan end) Parameters start TimeSpan end TimeSpan"
  },
  "api/EditSharp.Components.Channels.ChannelBlendMode.html": {
    "href": "api/EditSharp.Components.Channels.ChannelBlendMode.html",
    "title": "Enum ChannelBlendMode | EditSharp",
    "summary": "Enum ChannelBlendMode Namespace EditSharp.Components.Channels Assembly EditSharp.dll Replaces the old ffmpeg-era BlendMode enum (deleted). Rather than exposing SKBlendMode directly on Channel.BlendMode, this mirrors every native Skia blend mode 1:1 by name AND reserves four extra slots for the old enum's arithmetic modes that have no SKBlendMode equivalent at all (Average, Negation, Divide, Subtract — Photoshop/ GIMP-era formulas, not Porter-Duff/CSS compositing operators). Why a wrapper again, after item 7 deliberately deleted the old wrapper in favour of raw SKBlendMode: those four modes need SOMEWHERE to live if they're ever implemented via a custom SKRuntimeEffect (SkSL) shader, and SKBlendMode is a sealed native enum — there's no way to add a value to it. A superset wrapper gives them a stable, permanent slot in the public API now, so wiring in a real implementation later is additive (ChannelCompositor's mapping gains a case) rather than another breaking enum swap on Channel. The four reserved slots THROW today (see ChannelCompositor's mapping) rather than silently doing nothing or falling back to SrcOver — a clip configured with one of these gets a clear error, not a silently-wrong render. public enum ChannelBlendMode Fields Average = 29 Clear = 0 Color = 27 ColorBurn = 19 ColorDodge = 18 Darken = 16 Difference = 22 Divide = 31 Dst = 2 DstATop = 10 DstIn = 6 DstOut = 8 DstOver = 4 Exclusion = 23 HardLight = 20 Hue = 25 Lighten = 17 Luminosity = 28 Modulate = 13 Multiply = 24 Negation = 30 Overlay = 15 Plus = 12 Saturation = 26 Screen = 14 SoftLight = 21 Src = 1 SrcATop = 9 SrcIn = 5 SrcOut = 7 SrcOver = 3 Subtract = 32 Xor = 11"
  },
  "api/EditSharp.Components.Channels.VideoChannel.html": {
    "href": "api/EditSharp.Components.Channels.VideoChannel.html",
    "title": "Class VideoChannel | EditSharp",
    "summary": "Class VideoChannel Namespace EditSharp.Components.Channels Assembly EditSharp.dll public sealed class VideoChannel : Channel Inheritance object Channel VideoChannel Inherited Members Channel.Id Channel.Name Channel.Clips Channel.Transitions Channel.Timeline Channel.Index Channel.MoveUp() Channel.MoveDown() Channel.End Channel.AddClip(Clip) Channel.RippleAddClip(Clip) Channel.RemoveRange(TimeSpan, TimeSpan) Channel.RippleRemoveRange(TimeSpan, TimeSpan) Channel.AddTransition(Transition) Channel.RemoveTransition(Transition) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties BlendMode [Editable(\"Blend mode\")] public ChannelBlendMode BlendMode { get; set; } Property Value ChannelBlendMode Methods IsValidClipType(Clip) protected override bool IsValidClipType(Clip clip) Parameters clip Clip Returns bool"
  },
  "api/EditSharp.Components.Channels.html": {
    "href": "api/EditSharp.Components.Channels.html",
    "title": "Namespace EditSharp.Components.Channels | EditSharp",
    "summary": "Namespace EditSharp.Components.Channels Classes AudioChannel Channel A single row of clips. Enforces the one invariant that matters here: clips on a channel never overlap, except exactly two joined by a Transition may overlap for that transition's Duration. REWRITE note (\"clips are graphs\"): the old TimelineVideoClip/ TimelineAudioClip Clip subtypes are gone — an embedded nested Timeline is now just a TimelineVideoInputNode/TimelineAudioInputNode somewhere inside an ordinary VideoClip's/AudioClip's graph (see Clip.cs's own remarks). That means the cycle-check and Timeline.UsedBy bookkeeping that used to live in Timeline.AddTimelineClipCore (a SEPARATE add path from ordinary Channel.AddClip, reserved specifically for the old dedicated clip types) has moved here, into AddClipCore — the ONE place any clip newly enters a channel via the public Add path — generalized to scan whatever embed nodes a clip's graph happens to contain, of which there can now be any number (zero, one, or several, in a graph with multiple InputNodes). See Timeline.ValidateNoCycle/RegisterEmbeddedTimelines/NotifyClipDetached. VideoChannel Enums ChannelBlendMode Replaces the old ffmpeg-era BlendMode enum (deleted). Rather than exposing SKBlendMode directly on Channel.BlendMode, this mirrors every native Skia blend mode 1:1 by name AND reserves four extra slots for the old enum's arithmetic modes that have no SKBlendMode equivalent at all (Average, Negation, Divide, Subtract — Photoshop/ GIMP-era formulas, not Porter-Duff/CSS compositing operators). Why a wrapper again, after item 7 deliberately deleted the old wrapper in favour of raw SKBlendMode: those four modes need SOMEWHERE to live if they're ever implemented via a custom SKRuntimeEffect (SkSL) shader, and SKBlendMode is a sealed native enum — there's no way to add a value to it. A superset wrapper gives them a stable, permanent slot in the public API now, so wiring in a real implementation later is additive (ChannelCompositor's mapping gains a case) rather than another breaking enum swap on Channel. The four reserved slots THROW today (see ChannelCompositor's mapping) rather than silently doing nothing or falling back to SrcOver — a clip configured with one of these gets a clear error, not a silently-wrong render."
  },
  "api/EditSharp.Components.Clips.AudioClip.html": {
    "href": "api/EditSharp.Components.Clips.AudioClip.html",
    "title": "Class AudioClip | EditSharp",
    "summary": "Class AudioClip Namespace EditSharp.Components.Clips Assembly EditSharp.dll The only concrete audible Clip type — mirrors VideoClip exactly, one domain over. What used to be AudioClip's own flat Source property and the distinct TimelineAudioClip Clip subtype are now InputNode types wired into this class's single Audio-domain Graph — see the static factory methods below and EditSharp.Components.Nodes.Sources. Also new in this rewrite: CreateTone, wrapping the brand-new ToneGeneratorInputNode (there was no synthesized-tone clip type at all before this). public sealed class AudioClip : Clip, ITimelineEditable Inheritance object Clip AudioClip Implements ITimelineEditable Inherited Members Clip.Name Clip.Start Clip.Duration Clip.End Clip.Speed Clip.ContentDuration Clip.LinkGroupId Clip.Channel Clip.MinimumDuration Clip.HeadExtendLimit Clip.TailExtendLimit Clip.TrimStart(TimeSpan) Clip.TrimEnd(TimeSpan) Clip.ExtendStart(TimeSpan) Clip.RippleExtendStart(TimeSpan) Clip.ExtendEnd(TimeSpan) Clip.RippleExtendEnd(TimeSpan) Clip.StretchStart(TimeSpan) Clip.StretchEnd(TimeSpan) Clip.Move(TimeSpan, Channel) Clip.RippleMove(TimeSpan, Channel) Clip.Split(TimeSpan) Clip.Delete() Clip.RippleDelete() Clip.StartsAt(TimeSpan) Clip.EndsAt(TimeSpan) Clip.StartsInside(Clip) Clip.EndsInside(Clip) Clip.StartIntersectsWith(Clip) Clip.EndIntersectsWith(Clip) Clip.FullyIntersects(Clip) Clip.IsLongerThan(Clip) Clip.IntersectionWith(Clip) Clip.DistanceFrom(Clip) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Graph A clip's single Graph, fixed to this clip's own NodeDomain at construction (Image for VideoClip, Audio for AudioClip) — see this class's own remarks. public override Graph Graph { get; } Property Value Graph PreservePitch How the clip keeps its pitch when Speed isn't 1x. [Editable(\"Preserve pitch\")] public PitchPreservation PreservePitch { get; set; } Property Value PitchPreservation Methods CreateCustom(Graph, TimeSpan, TimeSpan) Escape hatch for a fully custom graph — see VideoClip.CreateCustom's own remarks. public static AudioClip CreateCustom(Graph graph, TimeSpan start, TimeSpan duration) Parameters graph Graph start TimeSpan duration TimeSpan Returns AudioClip CreateFromSource(AudioSource, TimeSpan, TimeSpan) public static AudioClip CreateFromSource(AudioSource source, TimeSpan start, TimeSpan duration) Parameters source AudioSource start TimeSpan duration TimeSpan Returns AudioClip CreateTimelineEmbed(Timeline, TimeSpan, TimeSpan) public static AudioClip CreateTimelineEmbed(Timeline timeline, TimeSpan start, TimeSpan duration) Parameters timeline Timeline start TimeSpan duration TimeSpan Returns AudioClip CreateTone(TimeSpan, TimeSpan, Waveform, float, float) public static AudioClip CreateTone(TimeSpan start, TimeSpan duration, Waveform waveform = Waveform.Sine, float frequencyHz = 440, float amplitude = 1) Parameters start TimeSpan duration TimeSpan waveform Waveform frequencyHz float amplitude float Returns AudioClip Duplicate() Deep copy — does NOT copy LinkGroupId/Channel (a duplicate starts unlinked and unplaced; the caller decides where it goes). public override AudioClip Duplicate() Returns AudioClip"
  },
  "api/EditSharp.Components.Clips.Clip.html": {
    "href": "api/EditSharp.Components.Clips.Clip.html",
    "title": "Class Clip | EditSharp",
    "summary": "Class Clip Namespace EditSharp.Components.Clips Assembly EditSharp.dll FUNDAMENTAL REWRITE (\"clips are graphs\"): a clip used to be treated as \"media that can have a graph of effects\" — a Clip subtype per kind of content (VideoClip/TextClip/GeneratorClip/NoiseClip/ TimelineVideoClip on the visual side; AudioClip/TimelineAudioClip on the audible side), each wrapping its own bespoke content field, with a Graph bolted on afterward. That was backwards: a clip IS its graph. The only remaining concrete Clip subtypes are VideoClip and AudioClip — everything that used to be a distinct Clip subtype (a media file, text, a generated color, procedural noise, a synthesized tone, an embedded nested Timeline) is now just a different InputNode wired into an otherwise perfectly ordinary Graph (see EditSharp.Components.Nodes). A graph may have more than one InputNode — nothing about a graph having two, three, or more requires a different kind of Clip; it's still just VideoClip or AudioClip. Abstract base, implements ITimelineEditable. public abstract class Clip : ITimelineEditable Inheritance object Clip Implements ITimelineEditable Derived AudioClip VideoClip Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Fields MinimumDuration public static readonly TimeSpan MinimumDuration Field Value TimeSpan Properties Channel public Channel? Channel { get; } Property Value Channel ContentDuration The span of content this clip covers — Duration scaled by Speed. public TimeSpan ContentDuration { get; } Property Value TimeSpan Duration [Editable(\"Duration\", Order = 2)] public TimeSpan Duration { get; set; } Property Value TimeSpan End public TimeSpan End { get; } Property Value TimeSpan Graph A clip's single Graph, fixed to this clip's own NodeDomain at construction (Image for VideoClip, Audio for AudioClip) — see this class's own remarks. public abstract Graph Graph { get; } Property Value Graph HeadExtendLimit How much earlier Start can be pulled before the content runs out, in TIMELINE time (MaxHeadExtend is content time, and the two differ once Speed is not 1). TimeSpan.MaxValue when nothing in the graph constrains it. Exposed so an editor can clamp a preview to what ExtendStart will actually accept. public TimeSpan HeadExtendLimit { get; } Property Value TimeSpan LinkGroupId public Guid? LinkGroupId { get; } Property Value Guid? Name [Editable(\"Name\", Order = 0)] public string Name { get; set; } Property Value string Speed [Editable(\"Speed\", Order = 3, Min = 0.01, Max = 100, Step = 0.01, Editor = PropertyEditor.Percent, Default = 1)] public double Speed { get; set; } Property Value double Start [Editable(\"Start\", Order = 1)] public TimeSpan Start { get; set; } Property Value TimeSpan TailExtendLimit How much later End can be pushed before a source runs out, in TIMELINE time: the tightest of the trimmable inputs with a known hard end, zero if one already ends inside the clip. TimeSpan.MaxValue when none has one (unbounded, looping, or not probed yet). public TimeSpan TailExtendLimit { get; } Property Value TimeSpan Methods Delete() public void Delete() DistanceFrom(Clip) public TimeSpan DistanceFrom(Clip other) Parameters other Clip Returns TimeSpan Duplicate() Deep copy — does NOT copy LinkGroupId/Channel (a duplicate starts unlinked and unplaced; the caller decides where it goes). public abstract Clip Duplicate() Returns Clip EndIntersectsWith(Clip) public bool EndIntersectsWith(Clip other) Parameters other Clip Returns bool EndsAt(TimeSpan) public bool EndsAt(TimeSpan time) Parameters time TimeSpan Returns bool EndsInside(Clip) public bool EndsInside(Clip other) Parameters other Clip Returns bool ExtendEnd(TimeSpan) Grows from the tail (Overwrite default) — can newly overlap a neighbor. public void ExtendEnd(TimeSpan amount) Parameters amount TimeSpan ExtendStart(TimeSpan) Grows from the head (Overwrite default) — can newly overlap a neighbor. public void ExtendStart(TimeSpan amount) Parameters amount TimeSpan FullyIntersects(Clip) public bool FullyIntersects(Clip other) Parameters other Clip Returns bool IntersectionWith(Clip) public TimeSpan IntersectionWith(Clip other) Parameters other Clip Returns TimeSpan IsLongerThan(Clip) public bool IsLongerThan(Clip other) Parameters other Clip Returns bool MaxHeadExtend() The most-constrained trimmable input node sets the ceiling for the WHOLE clip — extending the head further than any ONE of them has room for would push that node's in-point negative. TimeSpan.MaxValue (unconstrained) when there are no trimmable inputs at all in this clip's graph (pure generator/text/noise/ tone content) — matches the old TextClip/GeneratorClip/ NoiseClip's inherited unbounded default. protected virtual TimeSpan MaxHeadExtend() Returns TimeSpan Move(TimeSpan, Channel?) Overwrite semantics at the destination — see Placement Integrity in the schema doc. targetChannel defaults to the clip's current channel; a different, type-compatible channel is a cross-channel move. public void Move(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel OnHeadInPointShift(TimeSpan) Positive amount = trim (in-point advances), negative = extend (in-point recedes). protected virtual void OnHeadInPointShift(TimeSpan amount) Parameters amount TimeSpan RippleDelete() Delete, and close the gap this clip leaves on its own channel. public void RippleDelete() RippleExtendEnd(TimeSpan) public void RippleExtendEnd(TimeSpan amount) Parameters amount TimeSpan RippleExtendStart(TimeSpan) public void RippleExtendStart(TimeSpan amount) Parameters amount TimeSpan RippleMove(TimeSpan, Channel?) Same as Move, but destination-side neighbors shift later instead of being overwritten. public void RippleMove(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel Split(TimeSpan) Cuts at a point in time. On a Clip, splits just that clip (both halves keep the same LinkGroupId). On a LinkGroup, splits every member spanning the point and resolves into two groups — see the schema doc's Split section for the full DaVinci-Resolve-matching behavior. public void Split(TimeSpan at) Parameters at TimeSpan StartIntersectsWith(Clip) public bool StartIntersectsWith(Clip other) Parameters other Clip Returns bool StartsAt(TimeSpan) public bool StartsAt(TimeSpan time) Parameters time TimeSpan Returns bool StartsInside(Clip) public bool StartsInside(Clip other) Parameters other Clip Returns bool StretchEnd(TimeSpan) Positive amount = End moves later (clip grows, plays slower); negative = earlier (shrinks, plays faster). Start never moves. public void StretchEnd(TimeSpan amount) Parameters amount TimeSpan StretchStart(TimeSpan) Positive amount = Start moves earlier (clip grows, plays slower); negative = later (shrinks, plays faster). End never moves. public void StretchStart(TimeSpan amount) Parameters amount TimeSpan TrimEnd(TimeSpan) Shrinks from the tail — never creates overlap, so there is no Ripple variant. public void TrimEnd(TimeSpan amount) Parameters amount TimeSpan TrimStart(TimeSpan) Shrinks from the head — never creates overlap, so there is no Ripple variant. public void TrimStart(TimeSpan amount) Parameters amount TimeSpan"
  },
  "api/EditSharp.Components.Clips.ClipTransform.html": {
    "href": "api/EditSharp.Components.Clips.ClipTransform.html",
    "title": "Class ClipTransform | EditSharp",
    "summary": "Class ClipTransform Namespace EditSharp.Components.Clips Assembly EditSharp.dll Every field is individually Animatable<T> — see the schema doc's Keyframes section. This replaces the old model where an entire ClipTransform snapshot was one keyframe; here, Position can animate on a completely different schedule than Scale. public sealed class ClipTransform Inheritance object ClipTransform Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every track on this transform — see Node.Animatables. public IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Pitch [Editable(\"Pitch\", Editor = PropertyEditor.Angle)] public Animatable<float> Pitch { get; set; } Property Value Animatable<float> Position [Editable(\"Position\", Frame = FrameMeasure.HalfFrame)] public Animatable<Vector2> Position { get; set; } Property Value Animatable<Vector2> Rotation [Editable(\"Rotation\", Editor = PropertyEditor.Angle)] public Animatable<float> Rotation { get; set; } Property Value Animatable<float> Scale [Editable(\"Scale\")] public Animatable<Vector2> Scale { get; set; } Property Value Animatable<Vector2> Yaw [Editable(\"Yaw\", Editor = PropertyEditor.Angle)] public Animatable<float> Yaw { get; set; } Property Value Animatable<float> Methods Duplicate() public ClipTransform Duplicate() Returns ClipTransform Evaluate(TimeSpan) public ResolvedTransform Evaluate(TimeSpan clipRelativeTime) Parameters clipRelativeTime TimeSpan Returns ResolvedTransform FromPosition(Positions) public static Vector2 FromPosition(Positions position) Parameters position Positions Returns Vector2 UsePositionTrack() Position specifically gets the spatial-handle PositionTrack treatment (see the schema doc) — this is a convenience that installs one and points Position.Track at it, rather than a second, separate field. Calling it more than once is a no-op if Position already has a track. public PositionTrack UsePositionTrack() Returns PositionTrack"
  },
  "api/EditSharp.Components.Clips.PitchPreservation.html": {
    "href": "api/EditSharp.Components.Clips.PitchPreservation.html",
    "title": "Enum PitchPreservation | EditSharp",
    "summary": "Enum PitchPreservation Namespace EditSharp.Components.Clips Assembly EditSharp.dll How a clip's audio keeps its pitch when its speed isn't 1x. public enum PitchPreservation Fields Off = 0 Varispeed: faster is higher, slower is lower, like tape. PhaseVocoder = 2 Phase vocoder with phase locking. Smoother on sustained, tonal material; softer transients. WSOLA = 1 Waveform-similarity overlap-add. Cheap; clean on speech and most music."
  },
  "api/EditSharp.Components.Clips.Positions.html": {
    "href": "api/EditSharp.Components.Clips.Positions.html",
    "title": "Enum Positions | EditSharp",
    "summary": "Enum Positions Namespace EditSharp.Components.Clips Assembly EditSharp.dll public enum Positions Fields BottomCenter = 7 BottomLeft = 6 BottomRight = 8 CenterLeft = 3 CenterMiddle = 4 CenterRight = 5 TopCenter = 1 TopLeft = 0 TopRight = 2"
  },
  "api/EditSharp.Components.Clips.ResolvedTransform.html": {
    "href": "api/EditSharp.Components.Clips.ResolvedTransform.html",
    "title": "Struct ResolvedTransform | EditSharp",
    "summary": "Struct ResolvedTransform Namespace EditSharp.Components.Clips Assembly EditSharp.dll A resolved, plain-value snapshot of a ClipTransform at one instant — no tracks, just concrete numbers. This is what render code actually consumes; it replaces the old Clip.TransformAt(time)'s role, now composed from each field's own independently-timed Animatable<T> track instead of interpolating between two flat ClipTransform blobs. public readonly record struct ResolvedTransform : IEquatable<ResolvedTransform> Implements IEquatable<ResolvedTransform> Inherited Members ValueType.Equals(object) ValueType.GetHashCode() ValueType.ToString() object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) Constructors ResolvedTransform(Vector2, Vector2, float, float, float) A resolved, plain-value snapshot of a ClipTransform at one instant — no tracks, just concrete numbers. This is what render code actually consumes; it replaces the old Clip.TransformAt(time)'s role, now composed from each field's own independently-timed Animatable<T> track instead of interpolating between two flat ClipTransform blobs. public ResolvedTransform(Vector2 Position, Vector2 Scale, float Rotation, float Pitch, float Yaw) Parameters Position Vector2 Scale Vector2 Rotation float Pitch float Yaw float Properties Pitch public float Pitch { get; init; } Property Value float Position public Vector2 Position { get; init; } Property Value Vector2 Rotation public float Rotation { get; init; } Property Value float Scale public Vector2 Scale { get; init; } Property Value Vector2 Yaw public float Yaw { get; init; } Property Value float"
  },
  "api/EditSharp.Components.Clips.VideoClip.html": {
    "href": "api/EditSharp.Components.Clips.VideoClip.html",
    "title": "Class VideoClip | EditSharp",
    "summary": "Class VideoClip Namespace EditSharp.Components.Clips Assembly EditSharp.dll The only concrete visual Clip type — see Clip's own remarks on the \"clips are graphs\" rewrite. What used to be TextClip/GeneratorClip/ NoiseClip/TimelineVideoClip (each a distinct Clip subtype) and VideoClip's own flat Source property are now all just different InputNode types wired into this class's single Image-domain Graph — see the static factory methods below and EditSharp.Components.Nodes.Sources. Sealed and otherwise data-less beyond Start/Duration/LinkGroupId (inherited from Clip) and Graph — everything else a visual clip used to carry directly (Modulate, Transform) now lives on nodes INSIDE the graph (TintNode, TransformNode), not on this class. public sealed class VideoClip : Clip, ITimelineEditable Inheritance object Clip VideoClip Implements ITimelineEditable Inherited Members Clip.Name Clip.Start Clip.Duration Clip.End Clip.Speed Clip.ContentDuration Clip.LinkGroupId Clip.Channel Clip.MinimumDuration Clip.HeadExtendLimit Clip.TailExtendLimit Clip.TrimStart(TimeSpan) Clip.TrimEnd(TimeSpan) Clip.ExtendStart(TimeSpan) Clip.RippleExtendStart(TimeSpan) Clip.ExtendEnd(TimeSpan) Clip.RippleExtendEnd(TimeSpan) Clip.StretchStart(TimeSpan) Clip.StretchEnd(TimeSpan) Clip.Move(TimeSpan, Channel) Clip.RippleMove(TimeSpan, Channel) Clip.Split(TimeSpan) Clip.Delete() Clip.RippleDelete() Clip.StartsAt(TimeSpan) Clip.EndsAt(TimeSpan) Clip.StartsInside(Clip) Clip.EndsInside(Clip) Clip.StartIntersectsWith(Clip) Clip.EndIntersectsWith(Clip) Clip.FullyIntersects(Clip) Clip.IsLongerThan(Clip) Clip.IntersectionWith(Clip) Clip.DistanceFrom(Clip) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Graph A clip's single Graph, fixed to this clip's own NodeDomain at construction (Image for VideoClip, Audio for AudioClip) — see this class's own remarks. public override Graph Graph { get; } Property Value Graph Methods CreateColorGenerator(SKColor, TimeSpan, TimeSpan) public static VideoClip CreateColorGenerator(SKColor color, TimeSpan start, TimeSpan duration) Parameters color SKColor start TimeSpan duration TimeSpan Returns VideoClip CreateCustom(Graph, TimeSpan, TimeSpan) Escape hatch for a fully custom graph — multiple InputNodes, branches merged through MergeNode, extra effect nodes, whatever an author wants. graph must already be a valid Image-domain Graph (see Graph.CreateEmptyVideoGraph to start one from scratch, or build on a Create* factory's own graph after construction via clip.Graph.AddNode(...)/Connect(...)). public static VideoClip CreateCustom(Graph graph, TimeSpan start, TimeSpan duration) Parameters graph Graph start TimeSpan duration TimeSpan Returns VideoClip CreateFromSource(VideoSource, TimeSpan, TimeSpan) public static VideoClip CreateFromSource(VideoSource source, TimeSpan start, TimeSpan duration) Parameters source VideoSource start TimeSpan duration TimeSpan Returns VideoClip CreateNoise(TimeSpan, TimeSpan, int?, float, float) public static VideoClip CreateNoise(TimeSpan start, TimeSpan duration, int? seed = null, float detail = 0.03, float seetheRate = 0.03) Parameters start TimeSpan duration TimeSpan seed int? detail float seetheRate float Returns VideoClip CreateText(string, TimeSpan, TimeSpan, string, int, bool, HorizontalTextAlignment, float) public static VideoClip CreateText(string content, TimeSpan start, TimeSpan duration, string font = \"Comic Sans MS\", int weight = 400, bool italic = false, HorizontalTextAlignment align = HorizontalTextAlignment.Center, float size = 0.05) Parameters content string start TimeSpan duration TimeSpan font string weight int italic bool align HorizontalTextAlignment size float Returns VideoClip CreateTimelineEmbed(Timeline, TimeSpan, TimeSpan) public static VideoClip CreateTimelineEmbed(Timeline timeline, TimeSpan start, TimeSpan duration) Parameters timeline Timeline start TimeSpan duration TimeSpan Returns VideoClip Duplicate() Deep copy — does NOT copy LinkGroupId/Channel (a duplicate starts unlinked and unplaced; the caller decides where it goes). public override VideoClip Duplicate() Returns VideoClip"
  },
  "api/EditSharp.Components.Clips.html": {
    "href": "api/EditSharp.Components.Clips.html",
    "title": "Namespace EditSharp.Components.Clips | EditSharp",
    "summary": "Namespace EditSharp.Components.Clips Classes AudioClip The only concrete audible Clip type — mirrors VideoClip exactly, one domain over. What used to be AudioClip's own flat Source property and the distinct TimelineAudioClip Clip subtype are now InputNode types wired into this class's single Audio-domain Graph — see the static factory methods below and EditSharp.Components.Nodes.Sources. Also new in this rewrite: CreateTone, wrapping the brand-new ToneGeneratorInputNode (there was no synthesized-tone clip type at all before this). Clip FUNDAMENTAL REWRITE (\"clips are graphs\"): a clip used to be treated as \"media that can have a graph of effects\" — a Clip subtype per kind of content (VideoClip/TextClip/GeneratorClip/NoiseClip/ TimelineVideoClip on the visual side; AudioClip/TimelineAudioClip on the audible side), each wrapping its own bespoke content field, with a Graph bolted on afterward. That was backwards: a clip IS its graph. The only remaining concrete Clip subtypes are VideoClip and AudioClip — everything that used to be a distinct Clip subtype (a media file, text, a generated color, procedural noise, a synthesized tone, an embedded nested Timeline) is now just a different InputNode wired into an otherwise perfectly ordinary Graph (see EditSharp.Components.Nodes). A graph may have more than one InputNode — nothing about a graph having two, three, or more requires a different kind of Clip; it's still just VideoClip or AudioClip. Abstract base, implements ITimelineEditable. ClipTransform Every field is individually Animatable<T> — see the schema doc's Keyframes section. This replaces the old model where an entire ClipTransform snapshot was one keyframe; here, Position can animate on a completely different schedule than Scale. VideoClip The only concrete visual Clip type — see Clip's own remarks on the \"clips are graphs\" rewrite. What used to be TextClip/GeneratorClip/ NoiseClip/TimelineVideoClip (each a distinct Clip subtype) and VideoClip's own flat Source property are now all just different InputNode types wired into this class's single Image-domain Graph — see the static factory methods below and EditSharp.Components.Nodes.Sources. Sealed and otherwise data-less beyond Start/Duration/LinkGroupId (inherited from Clip) and Graph — everything else a visual clip used to carry directly (Modulate, Transform) now lives on nodes INSIDE the graph (TintNode, TransformNode), not on this class. Structs ResolvedTransform A resolved, plain-value snapshot of a ClipTransform at one instant — no tracks, just concrete numbers. This is what render code actually consumes; it replaces the old Clip.TransformAt(time)'s role, now composed from each field's own independently-timed Animatable<T> track instead of interpolating between two flat ClipTransform blobs. Enums PitchPreservation How a clip's audio keeps its pitch when its speed isn't 1x. Positions"
  },
  "api/EditSharp.Components.FontFamilies.html": {
    "href": "api/EditSharp.Components.FontFamilies.html",
    "title": "Class FontFamilies | EditSharp",
    "summary": "Class FontFamilies Namespace EditSharp.Components Assembly EditSharp.dll The font families installed on this machine, by name. public static class FontFamilies Inheritance object FontFamilies Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Installed Every installed family, Title Cased, sorted. Re-read at most every two seconds, so a new install shows up. public static IReadOnlyList<string> Installed { get; } Property Value IReadOnlyList<string> Methods IsInstalled(string) public static bool IsInstalled(string family) Parameters family string Returns bool Nearest(IReadOnlyList<int>, int) Of weights, the one nearest weight; weight itself when there are none. public static int Nearest(IReadOnlyList<int> weights, int weight) Parameters weights IReadOnlyList<int> weight int Returns int Resolve(string, SKFontStyle) The family's typeface in this style; a missing family draws with the default font and warns once. public static SKTypeface Resolve(string family, SKFontStyle style) Parameters family string style SKFontStyle Returns SKTypeface TitleCase(string) Each word's first letter capitalized; a word already in capitals (UI, MS) is left as it is. Nothing is lowercased. public static string TitleCase(string name) Parameters name string Returns string WeightName(int) A weight's usual name (\"Semibold\"), by the nearest standard weight. public static string WeightName(int weight) Parameters weight int Returns string WeightsOf(string) The weights a family ships, lightest first; empty when it isn't installed. public static IReadOnlyList<int> WeightsOf(string family) Parameters family string Returns IReadOnlyList<int>"
  },
  "api/EditSharp.Components.IAnimatable.html": {
    "href": "api/EditSharp.Components.IAnimatable.html",
    "title": "Interface IAnimatable | EditSharp",
    "summary": "Interface IAnimatable Namespace EditSharp.Components Assembly EditSharp.dll public interface IAnimatable Properties IsAnimated True once the track has enough keyframes to override the static value. bool IsAnimated { get; } Property Value bool Keyframes Every keyframe on this property, in time order; empty when it has no track. Times are content-relative, like everything else on a track. IReadOnlyList<IKeyframe> Keyframes { get; } Property Value IReadOnlyList<IKeyframe> ValueType The T in Animatable<T>. Type ValueType { get; } Property Value Type Methods ClearKeyframes() Drops the track: no keyframes, the static value alone. void ClearKeyframes() Evaluate(TimeSpan) The value at a content time, keyframes or not. object? Evaluate(TimeSpan clipRelativeTime) Parameters clipRelativeTime TimeSpan Returns object GetStaticValue() The static value, untyped — for editors that only know the descriptor. object? GetStaticValue() Returns object RemoveKeyframeAt(TimeSpan) Removes the keyframe at exactly this time, if any. bool RemoveKeyframeAt(TimeSpan time) Parameters time TimeSpan Returns bool SetKeyframe(TimeSpan, object?) Adds a keyframe at the time, or updates the one already there. Creates the track if there was none. void SetKeyframe(TimeSpan time, object? value) Parameters time TimeSpan value object SetStaticValue(object?) Writes the static value from an untyped editor, coercing numbers and enums. void SetStaticValue(object? value) Parameters value object ShiftKeyframes(TimeSpan) Moves every keyframe by amount. Keyframes are anchored to the content, so a head trim/extend shifts them all. void ShiftKeyframes(TimeSpan amount) Parameters amount TimeSpan"
  },
  "api/EditSharp.Components.IInterpolator-1.html": {
    "href": "api/EditSharp.Components.IInterpolator-1.html",
    "title": "Interface IInterpolator<T> | EditSharp",
    "summary": "Interface IInterpolator<T> Namespace EditSharp.Components Assembly EditSharp.dll The arithmetic Animatable<T>/KeyframeTrack<T> need to do their job: add/subtract two values (for handle offsets, which are stored as deltas from their keyframe's own value) and blend two values by a normalized progress (for the cubic evaluation itself). Deliberately a small strategy interface resolved per-T, NOT a self-referential generic constraint (T : IInterpolatable<T>) — the three real payload types this schema keyframes (float, Vector2, SKColor) are all BCL/SkiaSharp types this package doesn't own, so they can't be made to declare an interface themselves. See Interpolators below for the registry that resolves one of these per T. public interface IInterpolator<T> Type Parameters T Methods Add(T, T) T Add(T a, T b) Parameters a T b T Returns T Lerp(T, T, float) T Lerp(T a, T b, float t) Parameters a T b T t float Returns T Scale(T, float) T Scale(T a, float s) Parameters a T s float Returns T Subtract(T, T) T Subtract(T a, T b) Parameters a T b T Returns T"
  },
  "api/EditSharp.Components.IKeyframe.html": {
    "href": "api/EditSharp.Components.IKeyframe.html",
    "title": "Interface IKeyframe | EditSharp",
    "summary": "Interface IKeyframe Namespace EditSharp.Components Assembly EditSharp.dll The general per-property wrapper — see the schema doc's Keyframes section. Wrapping a property in Animatable<T> is the only step required to make it keyframeable; nothing about KeyframeTrack<T> itself needs to change for a new property type (as long as an IInterpolator<T> is registered — see Interpolators.Resolve). public interface IKeyframe Properties Start TimeSpan Start { get; } Property Value TimeSpan Value object? Value { get; } Property Value object"
  },
  "api/EditSharp.Components.ITimelineEditable.html": {
    "href": "api/EditSharp.Components.ITimelineEditable.html",
    "title": "Interface ITimelineEditable | EditSharp",
    "summary": "Interface ITimelineEditable Namespace EditSharp.Components Assembly EditSharp.dll The core behavior contract shared by Clip and LinkGroup — see the schema doc's \"The core behavior rule\" section. Called on a Clip, every member here affects only that clip, exactly like an unlinked clip's behavior always has, regardless of whether it belongs to a LinkGroup. Called on a LinkGroup, the same member coordinates the edit across every member, handling type/constraint mismatches internally. This is a universal contract — any operation added here in the future automatically gets that same scoping from both implementations, not a per-operation special case. Exact member shapes are this rewrite's own call (the schema doc explicitly deferred \"exact ITimelineEditable signatures\"); Split deliberately returns void rather than the new fragments — the encapsulation principle means a channel's Clips collection is the source of truth for what exists after a split, not a value handed back from the call that caused it. public interface ITimelineEditable Methods Delete() void Delete() ExtendEnd(TimeSpan) Grows from the tail (Overwrite default) — can newly overlap a neighbor. void ExtendEnd(TimeSpan amount) Parameters amount TimeSpan ExtendStart(TimeSpan) Grows from the head (Overwrite default) — can newly overlap a neighbor. void ExtendStart(TimeSpan amount) Parameters amount TimeSpan Move(TimeSpan, Channel?) Overwrite semantics at the destination — see Placement Integrity in the schema doc. targetChannel defaults to the clip's current channel; a different, type-compatible channel is a cross-channel move. void Move(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel RippleExtendEnd(TimeSpan) void RippleExtendEnd(TimeSpan amount) Parameters amount TimeSpan RippleExtendStart(TimeSpan) void RippleExtendStart(TimeSpan amount) Parameters amount TimeSpan RippleMove(TimeSpan, Channel?) Same as Move, but destination-side neighbors shift later instead of being overwritten. void RippleMove(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel Split(TimeSpan) Cuts at a point in time. On a Clip, splits just that clip (both halves keep the same LinkGroupId). On a LinkGroup, splits every member spanning the point and resolves into two groups — see the schema doc's Split section for the full DaVinci-Resolve-matching behavior. void Split(TimeSpan at) Parameters at TimeSpan TrimEnd(TimeSpan) Shrinks from the tail — never creates overlap, so there is no Ripple variant. void TrimEnd(TimeSpan amount) Parameters amount TimeSpan TrimStart(TimeSpan) Shrinks from the head — never creates overlap, so there is no Ripple variant. void TrimStart(TimeSpan amount) Parameters amount TimeSpan"
  },
  "api/EditSharp.Components.InterpolationType.html": {
    "href": "api/EditSharp.Components.InterpolationType.html",
    "title": "Enum InterpolationType | EditSharp",
    "summary": "Enum InterpolationType Namespace EditSharp.Components Assembly EditSharp.dll public enum InterpolationType Fields Bezier = 2 Hold = 0 Linear = 1"
  },
  "api/EditSharp.Components.Interpolators.html": {
    "href": "api/EditSharp.Components.Interpolators.html",
    "title": "Class Interpolators | EditSharp",
    "summary": "Class Interpolators Namespace EditSharp.Components Assembly EditSharp.dll Resolves the IInterpolator<T> for a payload type. Covers the three types this schema actually keyframes today (float, Vector2, SKColor — see the Effects/Keyframes sections of the schema doc). A new Animatable<T> payload type needs one line registered here; that is the entire cost of the \"new properties can easily become animatable\" goal for anything that isn't one of these three already. public static class Interpolators Inheritance object Interpolators Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Register<T>(IInterpolator<T>) public static void Register<T>(IInterpolator<T> interpolator) Parameters interpolator IInterpolator<T> Type Parameters T Resolve<T>() public static IInterpolator<T> Resolve<T>() Returns IInterpolator<T> Type Parameters T"
  },
  "api/EditSharp.Components.Keyframe-1.html": {
    "href": "api/EditSharp.Components.Keyframe-1.html",
    "title": "Class Keyframe<T> | EditSharp",
    "summary": "Class Keyframe<T> Namespace EditSharp.Components Assembly EditSharp.dll public class Keyframe<T> : IKeyframe Type Parameters T Inheritance object Keyframe<T> Implements IKeyframe Derived SpatialKeyframe Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties InHandle public KeyframeHandle<T>? InHandle { get; } Property Value KeyframeHandle<T> InInterpolation public InterpolationType InInterpolation { get; set; } Property Value InterpolationType InTangentMode public TangentMode InTangentMode { get; } Property Value TangentMode OutHandle public KeyframeHandle<T>? OutHandle { get; } Property Value KeyframeHandle<T> OutInterpolation public InterpolationType OutInterpolation { get; set; } Property Value InterpolationType OutTangentMode public TangentMode OutTangentMode { get; } Property Value TangentMode Start public TimeSpan Start { get; } Property Value TimeSpan Value public T Value { get; set; } Property Value T"
  },
  "api/EditSharp.Components.KeyframeHandle-1.html": {
    "href": "api/EditSharp.Components.KeyframeHandle-1.html",
    "title": "Class KeyframeHandle<T> | EditSharp",
    "summary": "Class KeyframeHandle<T> Namespace EditSharp.Components Assembly EditSharp.dll One end of a keyframe's bezier handle: a 2D offset from the keyframe itself (time offset + value offset), not a single scalar \"how much speed to remove\" the way the old EaseInStrength/EaseOutStrength were. See the migration note on Keyframe.cs's old shape. CONVENTION: TimeOffset is always stored as a non-negative magnitude — \"how far this handle reaches toward the OTHER keyframe of the segment,\" regardless of whether it's an in-handle or an out-handle. An out-handle is applied by ADDING TimeOffset to its own keyframe's Start; an in-handle is applied by SUBTRACTING TimeOffset from its own keyframe's Start. This is what makes \"clamp so it can never cross the neighboring keyframe\" a single one-directional clamp (TimeOffset can never exceed the gap to the neighbor) instead of needing a sign check at every call site. public sealed class KeyframeHandle<T> Type Parameters T Inheritance object KeyframeHandle<T> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties TimeOffset public TimeSpan TimeOffset { get; } Property Value TimeSpan ValueOffset public T ValueOffset { get; } Property Value T"
  },
  "api/EditSharp.Components.KeyframeTrack-1.html": {
    "href": "api/EditSharp.Components.KeyframeTrack-1.html",
    "title": "Class KeyframeTrack<T> | EditSharp",
    "summary": "Class KeyframeTrack<T> Namespace EditSharp.Components Assembly EditSharp.dll One animatable property's own timeline of keyframes. See the schema doc's Keyframes section for the full design — this is \"one track per animatable property,\" replacing the old whole-ClipTransform-per- keyframe list. Encapsulation principle applies here same as everywhere else in the schema: Keyframes is read-only externally, all mutation goes through this class's own methods — which is what makes handle-clamping and Auto-tangent-recompute-on-neighbor-change actually enforceable rather than something a caller could bypass by editing a Keyframe directly. public class KeyframeTrack<T> Type Parameters T Inheritance object KeyframeTrack<T> Derived PositionTrack Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Constructors KeyframeTrack() public KeyframeTrack() KeyframeTrack(IInterpolator<T>) public KeyframeTrack(IInterpolator<T> interpolator) Parameters interpolator IInterpolator<T> Properties Keyframes public IReadOnlyList<Keyframe<T>> Keyframes { get; } Property Value IReadOnlyList<Keyframe<T>> Methods AddKeyframe(TimeSpan, T) Adding at a Start that already has a keyframe updates that keyframe's Value in place rather than creating a duplicate entry — no two keyframes on one track can share a Start, and this is the least surprising way to enforce it (matches \"add a keyframe at the current time\" in every mainstream tool when one's already there). public Keyframe<T> AddKeyframe(TimeSpan start, T value) Parameters start TimeSpan value T Returns Keyframe<T> CopyKeyframeExtras(Keyframe<T>, Keyframe<T>) Copies whatever a keyframe subtype carries beyond the base fields — see PositionTrack. protected virtual void CopyKeyframeExtras(Keyframe<T> source, Keyframe<T> target) Parameters source Keyframe<T> target Keyframe<T> CreateEmptyCopy() An empty track of this exact type, for Duplicate — a subclass returns its own type. protected virtual KeyframeTrack<T> CreateEmptyCopy() Returns KeyframeTrack<T> CreateKeyframe(TimeSpan, T) Factory hook so a subclass (PositionTrack) can hand back its own Keyframe subtype (SpatialKeyframe) instead of a plain Keyframe<T>, without AddKeyframe itself needing to know about that. protected virtual Keyframe<T> CreateKeyframe(TimeSpan start, T value) Parameters start TimeSpan value T Returns Keyframe<T> Duplicate() Deep copy, of the same concrete track type — see CreateEmptyCopy/CopyKeyframeExtras. public KeyframeTrack<T> Duplicate() Returns KeyframeTrack<T> Evaluate(TimeSpan) Fewer than 2 keyframes: Animatable<T> handles the StaticValue fallback, but Evaluate is still well-defined on its own (holds the single keyframe's value, or the interpolator's default if empty). public T Evaluate(TimeSpan time) Parameters time TimeSpan Returns T InterpolateSegment(Keyframe<T>, Keyframe<T>, TimeSpan) Resolves one segment's value at time. Virtual so PositionTrack can reuse the Hold/Linear/Bezier TIMING logic (via the protected SolveU helper) while swapping in a spatial bezier for the actual value, instead of this class's plain value-cubic — see the schema doc's \"two-stage, deliberately NOT independent per-axis easing\" remark on PositionTrack. protected virtual T InterpolateSegment(Keyframe<T> from, Keyframe<T> to, TimeSpan time) Parameters from Keyframe<T> to Keyframe<T> time TimeSpan Returns T MoveKeyframe(Keyframe<T>, TimeSpan, T) Clamps newStart so the keyframe can never move onto or past a neighboring keyframe on the same track — same clamping principle already applied to a handle's own TimeOffset, just applied to the keyframe's own position now too. public void MoveKeyframe(Keyframe<T> keyframe, TimeSpan newStart, T newValue) Parameters keyframe Keyframe<T> newStart TimeSpan newValue T RemoveKeyframe(Keyframe<T>) A track reduced to zero keyframes is a valid state, not an error — Animatable<T>.Evaluate falls back to StaticValue whenever there are fewer than 2 (0 or 1 both qualify). public void RemoveKeyframe(Keyframe<T> keyframe) Parameters keyframe Keyframe<T> SetHandle(Keyframe<T>, bool, TimeSpan, T) Sets a handle's offset, clamping TimeOffset so it can never cross the neighboring keyframe on that side. Setting a handle on a keyframe whose TangentMode for that side is Auto switches it to Free first — matches After-Effects-style \"manually dragging an Auto handle promotes it to Free.\" Smooth mirrors the OTHER side's handle through the keyframe automatically: setting one side of a Smooth-paired keyframe updates the mirrored side too, so the curve stays continuous without the caller having to set both by hand. public void SetHandle(Keyframe<T> keyframe, bool isInHandle, TimeSpan timeOffset, T valueOffset) Parameters keyframe Keyframe<T> isInHandle bool timeOffset TimeSpan valueOffset T SetTangentMode(Keyframe<T>, bool, TangentMode) public void SetTangentMode(Keyframe<T> keyframe, bool isIn, TangentMode mode) Parameters keyframe Keyframe<T> isIn bool mode TangentMode Shift(TimeSpan) Moves every keyframe by amount, in place. Handles are offsets from their own keyframe, so they come along untouched. Keyframes may end up before zero: a head trim leaves the ones it cut past at negative times, so an extend back restores them exactly. public void Shift(TimeSpan amount) Parameters amount TimeSpan SolveU(TimeSpan, Keyframe<T>, Keyframe<T>) Shared by PositionTrack's temporal stage — see its own remarks. protected static float SolveU(TimeSpan time, Keyframe<T> from, Keyframe<T> to) Parameters time TimeSpan from Keyframe<T> to Keyframe<T> Returns float"
  },
  "api/EditSharp.Components.LinkGroup.html": {
    "href": "api/EditSharp.Components.LinkGroup.html",
    "title": "Class LinkGroup | EditSharp",
    "summary": "Class LinkGroup Namespace EditSharp.Components Assembly EditSharp.dll A real object owning coordination logic, not just a shared Guid — see the schema doc. Carries no state beyond Id and which Timeline to derive membership from; Members is always a live scan (matches the doc: \"IReadOnlyList<Clip> Members; // derived from Clip.LinkGroupId == Id\"), so Timeline.GetLinkGroup handing back a fresh instance on every call is correct, not wasteful. Membership is flat — a Clip belongs to at most one LinkGroup (a single nullable LinkGroupId, not a collection); merge-on-link is how two groups combine instead of nesting. public sealed class LinkGroup : ITimelineEditable Inheritance object LinkGroup Implements ITimelineEditable Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Id public Guid Id { get; } Property Value Guid Members public IReadOnlyList<Clip> Members { get; } Property Value IReadOnlyList<Clip> Methods CreateTimelineAudioVideoPair(Timeline, TimeSpan, TimeSpan) A linked video + audio pair of clips, both embedding timeline. Returns them unattached; placing them is up to the caller. public static (VideoClip Video, AudioClip Audio) CreateTimelineAudioVideoPair(Timeline timeline, TimeSpan start, TimeSpan duration) Parameters timeline Timeline start TimeSpan duration TimeSpan Returns (VideoClip Video, AudioClip Audio) Delete() public void Delete() ExtendEnd(TimeSpan) Grows from the tail (Overwrite default) — can newly overlap a neighbor. public void ExtendEnd(TimeSpan amount) Parameters amount TimeSpan ExtendStart(TimeSpan) Grows from the head (Overwrite default) — can newly overlap a neighbor. public void ExtendStart(TimeSpan amount) Parameters amount TimeSpan Move(TimeSpan, Channel?) Overwrite semantics at the destination — see Placement Integrity in the schema doc. targetChannel defaults to the clip's current channel; a different, type-compatible channel is a cross-channel move. public void Move(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel MoveChannel(int) public void MoveChannel(int delta) Parameters delta int RippleExtendEnd(TimeSpan) public void RippleExtendEnd(TimeSpan amount) Parameters amount TimeSpan RippleExtendStart(TimeSpan) public void RippleExtendStart(TimeSpan amount) Parameters amount TimeSpan RippleMove(TimeSpan, Channel?) Same as Move, but destination-side neighbors shift later instead of being overwritten. public void RippleMove(TimeSpan newStart, Channel? targetChannel = null) Parameters newStart TimeSpan targetChannel Channel RippleMoveChannel(int) public void RippleMoveChannel(int delta) Parameters delta int Split(TimeSpan) Cuts at a point in time. On a Clip, splits just that clip (both halves keep the same LinkGroupId). On a LinkGroup, splits every member spanning the point and resolves into two groups — see the schema doc's Split section for the full DaVinci-Resolve-matching behavior. public void Split(TimeSpan at) Parameters at TimeSpan TrimEnd(TimeSpan) Shrinks from the tail — never creates overlap, so there is no Ripple variant. public void TrimEnd(TimeSpan amount) Parameters amount TimeSpan TrimStart(TimeSpan) Shrinks from the head — never creates overlap, so there is no Ripple variant. public void TrimStart(TimeSpan amount) Parameters amount TimeSpan"
  },
  "api/EditSharp.Components.Nodes.AudioOutputNode.html": {
    "href": "api/EditSharp.Components.Nodes.AudioOutputNode.html",
    "title": "Class AudioOutputNode | EditSharp",
    "summary": "Class AudioOutputNode Namespace EditSharp.Components.Nodes Assembly EditSharp.dll Feeds the audio mixer. Mandatory, not removable — the one fixed anchor left in an Audio-domain graph. public sealed class AudioOutputNode : OutputNode Inheritance object Node OutputNode AudioOutputNode Inherited Members Node.Id Node.Enabled Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.CompositeNode.html": {
    "href": "api/EditSharp.Components.Nodes.CompositeNode.html",
    "title": "Class CompositeNode | EditSharp",
    "summary": "Class CompositeNode Namespace EditSharp.Components.Nodes Assembly EditSharp.dll A graph folded into one node — what a user gets by assembling a graph and saving it as a node of their own. It has the same domain as the graph inside it, one output (whatever feeds the inner graph's OutputNode), and as inputs exactly the inner input ports it chose to expose. Its editable properties are the inner ones it chose to expose, each under its own name (see IInspectable). Nothing evaluates a composite as such: Graph.Flattened replaces every composite with the nodes inside it before an evaluator sees the graph, so the effect and audio pipelines never know it was there. The inner nodes are the same objects, so their content, keyframes and in-points are found and shifted like any other's (see Graph.AllNodes). Not yet persisted — that arrives with the project file format. public sealed class CompositeNode : Node, IInspectable Inheritance object Node CompositeNode Implements IInspectable Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors CompositeNode(Graph, string) public CompositeNode(Graph inner, string name = \"Custom node\") Parameters inner Graph name string Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> ExposedProperties public IReadOnlyList<ExposedProperty> ExposedProperties { get; } Property Value IReadOnlyList<ExposedProperty> Inner public Graph Inner { get; } Property Value Graph Inputs public IReadOnlyList<ExposedPort> Inputs { get; } Property Value IReadOnlyList<ExposedPort> Name [Editable(\"Name\", Order = -90)] public string Name { get; set; } Property Value string Output The composite's single output: the inner graph's own output, in the graph's domain. public NodePort Output { get; } Property Value NodePort Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Properties public IReadOnlyList<PropertyDescriptor> Properties { get; } Property Value IReadOnlyList<PropertyDescriptor> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node ExposeInput(Node, string, string?) Shows an inner node's unconnected input port as one of this node's own. Recorded. public ExposedPort ExposeInput(Node node, string port, string? name = null) Parameters node Node port string name string Returns ExposedPort ExposeProperty(Node, string, string?) Shows an inner node's editable property as one of this node's own. Recorded. public ExposedProperty ExposeProperty(Node node, string property, string? name = null) Parameters node Node property string name string Returns ExposedProperty HideInput(ExposedPort) public void HideInput(ExposedPort exposed) Parameters exposed ExposedPort HideProperty(ExposedProperty) public void HideProperty(ExposedProperty exposed) Parameters exposed ExposedProperty"
  },
  "api/EditSharp.Components.Nodes.Connection.html": {
    "href": "api/EditSharp.Components.Nodes.Connection.html",
    "title": "Class Connection | EditSharp",
    "summary": "Class Connection Namespace EditSharp.Components.Nodes Assembly EditSharp.dll public sealed class Connection Inheritance object Connection Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties FromNodeId public Guid FromNodeId { get; } Property Value Guid FromPort public string FromPort { get; } Property Value string ToNodeId public Guid ToNodeId { get; } Property Value Guid ToPort public string ToPort { get; } Property Value string"
  },
  "api/EditSharp.Components.Nodes.Effects.AudioMixNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.AudioMixNode.html",
    "title": "Class AudioMixNode | EditSharp",
    "summary": "Class AudioMixNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll The audio-domain equivalent of MergeNode — enables parallel processing (e.g. parallel compression: split the signal, compress one branch heavily, mix it back under the dry signal), and the canonical way to combine TWO InputNodes in one audio graph now that a graph can have more than one. public sealed class AudioMixNode : Node Inheritance object Node AudioMixNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> MixA [Editable(\"Mix A\", Min = 0, Max = 1, Step = 0.01)] public Animatable<float> MixA { get; set; } Property Value Animatable<float> MixB [Editable(\"Mix B\", Min = 0, Max = 1, Step = 0.01)] public Animatable<float> MixB { get; set; } Property Value Animatable<float> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.BlurNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.BlurNode.html",
    "title": "Class BlurNode | EditSharp",
    "summary": "Class BlurNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class BlurNode : Node Inheritance object Node BlurNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Radius [Editable(\"Radius\", Min = 0, Max = 0.5, Step = 0.001, Unit = \"of width\", Frame = FrameMeasure.Width)] public Animatable<float> Radius { get; set; } Property Value Animatable<float> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.CompressorNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.CompressorNode.html",
    "title": "Class CompressorNode | EditSharp",
    "summary": "Class CompressorNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class CompressorNode : Node Inheritance object Node CompressorNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> AttackMs [Editable(\"Attack\", Min = 0, Max = 500, Step = 1, Unit = \"ms\")] public Animatable<float> AttackMs { get; set; } Property Value Animatable<float> MakeupGainDb [Editable(\"Makeup gain\", Min = -24, Max = 24, Step = 0.5, Unit = \"dB\")] public Animatable<float> MakeupGainDb { get; set; } Property Value Animatable<float> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Ratio [Editable(\"Ratio\", Min = 1, Max = 20, Step = 0.1)] public Animatable<float> Ratio { get; set; } Property Value Animatable<float> ReleaseMs [Editable(\"Release\", Min = 0, Max = 2000, Step = 1, Unit = \"ms\")] public Animatable<float> ReleaseMs { get; set; } Property Value Animatable<float> Threshold [Editable(\"Threshold\", Min = -60, Max = 0, Step = 0.5, Unit = \"dB\")] public Animatable<float> Threshold { get; set; } Property Value Animatable<float> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.DropShadowNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.DropShadowNode.html",
    "title": "Class DropShadowNode | EditSharp",
    "summary": "Class DropShadowNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class DropShadowNode : Node Inheritance object Node DropShadowNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Blur [Editable(\"Blur\", Min = 0, Max = 0.5, Step = 0.001, Frame = FrameMeasure.Width)] public Animatable<float> Blur { get; set; } Property Value Animatable<float> Color [Editable(\"Color\")] public Animatable<SKColor> Color { get; set; } Property Value Animatable<SKColor> Offset [Editable(\"Offset\", Frame = FrameMeasure.HalfFrame)] public Animatable<Vector2> Offset { get; set; } Property Value Animatable<Vector2> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.EQBand.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.EQBand.html",
    "title": "Class EQBand | EditSharp",
    "summary": "Class EQBand Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class EQBand Inheritance object EQBand Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties FrequencyHz [Editable(\"Frequency\", Min = 20, Max = 20000, Step = 1, Unit = \"Hz\")] public Animatable<float> FrequencyHz { get; set; } Property Value Animatable<float> GainDb [Editable(\"Gain\", Min = -24, Max = 24, Step = 0.5, Unit = \"dB\")] public Animatable<float> GainDb { get; set; } Property Value Animatable<float> Q [Editable(\"Q\", Min = 0.1, Max = 10, Step = 0.1)] public Animatable<float> Q { get; set; } Property Value Animatable<float> Methods Duplicate() public EQBand Duplicate() Returns EQBand"
  },
  "api/EditSharp.Components.Nodes.Effects.EQNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.EQNode.html",
    "title": "Class EQNode | EditSharp",
    "summary": "Class EQNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class EQNode : Node Inheritance object Node EQNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Bands [Editable(\"Bands\")] public List<EQBand> Bands { get; set; } Property Value List<EQBand> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.GainNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.GainNode.html",
    "title": "Class GainNode | EditSharp",
    "summary": "Class GainNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll Auto-created and wired in by default (see Graph.CreateAudioGraph) — this is what replaces the old flat AudioClip.Volume field. A normal, removable node beyond that default: an artist can delete it, add several gain stages at different points in a chain, or route around it entirely. public sealed class GainNode : Node Inheritance object Node GainNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Gain [Editable(\"Gain\", Min = 0, Max = 4, Step = 0.01)] public Animatable<float> Gain { get; set; } Property Value Animatable<float> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.ImageToMaskNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.ImageToMaskNode.html",
    "title": "Class ImageToMaskNode | EditSharp",
    "summary": "Class ImageToMaskNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class ImageToMaskNode : Node Inheritance object Node ImageToMaskNode Inherited Members Node.Id Node.Enabled Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Channel [Editable(\"Channel\")] public MaskChannelSource Channel { get; set; } Property Value MaskChannelSource Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.MaskChannelSource.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.MaskChannelSource.html",
    "title": "Enum MaskChannelSource | EditSharp",
    "summary": "Enum MaskChannelSource Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public enum MaskChannelSource Fields Alpha = 1 Luma = 0"
  },
  "api/EditSharp.Components.Nodes.Effects.MaskCombineMode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.MaskCombineMode.html",
    "title": "Enum MaskCombineMode | EditSharp",
    "summary": "Enum MaskCombineMode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public enum MaskCombineMode Fields Add = 0 Intersect = 2 Subtract = 1"
  },
  "api/EditSharp.Components.Nodes.Effects.MaskCombineNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.MaskCombineNode.html",
    "title": "Class MaskCombineNode | EditSharp",
    "summary": "Class MaskCombineNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class MaskCombineNode : Node Inheritance object Node MaskCombineNode Inherited Members Node.Id Node.Enabled Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Mode [Editable(\"Mode\")] public MaskCombineMode Mode { get; set; } Property Value MaskCombineMode Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.MergeNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.MergeNode.html",
    "title": "Class MergeNode | EditSharp",
    "summary": "Class MergeNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll Branch/recombine — the canonical node-graph pattern (split into two branches, effect each differently, recombine) that justifies a graph over a flat stack in the first place. Also the canonical way to combine TWO InputNodes in one graph now that a graph can have more than one (e.g. two VideoSourceNodes composited together). public sealed class MergeNode : Node Inheritance object Node MergeNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> BlendMode [Editable(\"Blend mode\")] public ChannelBlendMode BlendMode { get; set; } Property Value ChannelBlendMode Mix [Editable(\"Mix\", Min = 0, Max = 1, Step = 0.01)] public Animatable<float> Mix { get; set; } Property Value Animatable<float> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.RoundedCornersNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.RoundedCornersNode.html",
    "title": "Class RoundedCornersNode | EditSharp",
    "summary": "Class RoundedCornersNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public sealed class RoundedCornersNode : Node Inheritance object Node RoundedCornersNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Radius [Editable(\"Radius\", Min = 0, Max = 0.5, Step = 0.001)] public Animatable<float> Radius { get; set; } Property Value Animatable<float> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.ShapeMaskNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.ShapeMaskNode.html",
    "title": "Class ShapeMaskNode | EditSharp",
    "summary": "Class ShapeMaskNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll Geometry is normalized 0-1 — a fraction of whatever image this mask ends up merged against — rather than absolute pixels. See the schema doc's \"Masks are canvas-agnostic\" note for why this sidesteps the pre-/post-TransformNode coordinate-space question entirely. public sealed class ShapeMaskNode : Node Inheritance object Node ShapeMaskNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Feather [Editable(\"Feather\", Min = 0, Max = 1, Step = 0.001)] public Animatable<float> Feather { get; set; } Property Value Animatable<float> PolygonPoints [Editable(\"Points\", Frame = FrameMeasure.HalfFrame)] [VisibleWhen(\"Shape\", new object?[] { ShapeType.Polygon })] public List<Animatable<Vector2>> PolygonPoints { get; set; } Property Value List<Animatable<Vector2>> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Position [Editable(\"Position\", Frame = FrameMeasure.HalfFrame)] public Animatable<Vector2> Position { get; set; } Property Value Animatable<Vector2> Rotation [Editable(\"Rotation\", Editor = PropertyEditor.Angle)] public Animatable<float> Rotation { get; set; } Property Value Animatable<float> Shape [Editable(\"Shape\")] public ShapeType Shape { get; set; } Property Value ShapeType Size [Editable(\"Size\", Frame = FrameMeasure.Frame)] public Animatable<Vector2> Size { get; set; } Property Value Animatable<Vector2> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.ShapeType.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.ShapeType.html",
    "title": "Enum ShapeType | EditSharp",
    "summary": "Enum ShapeType Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll public enum ShapeType Fields Ellipse = 1 Polygon = 2 Rectangle = 0"
  },
  "api/EditSharp.Components.Nodes.Effects.TintNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.TintNode.html",
    "title": "Class TintNode | EditSharp",
    "summary": "Class TintNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll Color tint, doubling as transparency via alpha. Auto-created and wired in by default (see Graph.CreateVideoGraph) alongside TransformNode — this is what replaced the old flat VisualClip.Modulate property entirely, fully consistent with how GainNode already replaced the old flat AudioClip.Volume: a normal, removable, reorderable node beyond the default, not a fixed clip property any more. public sealed class TintNode : Node Inheritance object Node TintNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Color [Editable(\"Color\")] public Animatable<SKColor> Color { get; set; } Property Value Animatable<SKColor> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.TransformNode.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.TransformNode.html",
    "title": "Class TransformNode | EditSharp",
    "summary": "Class TransformNode Namespace EditSharp.Components.Nodes.Effects Assembly EditSharp.dll Wraps a clip's transform. Auto-created and wired in by default when a VideoClip's graph is first constructed, but is otherwise a normal, removable, rewireable node — this is what replaces the old EffectStage.PreTransform/PostTransform enum entirely (see the schema doc): \"stage\" is purely a function of a node's position relative to TransformNode in the graph, not a fixed property of an effect type. REWRITE: this node now carries its OWN ClipTransform data (Position/ Scale/Rotation/Pitch/Yaw), rather than being a data-less marker whose data lived on the owning VisualClip — there is no more VisualClip to hold it now that VideoClip is the only concrete visual Clip type and a clip IS its graph. A graph is free to have more than one TransformNode (e.g. one per branch before a MergeNode) now that Transform data travels with the node instead of the clip. public sealed class TransformNode : Node Inheritance object Node TransformNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Transform [Editable(\"Transform\")] public ClipTransform Transform { get; set; } Property Value ClipTransform Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Effects.html": {
    "href": "api/EditSharp.Components.Nodes.Effects.html",
    "title": "Namespace EditSharp.Components.Nodes.Effects | EditSharp",
    "summary": "Namespace EditSharp.Components.Nodes.Effects Classes AudioMixNode The audio-domain equivalent of MergeNode — enables parallel processing (e.g. parallel compression: split the signal, compress one branch heavily, mix it back under the dry signal), and the canonical way to combine TWO InputNodes in one audio graph now that a graph can have more than one. BlurNode CompressorNode DropShadowNode EQBand EQNode GainNode Auto-created and wired in by default (see Graph.CreateAudioGraph) — this is what replaces the old flat AudioClip.Volume field. A normal, removable node beyond that default: an artist can delete it, add several gain stages at different points in a chain, or route around it entirely. ImageToMaskNode MaskCombineNode MergeNode Branch/recombine — the canonical node-graph pattern (split into two branches, effect each differently, recombine) that justifies a graph over a flat stack in the first place. Also the canonical way to combine TWO InputNodes in one graph now that a graph can have more than one (e.g. two VideoSourceNodes composited together). RoundedCornersNode ShapeMaskNode Geometry is normalized 0-1 — a fraction of whatever image this mask ends up merged against — rather than absolute pixels. See the schema doc's \"Masks are canvas-agnostic\" note for why this sidesteps the pre-/post-TransformNode coordinate-space question entirely. TintNode Color tint, doubling as transparency via alpha. Auto-created and wired in by default (see Graph.CreateVideoGraph) alongside TransformNode — this is what replaced the old flat VisualClip.Modulate property entirely, fully consistent with how GainNode already replaced the old flat AudioClip.Volume: a normal, removable, reorderable node beyond the default, not a fixed clip property any more. TransformNode Wraps a clip's transform. Auto-created and wired in by default when a VideoClip's graph is first constructed, but is otherwise a normal, removable, rewireable node — this is what replaces the old EffectStage.PreTransform/PostTransform enum entirely (see the schema doc): \"stage\" is purely a function of a node's position relative to TransformNode in the graph, not a fixed property of an effect type. REWRITE: this node now carries its OWN ClipTransform data (Position/ Scale/Rotation/Pitch/Yaw), rather than being a data-less marker whose data lived on the owning VisualClip — there is no more VisualClip to hold it now that VideoClip is the only concrete visual Clip type and a clip IS its graph. A graph is free to have more than one TransformNode (e.g. one per branch before a MergeNode) now that Transform data travels with the node instead of the clip. Enums MaskChannelSource MaskCombineMode ShapeType"
  },
  "api/EditSharp.Components.Nodes.ExposedPort.html": {
    "href": "api/EditSharp.Components.Nodes.ExposedPort.html",
    "title": "Class ExposedPort | EditSharp",
    "summary": "Class ExposedPort Namespace EditSharp.Components.Nodes Assembly EditSharp.dll An inner node's input port, shown on the composite under Name. public sealed class ExposedPort Inheritance object ExposedPort Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Name public string Name { get; } Property Value string Node public Guid Node { get; } Property Value Guid Port public string Port { get; } Property Value string"
  },
  "api/EditSharp.Components.Nodes.ExposedProperty.html": {
    "href": "api/EditSharp.Components.Nodes.ExposedProperty.html",
    "title": "Class ExposedProperty | EditSharp",
    "summary": "Class ExposedProperty Namespace EditSharp.Components.Nodes Assembly EditSharp.dll An inner node's editable property, shown on the composite under Name. public sealed class ExposedProperty Inheritance object ExposedProperty Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Name public string Name { get; } Property Value string Node public Guid Node { get; } Property Value Guid Property public string Property { get; } Property Value string"
  },
  "api/EditSharp.Components.Nodes.Graph.html": {
    "href": "api/EditSharp.Components.Nodes.Graph.html",
    "title": "Class Graph | EditSharp",
    "summary": "Class Graph Namespace EditSharp.Components.Nodes Assembly EditSharp.dll A directed graph of nodes — see the schema doc's \"Effects: Node-Based Compositing Graph\" section for the full design. The same primitives here power both the Image/Mask domain (VideoClip) and the Audio domain (AudioClip); NodeDomain is what keeps the two from ever being wired together, EXCEPT for domain-universal nodes (see InferDomain below), which is exactly how the \"base node type for math functions that are universally applicable\" the user asked for is implemented: ValueConstantNode/MathNode (see EditSharp.Components.Nodes.Math) declare only PortType.Value ports, which InferDomain treats as belonging to NEITHER domain specifically, and therefore addable to either. FUNDAMENTAL REWRITE (\"clips are graphs\"): there is no longer a second fixed anchor alongside OutputNode. The old ImageSourceNode/ AudioSourceNode were each a single, mandatory, non-removable \"the clip's content enters here\" anchor — every clip's graph had EXACTLY one. That model assumed a clip was \"media with a graph of effects bolted on\". The corrected model is that a clip IS its graph, and a graph can have as many InputNodes as an author wants (a minimum of one for the graph to produce anything, enforced the same way it always effectively was — OutputNode must have SOME path reaching it at render time). See InputNode's own remarks. Encapsulation principle applies: Nodes/Connections are read-only externally, all mutation goes through AddNode/RemoveNode/Connect/ Disconnect — which is what makes \"no dangling references, no cycles, no type mismatches\" an actual invariant rather than a convention a caller could violate by touching a public list directly. public sealed class Graph Inheritance object Graph Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties AllNodes Every node, reaching inside composites — for anything that has to find all the sources, trimmable inputs or embeds a clip really contains. Inner OutputNodes are left out; they are plumbing, not content. Just Nodes when nothing is composite. public IEnumerable<Node> AllNodes { get; } Property Value IEnumerable<Node> Animatables Every keyframe track on every node — see Node.Animatables. public IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Connections public IReadOnlyList<Connection> Connections { get; } Property Value IReadOnlyList<Connection> Domain public NodeDomain Domain { get; } Property Value NodeDomain Flattened The graph as an evaluator should see it: every CompositeNode replaced by the nodes inside it, its connections rewired to the inner ports they were really aimed at. The inner nodes are the same objects, so content keyed by their ids still matches. This graph itself when there is nothing to flatten, so the ordinary case costs nothing. public Graph Flattened { get; } Property Value Graph InputNodes Convenience access to every InputNode currently in this graph, without the caller having to filter Nodes by hand — see InputNode's own remarks. public IReadOnlyList<InputNode> InputNodes { get; } Property Value IReadOnlyList<InputNode> Nodes public IReadOnlyList<Node> Nodes { get; } Property Value IReadOnlyList<Node> OutputNode public OutputNode OutputNode { get; } Property Value OutputNode Methods AddNode(Node) Rejects a node whose port domain doesn't match this graph's own (universal nodes always pass). public Node AddNode(Node node) Parameters node Node Returns Node Connect(Guid, string, Guid, string) Validates port types match, no cycle results, and the target input port doesn't already have an incoming connection (every input port — OutputNode included — accepts at most one; an interactive graph editor disconnects before reconnecting rather than this silently accumulating extra edges into one input). Zero connections into a mandatory input is a valid, if incomplete, intermediate state — see the schema doc's own remarks on why this isn't rejected at edit time. public Connection Connect(Guid fromNode, string fromPort, Guid toNode, string toPort) Parameters fromNode Guid fromPort string toNode Guid toPort string Returns Connection CreateAudioGraph(InputNode) A \"normal/default\" audio clip: a single InputNode you supply (a AudioSourceNode wrapping a Source, in the common case — see AudioClip's own static factories), wired through GainNode — what replaced the old flat AudioClip.Volume field, unchanged from before this rewrite. public static Graph CreateAudioGraph(InputNode input) Parameters input InputNode Returns Graph CreateEmptyAudioGraph() public static Graph CreateEmptyAudioGraph() Returns Graph CreateEmptyVideoGraph() An empty graph with no InputNode at all yet — for building a fully custom multi-input graph from scratch (see VideoClip.CreateCustom/AudioClip.CreateCustom). Not renderable until at least one node reaches OutputNode — same as any other disconnected-Output state, not a special case. public static Graph CreateEmptyVideoGraph() Returns Graph CreateVideoGraph(InputNode) A \"normal/default\" video clip: a single InputNode you supply (a VideoSourceNode wrapping a Source, in the common case, but any InputNode works — see VideoClip's own static factories), wired through the two nodes every visual clip gets by default: TintNode (what replaced the old flat Modulate/tint-and-opacity property) and TransformNode (what replaced the old flat ClipTransform property — its data now lives ON the node itself, not on the owning clip). Both are ordinary, removable, reorderable nodes beyond this default, exactly like GainNode always has been on the audio side. public static Graph CreateVideoGraph(InputNode input) Parameters input InputNode Returns Graph Disconnect(Connection) public void Disconnect(Connection connection) Parameters connection Connection Duplicate() Deep copy — a fresh graph with fresh node Ids, connections remapped to match. public Graph Duplicate() Returns Graph RemoveNode(Node) Rejects OutputNode — the one fixed anchor; everything else (including every InputNode, TintNode/TransformNode/GainNode, which are defaults, not anchors) is fully removable. Also removes every Connection that referenced this node, both incoming and outgoing, so the graph never carries a Connection pointing at a Guid that no longer resolves to anything in Nodes. public void RemoveNode(Node node) Parameters node Node"
  },
  "api/EditSharp.Components.Nodes.ITrimmableInput.html": {
    "href": "api/EditSharp.Components.Nodes.ITrimmableInput.html",
    "title": "Interface ITrimmableInput | EditSharp",
    "summary": "Interface ITrimmableInput Namespace EditSharp.Components.Nodes Assembly EditSharp.dll A node whose own in-point should shift in lockstep with every OTHER trimmable input when the owning Clip's head is trimmed/extended — see Clip.OnHeadInPointShift/MaxHeadExtend, and the schema doc's multi-input-trim rule (\"shift them all together, clamped by whichever one has the least room left\"). Implemented by VideoSourceNode/AudioSourceNode (wrapping Source.Start) and TimelineVideoInputNode/TimelineAudioInputNode (wrapping TimelineReference.Start). Generator/procedural/text input nodes (ColorGeneratorInputNode, NoiseInputNode, TextInputNode, ToneGeneratorInputNode) do NOT implement this — they have no \"in-point\" concept, matching the old TextClip/GeneratorClip/ NoiseClip's inherited no-op/unbounded Clip defaults from before this rewrite. public interface ITrimmableInput Properties ContentLength How much content follows the in-point when there's a known hard end; null when unbounded, looping, or not known yet. TimeSpan? ContentLength { get; } Property Value TimeSpan? InPoint The node's own in-point. Setting this is what OnHeadInPointShift actually does, per node. TimeSpan InPoint { get; set; } Property Value TimeSpan MaxHeadroom How far InPoint could move EARLIER (an extend) — TimeSpan.MaxValue if unconstrained. TimeSpan MaxHeadroom { get; } Property Value TimeSpan"
  },
  "api/EditSharp.Components.Nodes.ImageOutputNode.html": {
    "href": "api/EditSharp.Components.Nodes.ImageOutputNode.html",
    "title": "Class ImageOutputNode | EditSharp",
    "summary": "Class ImageOutputNode Namespace EditSharp.Components.Nodes Assembly EditSharp.dll Feeds the renderer. Mandatory, not removable — the one fixed anchor left in an Image-domain graph. public sealed class ImageOutputNode : OutputNode Inheritance object Node OutputNode ImageOutputNode Inherited Members Node.Id Node.Enabled Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.InputNode.html": {
    "href": "api/EditSharp.Components.Nodes.InputNode.html",
    "title": "Class InputNode | EditSharp",
    "summary": "Class InputNode Namespace EditSharp.Components.Nodes Assembly EditSharp.dll FUNDAMENTAL REWRITE: a node that ORIGINATES a clip's content, rather than receiving it from upstream — see the schema doc's \"clips are graphs\" model. This replaces the old fixed, mandatory ImageSourceNode/AudioSourceNode anchors entirely. An InputNode is just an ordinary node: fully addable, removable, and rewireable like any other. A graph needs at least one InputNode actually reaching OutputNode to render anything, but that's not tracked as a separate structural invariant here — it falls out naturally from the exact same \"Output has no incoming connection\" check every evaluator already performs (see ImageGraphEvaluator/AudioGraphEvaluator), the same way it always has for a disconnected Output. This is also what makes a MULTI-input graph (two media sources merged via MergeNode/AudioMixNode, say) nothing special: it's just a graph with more than one InputNode, no different in kind from one with a single InputNode. Concrete video-domain InputNodes: VideoSourceNode, TextInputNode, ColorGeneratorInputNode, NoiseInputNode, TimelineVideoInputNode (see EditSharp.Components.Nodes.Sources). Concrete audio-domain InputNodes: AudioSourceNode, ToneGeneratorInputNode, TimelineAudioInputNode (see EditSharp.Components.Nodes.Sources). These are what replaced the old VideoClip.Source-is-the-whole-clip / TextClip / GeneratorClip / NoiseClip / TimelineVideoClip / TimelineAudioClip Clip subtypes — the ONLY remaining concrete Clip subtypes are VideoClip and AudioClip, and what used to distinguish those Clip subtypes from each other is now just which InputNode(s) happen to be wired into an otherwise perfectly ordinary Graph. Also see Graph's own convenience InputNodes property, for reading every InputNode currently in a graph without having to filter Nodes by hand. public abstract class InputNode : Node Inheritance object Node InputNode Derived AudioSourceNode VideoSourceNode Inherited Members Node.Id Node.Ports Node.Enabled Node.Duplicate() Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString()"
  },
  "api/EditSharp.Components.Nodes.Math.MathNode.html": {
    "href": "api/EditSharp.Components.Nodes.Math.MathNode.html",
    "title": "Class MathNode | EditSharp",
    "summary": "Class MathNode Namespace EditSharp.Components.Nodes.Math Assembly EditSharp.dll The \"base node type for math functions that are universally applicable\" — combines two Value inputs with a plain arithmetic operation. Domain-universal for the same reason ValueConstantNode is (only Value ports), so the SAME MathNode class places into either an Image-domain or an Audio-domain graph. On its own this only produces a number — it has no effect on any signal until it's wired into one of the small set of nodes that expose an optional Value modulation input (GainNode's \"Modulation\", AudioMixNode's \"MixAModulation\"/\"MixBModulation\", MergeNode's \"MixModulation\" — see each node's own remarks). Wire a ValueConstantNode (or a whole MathNode chain) into one of those, and it multiplies against that node's own keyframed value every time it's evaluated, rather than replacing it outright — so the node's own curve and a procedurally-computed modulation compose, instead of one silently overriding the other. public sealed class MathNode : Node Inheritance object Node MathNode Inherited Members Node.Id Node.Enabled Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Operation [Editable(\"Operation\")] public MathOperation Operation { get; set; } Property Value MathOperation Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Math.MathOperation.html": {
    "href": "api/EditSharp.Components.Nodes.Math.MathOperation.html",
    "title": "Enum MathOperation | EditSharp",
    "summary": "Enum MathOperation Namespace EditSharp.Components.Nodes.Math Assembly EditSharp.dll public enum MathOperation Fields Add = 0 Divide = 3 Max = 5 Min = 4 Multiply = 2 Subtract = 1"
  },
  "api/EditSharp.Components.Nodes.Math.ValueConstantNode.html": {
    "href": "api/EditSharp.Components.Nodes.Math.ValueConstantNode.html",
    "title": "Class ValueConstantNode | EditSharp",
    "summary": "Class ValueConstantNode Namespace EditSharp.Components.Nodes.Math Assembly EditSharp.dll Wraps one keyframeable float as a Value-port output — the simplest way to originate a number for a MathNode (or for one of the optional Value modulation inputs a few signal nodes now expose — see GainNode/AudioMixNode/MergeNode's own remarks) to consume. Declares ONLY a Value port, so Graph.AddNode treats it as domain-universal (see Graph.InferDomain) — the exact same ValueConstantNode class works unmodified inside a VideoClip's Image-domain graph or an AudioClip's Audio-domain graph. public sealed class ValueConstantNode : Node Inheritance object Node ValueConstantNode Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Value [Editable(\"Value\")] public Animatable<float> Value { get; set; } Property Value Animatable<float> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Math.html": {
    "href": "api/EditSharp.Components.Nodes.Math.html",
    "title": "Namespace EditSharp.Components.Nodes.Math | EditSharp",
    "summary": "Namespace EditSharp.Components.Nodes.Math Classes MathNode The \"base node type for math functions that are universally applicable\" — combines two Value inputs with a plain arithmetic operation. Domain-universal for the same reason ValueConstantNode is (only Value ports), so the SAME MathNode class places into either an Image-domain or an Audio-domain graph. On its own this only produces a number — it has no effect on any signal until it's wired into one of the small set of nodes that expose an optional Value modulation input (GainNode's \"Modulation\", AudioMixNode's \"MixAModulation\"/\"MixBModulation\", MergeNode's \"MixModulation\" — see each node's own remarks). Wire a ValueConstantNode (or a whole MathNode chain) into one of those, and it multiplies against that node's own keyframed value every time it's evaluated, rather than replacing it outright — so the node's own curve and a procedurally-computed modulation compose, instead of one silently overriding the other. ValueConstantNode Wraps one keyframeable float as a Value-port output — the simplest way to originate a number for a MathNode (or for one of the optional Value modulation inputs a few signal nodes now expose — see GainNode/AudioMixNode/MergeNode's own remarks) to consume. Declares ONLY a Value port, so Graph.AddNode treats it as domain-universal (see Graph.InferDomain) — the exact same ValueConstantNode class works unmodified inside a VideoClip's Image-domain graph or an AudioClip's Audio-domain graph. Enums MathOperation"
  },
  "api/EditSharp.Components.Nodes.Node.html": {
    "href": "api/EditSharp.Components.Nodes.Node.html",
    "title": "Class Node | EditSharp",
    "summary": "Class Node Namespace EditSharp.Components.Nodes Assembly EditSharp.dll public abstract class Node Inheritance object Node Derived CompositeNode AudioMixNode BlurNode CompressorNode DropShadowNode EQNode GainNode ImageToMaskNode MaskCombineNode MergeNode RoundedCornersNode ShapeMaskNode TintNode TransformNode InputNode MathNode ValueConstantNode OutputNode Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public virtual IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Enabled [Editable(\"Enabled\", Order = -100)] public bool Enabled { get; set; } Property Value bool Id public Guid Id { get; } Property Value Guid Ports public abstract IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public abstract Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.NodeDomain.html": {
    "href": "api/EditSharp.Components.Nodes.NodeDomain.html",
    "title": "Enum NodeDomain | EditSharp",
    "summary": "Enum NodeDomain Namespace EditSharp.Components.Nodes Assembly EditSharp.dll Which domain a graph's ports may use — see Graph.AddNode. public enum NodeDomain Fields Audio = 1 Image = 0"
  },
  "api/EditSharp.Components.Nodes.NodePort.html": {
    "href": "api/EditSharp.Components.Nodes.NodePort.html",
    "title": "Class NodePort | EditSharp",
    "summary": "Class NodePort Namespace EditSharp.Components.Nodes Assembly EditSharp.dll A fixed port declared by a concrete Node type. public sealed class NodePort Inheritance object NodePort Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors NodePort(string, PortType, PortDirection, bool) public NodePort(string name, PortType type, PortDirection direction, bool optional = false) Parameters name string type PortType direction PortDirection optional bool Properties Direction public PortDirection Direction { get; } Property Value PortDirection Name public string Name { get; } Property Value string Optional public bool Optional { get; } Property Value bool Type public PortType Type { get; } Property Value PortType"
  },
  "api/EditSharp.Components.Nodes.OutputNode.html": {
    "href": "api/EditSharp.Components.Nodes.OutputNode.html",
    "title": "Class OutputNode | EditSharp",
    "summary": "Class OutputNode Namespace EditSharp.Components.Nodes Assembly EditSharp.dll Common base for the one fixed, mandatory, non-removable anchor every graph has — ImageOutputNode for an Image-domain graph, AudioOutputNode for an Audio-domain graph (see Graph.OutputNode). Carries no members of its own beyond what Node already provides; it exists purely so Graph.OutputNode can be typed as \"the output anchor\" rather than the generic base Node type. public abstract class OutputNode : Node Inheritance object Node OutputNode Derived AudioOutputNode ImageOutputNode Inherited Members Node.Id Node.Ports Node.Enabled Node.Duplicate() Node.Animatables object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString()"
  },
  "api/EditSharp.Components.Nodes.PortDirection.html": {
    "href": "api/EditSharp.Components.Nodes.PortDirection.html",
    "title": "Enum PortDirection | EditSharp",
    "summary": "Enum PortDirection Namespace EditSharp.Components.Nodes Assembly EditSharp.dll public enum PortDirection Fields Input = 0 Output = 1"
  },
  "api/EditSharp.Components.Nodes.PortType.html": {
    "href": "api/EditSharp.Components.Nodes.PortType.html",
    "title": "Enum PortType | EditSharp",
    "summary": "Enum PortType Namespace EditSharp.Components.Nodes Assembly EditSharp.dll public enum PortType Fields Audio = 2 Image = 0 Mask = 1 Value = 3"
  },
  "api/EditSharp.Components.Nodes.Sources.AudioSourceNode.html": {
    "href": "api/EditSharp.Components.Nodes.Sources.AudioSourceNode.html",
    "title": "Class AudioSourceNode | EditSharp",
    "summary": "Class AudioSourceNode Namespace EditSharp.Components.Nodes.Sources Assembly EditSharp.dll Feeds an AudioSource's samples into the graph, whatever kind it is. Replaces the old AudioClip.Source property directly. public sealed class AudioSourceNode : InputNode, ITrimmableInput Inheritance object Node InputNode AudioSourceNode Implements ITrimmableInput Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> ContentLength How much content follows the in-point when there's a known hard end; null when unbounded, looping, or not known yet. public TimeSpan? ContentLength { get; } Property Value TimeSpan? InPoint The node's own in-point. Setting this is what OnHeadInPointShift actually does, per node. public TimeSpan InPoint { get; set; } Property Value TimeSpan MaxHeadroom How far InPoint could move EARLIER (an extend) — TimeSpan.MaxValue if unconstrained. public TimeSpan MaxHeadroom { get; } Property Value TimeSpan Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Source [Editable(\"Source\")] public required AudioSource Source { get; set; } Property Value AudioSource Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Sources.VideoSourceNode.html": {
    "href": "api/EditSharp.Components.Nodes.Sources.VideoSourceNode.html",
    "title": "Class VideoSourceNode | EditSharp",
    "summary": "Class VideoSourceNode Namespace EditSharp.Components.Nodes.Sources Assembly EditSharp.dll Feeds a VideoSource's frames into the graph; a media file, a still image, or any other video kind; the node neither knows nor cares which. Replaces the old VideoClip.Source property directly. public sealed class VideoSourceNode : InputNode, ITrimmableInput Inheritance object Node InputNode VideoSourceNode Implements ITrimmableInput Inherited Members Node.Id Node.Enabled object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Animatables Every keyframeable property this node owns, so a clip can reach all of its tracks at once — a head trim/extend shifts every keyframe in the graph (see Clip.OnHeadInPointShift). Empty by default; a node with Animatable properties overrides it and lists each one. An animated property left out here simply stops following the content when the clip's head moves. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> ContentLength How much content follows the in-point when there's a known hard end; null when unbounded, looping, or not known yet. public TimeSpan? ContentLength { get; } Property Value TimeSpan? InPoint The node's own in-point. Setting this is what OnHeadInPointShift actually does, per node. public TimeSpan InPoint { get; set; } Property Value TimeSpan MaxHeadroom How far InPoint could move EARLIER (an extend) — TimeSpan.MaxValue if unconstrained. public TimeSpan MaxHeadroom { get; } Property Value TimeSpan Ports public override IReadOnlyList<NodePort> Ports { get; } Property Value IReadOnlyList<NodePort> Source [Editable(\"Source\")] public required VideoSource Source { get; set; } Property Value VideoSource Methods Duplicate() Deep copy with a FRESH Id — a duplicated clip's graph must not share node identity with the original, or a Connection recorded against one graph could be mistaken for referencing a node in the other. public override Node Duplicate() Returns Node"
  },
  "api/EditSharp.Components.Nodes.Sources.html": {
    "href": "api/EditSharp.Components.Nodes.Sources.html",
    "title": "Namespace EditSharp.Components.Nodes.Sources | EditSharp",
    "summary": "Namespace EditSharp.Components.Nodes.Sources Classes AudioSourceNode Feeds an AudioSource's samples into the graph, whatever kind it is. Replaces the old AudioClip.Source property directly. VideoSourceNode Feeds a VideoSource's frames into the graph; a media file, a still image, or any other video kind; the node neither knows nor cares which. Replaces the old VideoClip.Source property directly."
  },
  "api/EditSharp.Components.Nodes.html": {
    "href": "api/EditSharp.Components.Nodes.html",
    "title": "Namespace EditSharp.Components.Nodes | EditSharp",
    "summary": "Namespace EditSharp.Components.Nodes Namespaces EditSharp.Components.Nodes.Effects EditSharp.Components.Nodes.Math EditSharp.Components.Nodes.Sources Classes AudioOutputNode Feeds the audio mixer. Mandatory, not removable — the one fixed anchor left in an Audio-domain graph. CompositeNode A graph folded into one node — what a user gets by assembling a graph and saving it as a node of their own. It has the same domain as the graph inside it, one output (whatever feeds the inner graph's OutputNode), and as inputs exactly the inner input ports it chose to expose. Its editable properties are the inner ones it chose to expose, each under its own name (see IInspectable). Nothing evaluates a composite as such: Graph.Flattened replaces every composite with the nodes inside it before an evaluator sees the graph, so the effect and audio pipelines never know it was there. The inner nodes are the same objects, so their content, keyframes and in-points are found and shifted like any other's (see Graph.AllNodes). Not yet persisted — that arrives with the project file format. Connection ExposedPort An inner node's input port, shown on the composite under Name. ExposedProperty An inner node's editable property, shown on the composite under Name. Graph A directed graph of nodes — see the schema doc's \"Effects: Node-Based Compositing Graph\" section for the full design. The same primitives here power both the Image/Mask domain (VideoClip) and the Audio domain (AudioClip); NodeDomain is what keeps the two from ever being wired together, EXCEPT for domain-universal nodes (see InferDomain below), which is exactly how the \"base node type for math functions that are universally applicable\" the user asked for is implemented: ValueConstantNode/MathNode (see EditSharp.Components.Nodes.Math) declare only PortType.Value ports, which InferDomain treats as belonging to NEITHER domain specifically, and therefore addable to either. FUNDAMENTAL REWRITE (\"clips are graphs\"): there is no longer a second fixed anchor alongside OutputNode. The old ImageSourceNode/ AudioSourceNode were each a single, mandatory, non-removable \"the clip's content enters here\" anchor — every clip's graph had EXACTLY one. That model assumed a clip was \"media with a graph of effects bolted on\". The corrected model is that a clip IS its graph, and a graph can have as many InputNodes as an author wants (a minimum of one for the graph to produce anything, enforced the same way it always effectively was — OutputNode must have SOME path reaching it at render time). See InputNode's own remarks. Encapsulation principle applies: Nodes/Connections are read-only externally, all mutation goes through AddNode/RemoveNode/Connect/ Disconnect — which is what makes \"no dangling references, no cycles, no type mismatches\" an actual invariant rather than a convention a caller could violate by touching a public list directly. ImageOutputNode Feeds the renderer. Mandatory, not removable — the one fixed anchor left in an Image-domain graph. InputNode FUNDAMENTAL REWRITE: a node that ORIGINATES a clip's content, rather than receiving it from upstream — see the schema doc's \"clips are graphs\" model. This replaces the old fixed, mandatory ImageSourceNode/AudioSourceNode anchors entirely. An InputNode is just an ordinary node: fully addable, removable, and rewireable like any other. A graph needs at least one InputNode actually reaching OutputNode to render anything, but that's not tracked as a separate structural invariant here — it falls out naturally from the exact same \"Output has no incoming connection\" check every evaluator already performs (see ImageGraphEvaluator/AudioGraphEvaluator), the same way it always has for a disconnected Output. This is also what makes a MULTI-input graph (two media sources merged via MergeNode/AudioMixNode, say) nothing special: it's just a graph with more than one InputNode, no different in kind from one with a single InputNode. Concrete video-domain InputNodes: VideoSourceNode, TextInputNode, ColorGeneratorInputNode, NoiseInputNode, TimelineVideoInputNode (see EditSharp.Components.Nodes.Sources). Concrete audio-domain InputNodes: AudioSourceNode, ToneGeneratorInputNode, TimelineAudioInputNode (see EditSharp.Components.Nodes.Sources). These are what replaced the old VideoClip.Source-is-the-whole-clip / TextClip / GeneratorClip / NoiseClip / TimelineVideoClip / TimelineAudioClip Clip subtypes — the ONLY remaining concrete Clip subtypes are VideoClip and AudioClip, and what used to distinguish those Clip subtypes from each other is now just which InputNode(s) happen to be wired into an otherwise perfectly ordinary Graph. Also see Graph's own convenience InputNodes property, for reading every InputNode currently in a graph without having to filter Nodes by hand. Node NodePort A fixed port declared by a concrete Node type. OutputNode Common base for the one fixed, mandatory, non-removable anchor every graph has — ImageOutputNode for an Image-domain graph, AudioOutputNode for an Audio-domain graph (see Graph.OutputNode). Carries no members of its own beyond what Node already provides; it exists purely so Graph.OutputNode can be typed as \"the output anchor\" rather than the generic base Node type. Interfaces ITrimmableInput A node whose own in-point should shift in lockstep with every OTHER trimmable input when the owning Clip's head is trimmed/extended — see Clip.OnHeadInPointShift/MaxHeadExtend, and the schema doc's multi-input-trim rule (\"shift them all together, clamped by whichever one has the least room left\"). Implemented by VideoSourceNode/AudioSourceNode (wrapping Source.Start) and TimelineVideoInputNode/TimelineAudioInputNode (wrapping TimelineReference.Start). Generator/procedural/text input nodes (ColorGeneratorInputNode, NoiseInputNode, TextInputNode, ToneGeneratorInputNode) do NOT implement this — they have no \"in-point\" concept, matching the old TextClip/GeneratorClip/ NoiseClip's inherited no-op/unbounded Clip defaults from before this rewrite. Enums NodeDomain Which domain a graph's ports may use — see Graph.AddNode. PortDirection PortType"
  },
  "api/EditSharp.Components.PositionTrack.html": {
    "href": "api/EditSharp.Components.PositionTrack.html",
    "title": "Class PositionTrack | EditSharp",
    "summary": "Class PositionTrack Namespace EditSharp.Components Assembly EditSharp.dll Position's own KeyframeTrack<Vector2> subclass. Evaluate is deliberately two-stage rather than sharing the base class's plain value-cubic: Temporal stage: the ordinary Hold/Linear/Bezier handle logic (this class's inherited InHandle/OutHandle, via the base's shared SolveU) produces a scalar progress u in [0,1] between the two bounding keyframes — exactly the same computation every other KeyframeTrack<T> does to find its cubic parameter. Spatial stage: u is fed as the parametric input to the cubic bezier PATH defined by (left.Value, left.SpatialOutHandle, right.SpatialInHandle, right.Value). Easing each axis independently (the base class's own approach, if it were used unmodified here) is the classic bug that produces visibly curved motion on an intended straight line — see the schema doc for why \"how fast\" and \"which way\" have to be two curves composed through one shared progress value instead. public sealed class PositionTrack : KeyframeTrack<Vector2> Inheritance object KeyframeTrack<Vector2> PositionTrack Inherited Members KeyframeTrack<Vector2>.Keyframes KeyframeTrack<Vector2>.AddKeyframe(TimeSpan, Vector2) KeyframeTrack<Vector2>.Shift(TimeSpan) KeyframeTrack<Vector2>.Duplicate() KeyframeTrack<Vector2>.RemoveKeyframe(Keyframe<Vector2>) KeyframeTrack<Vector2>.MoveKeyframe(Keyframe<Vector2>, TimeSpan, Vector2) KeyframeTrack<Vector2>.SetHandle(Keyframe<Vector2>, bool, TimeSpan, Vector2) KeyframeTrack<Vector2>.SetTangentMode(Keyframe<Vector2>, bool, TangentMode) KeyframeTrack<Vector2>.Evaluate(TimeSpan) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Methods AddSpatialKeyframe(TimeSpan, Vector2) public SpatialKeyframe AddSpatialKeyframe(TimeSpan start, Vector2 value) Parameters start TimeSpan value Vector2 Returns SpatialKeyframe CopyKeyframeExtras(Keyframe<Vector2>, Keyframe<Vector2>) Copies whatever a keyframe subtype carries beyond the base fields — see PositionTrack. protected override void CopyKeyframeExtras(Keyframe<Vector2> source, Keyframe<Vector2> target) Parameters source Keyframe<Vector2> target Keyframe<Vector2> CreateEmptyCopy() An empty track of this exact type, for Duplicate — a subclass returns its own type. protected override KeyframeTrack<Vector2> CreateEmptyCopy() Returns KeyframeTrack<Vector2> CreateKeyframe(TimeSpan, Vector2) Factory hook so a subclass (PositionTrack) can hand back its own Keyframe subtype (SpatialKeyframe) instead of a plain Keyframe<T>, without AddKeyframe itself needing to know about that. protected override Keyframe<Vector2> CreateKeyframe(TimeSpan start, Vector2 value) Parameters start TimeSpan value Vector2 Returns Keyframe<Vector2> InterpolateSegment(Keyframe<Vector2>, Keyframe<Vector2>, TimeSpan) Resolves one segment's value at time. Virtual so PositionTrack can reuse the Hold/Linear/Bezier TIMING logic (via the protected SolveU helper) while swapping in a spatial bezier for the actual value, instead of this class's plain value-cubic — see the schema doc's \"two-stage, deliberately NOT independent per-axis easing\" remark on PositionTrack. protected override Vector2 InterpolateSegment(Keyframe<Vector2> from, Keyframe<Vector2> to, TimeSpan time) Parameters from Keyframe<Vector2> to Keyframe<Vector2> time TimeSpan Returns Vector2 SetSpatialHandle(SpatialKeyframe, bool, Vector2) public void SetSpatialHandle(SpatialKeyframe keyframe, bool isInHandle, Vector2 offset) Parameters keyframe SpatialKeyframe isInHandle bool offset Vector2"
  },
  "api/EditSharp.Components.Sources.Audio.AudioSource.html": {
    "href": "api/EditSharp.Components.Sources.Audio.AudioSource.html",
    "title": "Class AudioSource | EditSharp",
    "summary": "Class AudioSource Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll A source of sound, read as a stream: PrepareAsync does the slow once-per-session work, and the prepared handle opens readers that pull interleaved float samples block by block, already in the format the caller asked for. Mirrors VideoSource's two-phase shape. public abstract class AudioSource : Source Inheritance object Source AudioSource Derived MediaAudioSource TimelineAudioSource ToneAudioSource VSTInstrumentSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetNaturalLengthAsync(CancellationToken) Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override AudioSource Duplicate() Returns AudioSource"
  },
  "api/EditSharp.Components.Sources.Audio.MediaAudioSource.html": {
    "href": "api/EditSharp.Components.Sources.Audio.MediaAudioSource.html",
    "title": "Class MediaAudioSource | EditSharp",
    "summary": "Class MediaAudioSource Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll The audio stream of a media file on disk (an audio file, or a video file's soundtrack), streamed through ffmpeg. A file with no audio stream reads as silence for its length. [SourceKind(\"media-audio\", DisplayName = \"Media\")] public class MediaAudioSource : AudioSource, IFileBackedSource Inheritance object Source AudioSource MediaAudioSource Implements IFileBackedSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Path [Editable(\"File\", Editor = PropertyEditor.Path)] public required string Path { get; set; } Property Value string Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override MediaAudioSource Duplicate() Returns MediaAudioSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?> TryGetNaturalLength(out TimeSpan?) GetNaturalLengthAsync's answer when it's known right away; false while it still has to be found (a file not probed yet). public override bool TryGetNaturalLength(out TimeSpan? length) Parameters length TimeSpan? Returns bool"
  },
  "api/EditSharp.Components.Sources.Audio.TimelineAudioSource.html": {
    "href": "api/EditSharp.Components.Sources.Audio.TimelineAudioSource.html",
    "title": "Class TimelineAudioSource | EditSharp",
    "summary": "Class TimelineAudioSource Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll Another timeline's mixed audio, rendered block by block by its own TimelineAudioRenderer in the reading session. Start/Duration/Loop trim it like any source; its natural length is the timeline's duration. Saved as the timeline's Id (see SourceSerializer.Deserialize). [SourceKind(\"timeline-audio\", DisplayName = \"Timeline\", Listed = false)] public class TimelineAudioSource : AudioSource Inheritance object Source AudioSource TimelineAudioSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Timeline [Editable(\"Timeline\")] public Timeline? Timeline { get; set; } Property Value Timeline Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override TimelineAudioSource Duplicate() Returns TimelineAudioSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Audio.ToneAudioSource.html": {
    "href": "api/EditSharp.Components.Sources.Audio.ToneAudioSource.html",
    "title": "Class ToneAudioSource | EditSharp",
    "summary": "Class ToneAudioSource Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll A synthesized tone. Frequency and amplitude can be keyframed; they're evaluated every sample, and phase carries on across blocks so a changing frequency never clicks. [SourceKind(\"tone\", DisplayName = \"Tone\")] public class ToneAudioSource : AudioSource Inheritance object Source AudioSource ToneAudioSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Amplitude [Editable(\"Amplitude\", Min = 0, Max = 1, Step = 0.01)] public Animatable<float> Amplitude { get; set; } Property Value Animatable<float> Animatables This source's keyframed values. The node holding the source reports them as its own, so keyframe editing, fingerprints and head-trim keyframe shifting reach them. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Frequency [Editable(\"Frequency\", Min = 20, Max = 20000, Step = 1, Unit = \"Hz\")] public Animatable<float> Frequency { get; set; } Property Value Animatable<float> Waveform [Editable(\"Waveform\")] public Waveform Waveform { get; set; } Property Value Waveform Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override ToneAudioSource Duplicate() Returns ToneAudioSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Audio.VSTInstrumentSource.html": {
    "href": "api/EditSharp.Components.Sources.Audio.VSTInstrumentSource.html",
    "title": "Class VSTInstrumentSource | EditSharp",
    "summary": "Class VSTInstrumentSource Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll [SourceKind(\"vst-instrument\", DisplayName = \"VST instrument\", Listed = false)] public class VSTInstrumentSource : AudioSource Inheritance object Source AudioSource VSTInstrumentSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override VSTInstrumentSource Duplicate() Returns VSTInstrumentSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Audio.Waveform.html": {
    "href": "api/EditSharp.Components.Sources.Audio.Waveform.html",
    "title": "Enum Waveform | EditSharp",
    "summary": "Enum Waveform Namespace EditSharp.Components.Sources.Audio Assembly EditSharp.dll public enum Waveform Fields Sawtooth = 2 Sine = 0 Square = 1 Triangle = 3"
  },
  "api/EditSharp.Components.Sources.Audio.html": {
    "href": "api/EditSharp.Components.Sources.Audio.html",
    "title": "Namespace EditSharp.Components.Sources.Audio | EditSharp",
    "summary": "Namespace EditSharp.Components.Sources.Audio Classes AudioSource A source of sound, read as a stream: PrepareAsync does the slow once-per-session work, and the prepared handle opens readers that pull interleaved float samples block by block, already in the format the caller asked for. Mirrors VideoSource's two-phase shape. MediaAudioSource The audio stream of a media file on disk (an audio file, or a video file's soundtrack), streamed through ffmpeg. A file with no audio stream reads as silence for its length. TimelineAudioSource Another timeline's mixed audio, rendered block by block by its own TimelineAudioRenderer in the reading session. Start/Duration/Loop trim it like any source; its natural length is the timeline's duration. Saved as the timeline's Id (see SourceSerializer.Deserialize). ToneAudioSource A synthesized tone. Frequency and amplitude can be keyframed; they're evaluated every sample, and phase carries on across blocks so a changing frequency never clicks. VSTInstrumentSource Enums Waveform"
  },
  "api/EditSharp.Components.Sources.IFileBackedSource.html": {
    "href": "api/EditSharp.Components.Sources.IFileBackedSource.html",
    "title": "Interface IFileBackedSource | EditSharp",
    "summary": "Interface IFileBackedSource Namespace EditSharp.Components.Sources Assembly EditSharp.dll A source whose content lives in one file on disk. File-level services (proxies, grouping clips that share a file) work against this rather than any concrete kind, so a new file-backed kind gets them by implementing it. public interface IFileBackedSource Properties FilePath string FilePath { get; } Property Value string"
  },
  "api/EditSharp.Components.Sources.Source.html": {
    "href": "api/EditSharp.Components.Sources.Source.html",
    "title": "Class Source | EditSharp",
    "summary": "Class Source Namespace EditSharp.Components.Sources Assembly EditSharp.dll Something that can PROVIDE a clip's raw content (a media file, a rendered scene, an instrument) independently of how that content is later composited or mixed. Compositing never knows what kind of source it is reading from; it only prepares a source, opens readers on it and reacts to the SourceUnavailableReason a failing read reports. Every source is plain data: its [Editable] properties are exactly what SourceSerializer saves, and Duplicate is a round-trip through that same serializer, so a new property is copied precisely when it is saved. Nothing prepared or opened (probe results, decoders) is ever stored on the source itself; several Playbacks may read one source at once. ADDING A KIND: derive from VideoSource or AudioSource, mark the class [SourceKind(\"stable-id\")] (the id is what saved files contain, so never change it), implement PrepareAsync and GetNaturalLengthAsync, and add anything identifying its content to AddFingerprint. See BlenderSceneVideoSource/VSTInstrumentSource for the bare shape. public abstract class Source Inheritance object Source Derived AudioSource VideoSource Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Animatables This source's keyframed values. The node holding the source reports them as its own, so keyframe editing, fingerprints and head-trim keyframe shifting reach them. [JsonIgnore] public virtual IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Duration [Editable(\"Duration\")] public TimeSpan? Duration { get; set; } Property Value TimeSpan? Loop [Editable(\"Loop\")] public bool Loop { get; set; } Property Value bool Start [Editable(\"In point\")] public TimeSpan? Start { get; set; } Property Value TimeSpan? Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public virtual Source Duplicate() Returns Source GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public abstract Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?> GetUsableLengthAsync(CancellationToken) How much material the source offers from Start: Duration, clamped to what really follows Start. Null when unbounded (no Duration and no natural length); zero when Start is at or past the end. public Task<TimeSpan?> GetUsableLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?> ResolveWindow(TimeSpan?) The window of source-local time this source actually offers: it begins at Start and runs for Duration, clamped to whatever material really follows Start. A null Length is unbounded. protected (TimeSpan Start, TimeSpan? Length) ResolveWindow(TimeSpan? naturalLength) Parameters naturalLength TimeSpan? Returns (TimeSpan Start, TimeSpan? Length) ToSourceTime(TimeSpan, TimeSpan?) The standard content-time to source-time mapping, for kinds that want it: adds Start, wraps around the trimmed window when Loop is set, and otherwise reports EndOfSource once content time runs past it. Exotic kinds are free to ignore this and map time themselves. protected TimeSpan ToSourceTime(TimeSpan contentTime, TimeSpan? naturalLength) Parameters contentTime TimeSpan naturalLength TimeSpan? Returns TimeSpan TryGetNaturalLength(out TimeSpan?) GetNaturalLengthAsync's answer when it's known right away; false while it still has to be found (a file not probed yet). public virtual bool TryGetNaturalLength(out TimeSpan? length) Parameters length TimeSpan? Returns bool TryGetUsableLength(out TimeSpan?) GetUsableLengthAsync's answer when it's known right away; see TryGetNaturalLength. public bool TryGetUsableLength(out TimeSpan? length) Parameters length TimeSpan? Returns bool"
  },
  "api/EditSharp.Components.Sources.SourceKindAttribute.html": {
    "href": "api/EditSharp.Components.Sources.SourceKindAttribute.html",
    "title": "Class SourceKindAttribute | EditSharp",
    "summary": "Class SourceKindAttribute Namespace EditSharp.Components.Sources Assembly EditSharp.dll Registers a concrete Source for serialization under a stable id. The id is written into saved data as \"$kind\", so it must never change once shipped; rename the class freely, never the id. DisplayName is what editors show; Listed false keeps a kind out of pickers (examples, unfinished kinds) while it still loads. [AttributeUsage(AttributeTargets.Class, Inherited = false, AllowMultiple = false)] public sealed class SourceKindAttribute : Attribute Inheritance object Attribute SourceKindAttribute Inherited Members Attribute.Equals(object) Attribute.GetCustomAttribute(Assembly, Type) Attribute.GetCustomAttribute(Assembly, Type, bool) Attribute.GetCustomAttribute(MemberInfo, Type) Attribute.GetCustomAttribute(MemberInfo, Type, bool) Attribute.GetCustomAttribute(Module, Type) Attribute.GetCustomAttribute(Module, Type, bool) Attribute.GetCustomAttribute(ParameterInfo, Type) Attribute.GetCustomAttribute(ParameterInfo, Type, bool) Attribute.GetCustomAttributes(Assembly) Attribute.GetCustomAttributes(Assembly, bool) Attribute.GetCustomAttributes(Assembly, Type) Attribute.GetCustomAttributes(Assembly, Type, bool) Attribute.GetCustomAttributes(MemberInfo) Attribute.GetCustomAttributes(MemberInfo, bool) Attribute.GetCustomAttributes(MemberInfo, Type) Attribute.GetCustomAttributes(MemberInfo, Type, bool) Attribute.GetCustomAttributes(Module) Attribute.GetCustomAttributes(Module, bool) Attribute.GetCustomAttributes(Module, Type) Attribute.GetCustomAttributes(Module, Type, bool) Attribute.GetCustomAttributes(ParameterInfo) Attribute.GetCustomAttributes(ParameterInfo, bool) Attribute.GetCustomAttributes(ParameterInfo, Type) Attribute.GetCustomAttributes(ParameterInfo, Type, bool) Attribute.GetHashCode() Attribute.IsDefaultAttribute() Attribute.IsDefined(Assembly, Type) Attribute.IsDefined(Assembly, Type, bool) Attribute.IsDefined(MemberInfo, Type) Attribute.IsDefined(MemberInfo, Type, bool) Attribute.IsDefined(Module, Type) Attribute.IsDefined(Module, Type, bool) Attribute.IsDefined(ParameterInfo, Type) Attribute.IsDefined(ParameterInfo, Type, bool) Attribute.Match(object) Attribute.TypeId object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors SourceKindAttribute(string) Registers a concrete Source for serialization under a stable id. The id is written into saved data as \"$kind\", so it must never change once shipped; rename the class freely, never the id. DisplayName is what editors show; Listed false keeps a kind out of pickers (examples, unfinished kinds) while it still loads. public SourceKindAttribute(string id) Parameters id string Properties DisplayName public string? DisplayName { get; init; } Property Value string Id public string Id { get; } Property Value string Listed public bool Listed { get; init; } Property Value bool"
  },
  "api/EditSharp.Components.Sources.SourceKindInfo.html": {
    "href": "api/EditSharp.Components.Sources.SourceKindInfo.html",
    "title": "Class SourceKindInfo | EditSharp",
    "summary": "Class SourceKindInfo Namespace EditSharp.Components.Sources Assembly EditSharp.dll A kind a source can be switched to: its saved id, its name in pickers, its class. public sealed record SourceKindInfo : IEquatable<SourceKindInfo> Inheritance object SourceKindInfo Implements IEquatable<SourceKindInfo> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors SourceKindInfo(string, string, Type) A kind a source can be switched to: its saved id, its name in pickers, its class. public SourceKindInfo(string Id, string DisplayName, Type Type) Parameters Id string DisplayName string Type Type Properties DisplayName public string DisplayName { get; init; } Property Value string Id public string Id { get; init; } Property Value string Type public Type Type { get; init; } Property Value Type"
  },
  "api/EditSharp.Components.Sources.SourceKinds.html": {
    "href": "api/EditSharp.Components.Sources.SourceKinds.html",
    "title": "Class SourceKinds | EditSharp",
    "summary": "Class SourceKinds Namespace EditSharp.Components.Sources Assembly EditSharp.dll The source kinds pickers offer, and switching a source from one kind to another. public static class SourceKinds Inheritance object SourceKinds Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods For(Type) The listed kinds that can stand in for baseType (VideoSource or AudioSource). public static IReadOnlyList<SourceKindInfo> For(Type baseType) Parameters baseType Type Returns IReadOnlyList<SourceKindInfo> Of(Source) The kind source is, listed or not; null for a class without a [SourceKind]. public static SourceKindInfo? Of(Source source) Parameters source Source Returns SourceKindInfo Switch(Source, SourceKindInfo) A new source of kind with its defaults, keeping from's Start, Duration and Loop. Assigning it is the caller's edit. public static Source Switch(Source from, SourceKindInfo kind) Parameters from Source kind SourceKindInfo Returns Source"
  },
  "api/EditSharp.Components.Sources.SourceMode.html": {
    "href": "api/EditSharp.Components.Sources.SourceMode.html",
    "title": "Enum SourceMode | EditSharp",
    "summary": "Enum SourceMode Namespace EditSharp.Components.Sources Assembly EditSharp.dll Whether proxy-capable sources read their proxy or their original. Kinds with no notion of a proxy (a still image, a 3D scene) ignore this and always render from source. Random access (scrubbing, reverse) always needs a proxy regardless of mode; decoding an original at an arbitrary position per frame isn't viable. public enum SourceMode Fields ProxiesAndSource = 1 The proxy wherever it covers the frame, the original everywhere else; decided frame by frame. ProxiesOnly = 0 Only proxies; a frame without one is ProxyPending while a build is under way, ProxyMissing otherwise. SourceOnly = 2 Only originals for sequential reads (random access still needs a proxy)."
  },
  "api/EditSharp.Components.Sources.SourceSerializer.html": {
    "href": "api/EditSharp.Components.Sources.SourceSerializer.html",
    "title": "Class SourceSerializer | EditSharp",
    "summary": "Class SourceSerializer Namespace EditSharp.Components.Sources Assembly EditSharp.dll Saves and loads sources as JSON. Every abstract Source type (Source, VideoSource, AudioSource, ...) is polymorphic, discriminated by \"$kind\" with the id from each concrete kind's [SourceKind]; no central list to edit when a kind is added. Property names are camelCase and TimeSpans are \"c\"-format strings (JsonSerializerDefaults.Web). Enums are names, keyframed values carry their whole track, colours are \"#AARRGGBB\", and a nested Timeline is saved as its Id (see Deserialize). Consumers embedding sources in their own JSON use Options directly (or chain its TypeInfoResolver into theirs); Serialize/Deserialize are the standalone shortcut. Loading always runs with history suppressed, so a deserialized source arrives with no undo entries of its own. Only kinds in this assembly are discovered for now (source kinds are internal to EditSharp); registering external assemblies belongs with opening the source contract up. public static class SourceSerializer Inheritance object SourceSerializer Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Fields KindProperty public const string KindProperty = \"$kind\" Field Value string Properties Options public static JsonSerializerOptions Options { get; } Property Value JsonSerializerOptions Methods Deserialize(string, Func<Guid, Timeline?>?) Loads a source. Sources that refer to a nested timeline store only its Id; timelines finds the Timeline for an id (typically from the project being loaded). public static Source Deserialize(string json, Func<Guid, Timeline?>? timelines = null) Parameters json string timelines Func<Guid, Timeline> Returns Source Deserialize<T>(string, Func<Guid, Timeline?>?) public static T Deserialize<T>(string json, Func<Guid, Timeline?>? timelines = null) where T : Source Parameters json string timelines Func<Guid, Timeline> Returns T Type Parameters T Serialize(Source) public static string Serialize(Source source) Parameters source Source Returns string"
  },
  "api/EditSharp.Components.Sources.SourceUnavailableException.html": {
    "href": "api/EditSharp.Components.Sources.SourceUnavailableException.html",
    "title": "Class SourceUnavailableException | EditSharp",
    "summary": "Class SourceUnavailableException Namespace EditSharp.Components.Sources Assembly EditSharp.dll The one way a source reports that it cannot provide content. Sources never substitute placeholders themselves; they throw this and let the caller decide by Reason. public sealed class SourceUnavailableException : Exception, ISerializable Inheritance object Exception SourceUnavailableException Implements ISerializable Inherited Members Exception.GetBaseException() Exception.GetType() Exception.ToString() Exception.Data Exception.HelpLink Exception.HResult Exception.InnerException Exception.Message Exception.Source Exception.StackTrace Exception.TargetSite object.Equals(object) object.Equals(object, object) object.GetHashCode() object.ReferenceEquals(object, object) Constructors SourceUnavailableException(SourceUnavailableReason, string, Exception?) The one way a source reports that it cannot provide content. Sources never substitute placeholders themselves; they throw this and let the caller decide by Reason. public SourceUnavailableException(SourceUnavailableReason reason, string message, Exception? inner = null) Parameters reason SourceUnavailableReason message string inner Exception Properties Reason public SourceUnavailableReason Reason { get; } Property Value SourceUnavailableReason"
  },
  "api/EditSharp.Components.Sources.SourceUnavailableReason.html": {
    "href": "api/EditSharp.Components.Sources.SourceUnavailableReason.html",
    "title": "Enum SourceUnavailableReason | EditSharp",
    "summary": "Enum SourceUnavailableReason Namespace EditSharp.Components.Sources Assembly EditSharp.dll Why a source could not provide content right now; compositing picks what to show from this. public enum SourceUnavailableReason Fields DecodeError = 4 The material is present but could not be decoded or rendered. EndOfSource = 5 The requested time is past the end of the source's window and Loop is off. MediaOffline = 3 The underlying material is missing or unreadable (file gone, device unplugged). Opening = 0 The reader is still opening and has nothing to hand back yet. ProxyMissing = 2 The read needs a proxy frame, and no build is queued or running to make it. ProxyPending = 1 The read needs a proxy frame that a queued or running build hasn't reached yet."
  },
  "api/EditSharp.Components.Sources.Video.BlenderSceneVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.BlenderSceneVideoSource.html",
    "title": "Class BlenderSceneVideoSource | EditSharp",
    "summary": "Class BlenderSceneVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll [SourceKind(\"blender-scene\", DisplayName = \"Blender scene\", Listed = false)] public class BlenderSceneVideoSource : VideoSource Inheritance object Source VideoSource BlenderSceneVideoSource Inherited Members VideoSource.GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override BlenderSceneVideoSource Duplicate() Returns BlenderSceneVideoSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Video.ColorVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.ColorVideoSource.html",
    "title": "Class ColorVideoSource | EditSharp",
    "summary": "Class ColorVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll A solid colour filling the canvas; the colour can be keyframed. [SourceKind(\"color\", DisplayName = \"Color\")] public class ColorVideoSource : VideoSource Inheritance object Source VideoSource ColorVideoSource Inherited Members VideoSource.GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Animatables This source's keyframed values. The node holding the source reports them as its own, so keyframe editing, fingerprints and head-trim keyframe shifting reach them. public override IEnumerable<IAnimatable> Animatables { get; } Property Value IEnumerable<IAnimatable> Color [Editable(\"Color\")] public Animatable<SKColor> Color { get; set; } Property Value Animatable<SKColor> Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override ColorVideoSource Duplicate() Returns ColorVideoSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Video.HorizontalTextAlignment.html": {
    "href": "api/EditSharp.Components.Sources.Video.HorizontalTextAlignment.html",
    "title": "Enum HorizontalTextAlignment | EditSharp",
    "summary": "Enum HorizontalTextAlignment Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll Justify stretches every wrapped line to the box's width; a paragraph's last line stays left. public enum HorizontalTextAlignment Fields Center = 1 Justify = 3 Left = 0 Right = 2"
  },
  "api/EditSharp.Components.Sources.Video.MediaVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.MediaVideoSource.html",
    "title": "Class MediaVideoSource | EditSharp",
    "summary": "Class MediaVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll A video file or a still image on disk; which one is decided by probing when prepared, never stored. A still has no natural length (Duration or the clip decides), has no proxy and is decoded once per session. A video reads its proxy (ProxyCache) or the original according to the session's SourceMode; random access always reads the proxy. [SourceKind(\"media-video\", DisplayName = \"Media\")] public class MediaVideoSource : VideoSource, IFileBackedSource Inheritance object Source VideoSource MediaVideoSource Implements IFileBackedSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Path [Editable(\"File\", Editor = PropertyEditor.Path)] public required string Path { get; set; } Property Value string Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override MediaVideoSource Duplicate() Returns MediaVideoSource GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) One frame at contentTime, for callers that just need a single picture (thumbnails); nothing stays open afterwards. The caller owns the result. mode picks proxy or original like a session would (ProxiesOnly throws ProxyPending or ProxyMissing where there's no proxy frame); maxWidth/maxHeight cap the size (0 = native). Kinds with a cheaper route (a still image) override it. public override Task<SKImage> GetFrameAtAsync(TimeSpan contentTime, SourceMode mode = SourceMode.SourceOnly, int maxWidth = 0, int maxHeight = 0, CancellationToken ct = default) Parameters contentTime TimeSpan mode SourceMode maxWidth int maxHeight int ct CancellationToken Returns Task<SKImage> GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?> TryGetNaturalLength(out TimeSpan?) GetNaturalLengthAsync's answer when it's known right away; false while it still has to be found (a file not probed yet). public override bool TryGetNaturalLength(out TimeSpan? length) Parameters length TimeSpan? Returns bool"
  },
  "api/EditSharp.Components.Sources.Video.NoiseVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.NoiseVideoSource.html",
    "title": "Class NoiseVideoSource | EditSharp",
    "summary": "Class NoiseVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll An evolving noise field filling the canvas. [SourceKind(\"noise\", DisplayName = \"Noise\")] public class NoiseVideoSource : VideoSource Inheritance object Source VideoSource NoiseVideoSource Inherited Members VideoSource.GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Detail [Editable(\"Detail\", Min = 0, Max = 1, Step = 0.001)] public float Detail { get; set; } Property Value float Seed [Editable(\"Seed\")] public int Seed { get; set; } Property Value int SeetheRate [Editable(\"Seethe rate\", Min = 0, Max = 1, Step = 0.001)] public float SeetheRate { get; set; } Property Value float Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override NoiseVideoSource Duplicate() Returns NoiseVideoSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Video.TextVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.TextVideoSource.html",
    "title": "Class TextVideoSource | EditSharp",
    "summary": "Class TextVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll A block of white text at a fixed size, centred in a frame-sized image, so a transform places it and editing the words never resizes them. [SourceKind(\"text\", DisplayName = \"Text\")] public class TextVideoSource : VideoSource, IChoiceProvider Inheritance object Source VideoSource TextVideoSource Implements IChoiceProvider Inherited Members VideoSource.GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Box [Editable(\"Box\", Min = 0.01, Max = 1, Step = 0.01, Frame = FrameMeasure.Frame)] public Vector2 Box { get; set; } Property Value Vector2 Content [Editable(\"Text\", Editor = PropertyEditor.Multiline)] public string Content { get; set; } Property Value string Font [Editable(\"Font\")] public string Font { get; set; } Property Value string HorizontalAlignment [Editable(\"Horizontal alignment\")] public HorizontalTextAlignment HorizontalAlignment { get; set; } Property Value HorizontalTextAlignment Italic [Editable(\"Italic\")] public bool Italic { get; set; } Property Value bool Size [Editable(\"Size\", Min = 0.001, Max = 1, Step = 0.001, Frame = FrameMeasure.Width)] public float Size { get; set; } Property Value float VerticalAlignment [Editable(\"Vertical alignment\")] public VerticalTextAlignment VerticalAlignment { get; set; } Property Value VerticalTextAlignment Weight [Editable(\"Weight\")] public int Weight { get; set; } Property Value int Wrap [Editable(\"Wrap\")] public TextWrap Wrap { get; set; } Property Value TextWrap Methods ChoicesFor(string) Null when the property is free to take any value. public IReadOnlyList<Choice>? ChoicesFor(string property) Parameters property string Returns IReadOnlyList<Choice> Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override TextVideoSource Duplicate() Returns TextVideoSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Video.TextWrap.html": {
    "href": "api/EditSharp.Components.Sources.Video.TextWrap.html",
    "title": "Enum TextWrap | EditSharp",
    "summary": "Enum TextWrap Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll public enum TextWrap Fields Off = 0 WrapCharacters = 2 WrapWords = 1"
  },
  "api/EditSharp.Components.Sources.Video.TimelineVideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.TimelineVideoSource.html",
    "title": "Class TimelineVideoSource | EditSharp",
    "summary": "Class TimelineVideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll Another timeline's picture, composited frame by frame. Start/Duration/Loop trim it like any source; its natural length is the timeline's duration. Saved as the timeline's Id (see SourceSerializer.Deserialize). Compositor-bound: frames are composited with the parent's own GPU context and surface pool, on its GPU thread, so they never cross GPU contexts and are never prefetched. [SourceKind(\"timeline-video\", DisplayName = \"Timeline\", Listed = false)] public class TimelineVideoSource : VideoSource Inheritance object Source VideoSource TimelineVideoSource Inherited Members VideoSource.GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) Source.Start Source.Duration Source.Loop Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Timeline [Editable(\"Timeline\")] public Timeline? Timeline { get; set; } Property Value Timeline Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override TimelineVideoSource Duplicate() Returns TimelineVideoSource GetNaturalLengthAsync(CancellationToken) How long the source's own material is, ignoring Start/Duration. Null means unbounded; a still image or a generator has no inherent end, so only Duration (or the clip) limits it. public override Task<TimeSpan?> GetNaturalLengthAsync(CancellationToken ct = default) Parameters ct CancellationToken Returns Task<TimeSpan?>"
  },
  "api/EditSharp.Components.Sources.Video.VerticalTextAlignment.html": {
    "href": "api/EditSharp.Components.Sources.Video.VerticalTextAlignment.html",
    "title": "Enum VerticalTextAlignment | EditSharp",
    "summary": "Enum VerticalTextAlignment Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll public enum VerticalTextAlignment Fields Bottom = 2 Center = 1 Top = 0"
  },
  "api/EditSharp.Components.Sources.Video.VideoSource.html": {
    "href": "api/EditSharp.Components.Sources.Video.VideoSource.html",
    "title": "Class VideoSource | EditSharp",
    "summary": "Class VideoSource Namespace EditSharp.Components.Sources.Video Assembly EditSharp.dll A source of frames. Reading is two-phase: PrepareAsync does the slow, once-per-session work (probing, choosing which file/decoder to use) and hands back a prepared handle; the handle then opens cheap, synchronous readers whenever compositing needs one. The handle and its readers hold all state; the source itself stays plain data. public abstract class VideoSource : Source Inheritance object Source VideoSource Derived BlenderSceneVideoSource ColorVideoSource MediaVideoSource NoiseVideoSource TextVideoSource TimelineVideoSource Inherited Members Source.Start Source.Duration Source.Loop Source.GetNaturalLengthAsync(CancellationToken) Source.GetUsableLengthAsync(CancellationToken) Source.TryGetNaturalLength(out TimeSpan?) Source.TryGetUsableLength(out TimeSpan?) Source.Animatables Source.ResolveWindow(TimeSpan?) Source.ToSourceTime(TimeSpan, TimeSpan?) object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Duplicate() Deep copy, through SourceSerializer; see class remarks. History is suppressed: a fresh copy has no past to undo. public override VideoSource Duplicate() Returns VideoSource GetFrameAtAsync(TimeSpan, SourceMode, int, int, CancellationToken) One frame at contentTime, for callers that just need a single picture (thumbnails); nothing stays open afterwards. The caller owns the result. mode picks proxy or original like a session would (ProxiesOnly throws ProxyPending or ProxyMissing where there's no proxy frame); maxWidth/maxHeight cap the size (0 = native). Kinds with a cheaper route (a still image) override it. public virtual Task<SKImage> GetFrameAtAsync(TimeSpan contentTime, SourceMode mode = SourceMode.SourceOnly, int maxWidth = 0, int maxHeight = 0, CancellationToken ct = default) Parameters contentTime TimeSpan mode SourceMode maxWidth int maxHeight int ct CancellationToken Returns Task<SKImage>"
  },
  "api/EditSharp.Components.Sources.Video.html": {
    "href": "api/EditSharp.Components.Sources.Video.html",
    "title": "Namespace EditSharp.Components.Sources.Video | EditSharp",
    "summary": "Namespace EditSharp.Components.Sources.Video Classes BlenderSceneVideoSource ColorVideoSource A solid colour filling the canvas; the colour can be keyframed. MediaVideoSource A video file or a still image on disk; which one is decided by probing when prepared, never stored. A still has no natural length (Duration or the clip decides), has no proxy and is decoded once per session. A video reads its proxy (ProxyCache) or the original according to the session's SourceMode; random access always reads the proxy. NoiseVideoSource An evolving noise field filling the canvas. TextVideoSource A block of white text at a fixed size, centred in a frame-sized image, so a transform places it and editing the words never resizes them. TimelineVideoSource Another timeline's picture, composited frame by frame. Start/Duration/Loop trim it like any source; its natural length is the timeline's duration. Saved as the timeline's Id (see SourceSerializer.Deserialize). Compositor-bound: frames are composited with the parent's own GPU context and surface pool, on its GPU thread, so they never cross GPU contexts and are never prefetched. VideoSource A source of frames. Reading is two-phase: PrepareAsync does the slow, once-per-session work (probing, choosing which file/decoder to use) and hands back a prepared handle; the handle then opens cheap, synchronous readers whenever compositing needs one. The handle and its readers hold all state; the source itself stays plain data. Enums HorizontalTextAlignment Justify stretches every wrapped line to the box's width; a paragraph's last line stays left. TextWrap VerticalTextAlignment"
  },
  "api/EditSharp.Components.Sources.html": {
    "href": "api/EditSharp.Components.Sources.html",
    "title": "Namespace EditSharp.Components.Sources | EditSharp",
    "summary": "Namespace EditSharp.Components.Sources Namespaces EditSharp.Components.Sources.Audio EditSharp.Components.Sources.Video Classes Source Something that can PROVIDE a clip's raw content (a media file, a rendered scene, an instrument) independently of how that content is later composited or mixed. Compositing never knows what kind of source it is reading from; it only prepares a source, opens readers on it and reacts to the SourceUnavailableReason a failing read reports. Every source is plain data: its [Editable] properties are exactly what SourceSerializer saves, and Duplicate is a round-trip through that same serializer, so a new property is copied precisely when it is saved. Nothing prepared or opened (probe results, decoders) is ever stored on the source itself; several Playbacks may read one source at once. ADDING A KIND: derive from VideoSource or AudioSource, mark the class [SourceKind(\"stable-id\")] (the id is what saved files contain, so never change it), implement PrepareAsync and GetNaturalLengthAsync, and add anything identifying its content to AddFingerprint. See BlenderSceneVideoSource/VSTInstrumentSource for the bare shape. SourceKindAttribute Registers a concrete Source for serialization under a stable id. The id is written into saved data as \"$kind\", so it must never change once shipped; rename the class freely, never the id. DisplayName is what editors show; Listed false keeps a kind out of pickers (examples, unfinished kinds) while it still loads. SourceKindInfo A kind a source can be switched to: its saved id, its name in pickers, its class. SourceKinds The source kinds pickers offer, and switching a source from one kind to another. SourceSerializer Saves and loads sources as JSON. Every abstract Source type (Source, VideoSource, AudioSource, ...) is polymorphic, discriminated by \"$kind\" with the id from each concrete kind's [SourceKind]; no central list to edit when a kind is added. Property names are camelCase and TimeSpans are \"c\"-format strings (JsonSerializerDefaults.Web). Enums are names, keyframed values carry their whole track, colours are \"#AARRGGBB\", and a nested Timeline is saved as its Id (see Deserialize). Consumers embedding sources in their own JSON use Options directly (or chain its TypeInfoResolver into theirs); Serialize/Deserialize are the standalone shortcut. Loading always runs with history suppressed, so a deserialized source arrives with no undo entries of its own. Only kinds in this assembly are discovered for now (source kinds are internal to EditSharp); registering external assemblies belongs with opening the source contract up. SourceUnavailableException The one way a source reports that it cannot provide content. Sources never substitute placeholders themselves; they throw this and let the caller decide by Reason. Interfaces IFileBackedSource A source whose content lives in one file on disk. File-level services (proxies, grouping clips that share a file) work against this rather than any concrete kind, so a new file-backed kind gets them by implementing it. Enums SourceMode Whether proxy-capable sources read their proxy or their original. Kinds with no notion of a proxy (a still image, a 3D scene) ignore this and always render from source. Random access (scrubbing, reverse) always needs a proxy regardless of mode; decoding an original at an arbitrary position per frame isn't viable. SourceUnavailableReason Why a source could not provide content right now; compositing picks what to show from this."
  },
  "api/EditSharp.Components.SpatialKeyframe.html": {
    "href": "api/EditSharp.Components.SpatialKeyframe.html",
    "title": "Class SpatialKeyframe | EditSharp",
    "summary": "Class SpatialKeyframe Namespace EditSharp.Components Assembly EditSharp.dll A position keyframe adds a SECOND, independent kind of handle beyond the temporal one every Keyframe<T> already has: the shape of the motion path itself in canvas space, separate from how speed eases along that path over time. See the schema doc's \"Spatial handles for position\" section for the full AE/Resolve-style reasoning. public sealed class SpatialKeyframe : Keyframe<Vector2>, IKeyframe Inheritance object Keyframe<Vector2> SpatialKeyframe Implements IKeyframe Inherited Members Keyframe<Vector2>.Start Keyframe<Vector2>.Value Keyframe<Vector2>.InInterpolation Keyframe<Vector2>.OutInterpolation Keyframe<Vector2>.InHandle Keyframe<Vector2>.OutHandle Keyframe<Vector2>.InTangentMode Keyframe<Vector2>.OutTangentMode object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties SpatialInHandle public Vector2? SpatialInHandle { get; } Property Value Vector2? SpatialInTangentMode public TangentMode SpatialInTangentMode { get; } Property Value TangentMode SpatialOutHandle public Vector2? SpatialOutHandle { get; } Property Value Vector2? SpatialOutTangentMode public TangentMode SpatialOutTangentMode { get; } Property Value TangentMode"
  },
  "api/EditSharp.Components.TangentMode.html": {
    "href": "api/EditSharp.Components.TangentMode.html",
    "title": "Enum TangentMode | EditSharp",
    "summary": "Enum TangentMode Namespace EditSharp.Components Assembly EditSharp.dll public enum TangentMode Fields Auto = 2 Free = 0 Smooth = 1"
  },
  "api/EditSharp.Components.Timeline.html": {
    "href": "api/EditSharp.Components.Timeline.html",
    "title": "Class Timeline | EditSharp",
    "summary": "Class Timeline Namespace EditSharp.Components Assembly EditSharp.dll A sequence of Channels. Also the seat of nested-timeline bookkeeping (cycle prevention, UsedBy tracking) — see the remarks on ValidateNoCycle/RegisterEmbeddedTimelines/NotifyClipDetached below. REWRITE (\"clips are graphs\"): the old dedicated AddTimelineClip/RippleAddTimelineClip/AddTimelineClipCore methods are GONE. Embedding a nested Timeline used to require a special add path because it was a distinct Clip subtype (TimelineVideoClip/ TimelineAudioClip) that only Timeline knew how to construct correctly (cycle-checked, UsedBy-registered). Now that embedding a nested Timeline is just wiring a TimelineVideoInputNode/ TimelineAudioInputNode into an ordinary VideoClip's/AudioClip's graph (built via VideoClip.CreateTimelineEmbed/AudioClip.CreateTimelineEmbed), there's nothing left for a separate Timeline-level add method to do — build the clip, then place it with the SAME Channel.AddClip/ RippleAddClip every other clip uses. Channel.AddClipCore calls back into ValidateNoCycle/RegisterEmbeddedTimelines itself (see Channel.cs), generalized to scan the clip's graph for however many embed nodes it actually has (zero, one, or several) rather than assuming exactly one. REWRITE (\"channels split by kind\"): VideoChannels and AudioChannels used to live together in one mixed _channels list, ordered however they were added. They're now two entirely separate lists. The reason is LinkGroup.MoveChannel/RippleMoveChannel (see LinkGroup.cs): asking a linked group to \"move up/down a channel\" only has an unambiguous meaning per member if each member's own channel index is counted among channels of its OWN kind — a video clip and its linked audio clip can never share a channel, so there's no single mixed index that means the same thing for both of them at once. With two separate lists, \"move this video clip's channel index by delta\" and \"move this audio clip's channel index by the SAME delta\" are just two independent, symmetric lookups — see ResolveChannelDelta below, which also creates whichever new channels (of the matching kind) a delta past the current top requires. VideoChannels keeps its own bottom-to-top compositing order exactly as before; AudioChannels' order still carries no acoustic meaning (see AudioMixer). public sealed class Timeline Inheritance object Timeline Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties AudioChannels Every AudioChannel on this timeline. Order has no acoustic meaning (see AudioMixer's own remarks) — it only affects how channels are listed in a UI. public IReadOnlyList<AudioChannel> AudioChannels { get; } Property Value IReadOnlyList<AudioChannel> Channels Every channel on this timeline, video channels first (in their own compositing order) then audio channels — a convenience view for code that genuinely doesn't care about the split (\"is there at least one channel at all,\" walking every clip on the timeline, and so on). Allocates a freshly merged list on every access, so a caller in a hot per-frame path should prefer VideoChannels/AudioChannels directly instead — see FrameStateResolver/AudioMixer, which do exactly that. public IReadOnlyList<Channel> Channels { get; } Property Value IReadOnlyList<Channel> Duration public TimeSpan Duration { get; } Property Value TimeSpan Id Stable identity, saved with the timeline; nested-timeline sources refer to it. public Guid Id { get; init; } Property Value Guid UsedBy public IReadOnlyList<Timeline> UsedBy { get; } Property Value IReadOnlyList<Timeline> VideoChannels Every VideoChannel on this timeline, in compositing order — index 0 is the bottom layer, each one after it draws on top, exactly as before this split. public IReadOnlyList<VideoChannel> VideoChannels { get; } Property Value IReadOnlyList<VideoChannel> Methods AddChannel(AudioChannel) public AudioChannel AddChannel(AudioChannel channel) Parameters channel AudioChannel Returns AudioChannel AddChannel(Channel) Runtime-typed fallback for a caller holding only a Channel reference (its concrete type isn't known until this runs) — dispatches to whichever typed overload above actually matches. Prefer AddChannel(VideoChannel)/AddChannel(AudioChannel) when the concrete type is known at the call site; they need no such dispatch and hand back the concrete type directly. public Channel AddChannel(Channel channel) Parameters channel Channel Returns Channel AddChannel(VideoChannel) public VideoChannel AddChannel(VideoChannel channel) Parameters channel VideoChannel Returns VideoChannel GetLinkGroup(Guid?) public LinkGroup? GetLinkGroup(Guid? id) Parameters id Guid? Returns LinkGroup Link(IEnumerable<Clip>) Adds every clip in clips to one link group (an existing one, if any of them already belongs to one; otherwise a fresh Id). BUG FOUND IN THE FIELD (fixed here): a clip being linked here may already belong to a DIFFERENT group than the one this call settles on — e.g. linking [B, C] where B is already grouped with A (group G1) and C is already grouped with D (group G2) resolves to G1, silently \"poaching\" C out of G2 and leaving D behind as an orphaned singleton group (LinkGroupId set, but the sole member). Every other group-membership change in this file (LinkGroup.Split, Timeline.NotifyClipDetached) auto-dissolves a group once it's down to one member; this method used to be the one place that didn't. Fixed by recording each clip's PRE-reassignment group, then dissolving any of those old groups left with exactly one member once the reassignment is done. public LinkGroup Link(IEnumerable<Clip> clips) Parameters clips IEnumerable<Clip> Returns LinkGroup RemoveChannel(Channel) public void RemoveChannel(Channel channel) Parameters channel Channel RippleRemoveRange(TimeSpan, TimeSpan) The same range taken out of every channel, each gap closed — the whole timeline gets shorter by the range, so nothing on one channel drifts against another. A ripple delete that keeps the tracks in sync, at the cost of whatever else sat in that range. public void RippleRemoveRange(TimeSpan start, TimeSpan end) Parameters start TimeSpan end TimeSpan"
  },
  "api/EditSharp.Components.Transitions.FadeToColorTransition.html": {
    "href": "api/EditSharp.Components.Transitions.FadeToColorTransition.html",
    "title": "Class FadeToColorTransition | EditSharp",
    "summary": "Class FadeToColorTransition Namespace EditSharp.Components.Transitions Assembly EditSharp.dll Fades OUT to Color, then fades IN from Color. public sealed class FadeToColorTransition : Transition Inheritance object Transition FadeToColorTransition Inherited Members Transition.From Transition.To Transition.Duration object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Color [Editable(\"Color\")] public SKColor Color { get; set; } Property Value SKColor Methods Duplicate() public override Transition Duplicate() Returns Transition"
  },
  "api/EditSharp.Components.Transitions.FadeTransition.html": {
    "href": "api/EditSharp.Components.Transitions.FadeTransition.html",
    "title": "Class FadeTransition | EditSharp",
    "summary": "Class FadeTransition Namespace EditSharp.Components.Transitions Assembly EditSharp.dll Direct crossfade between the two clips — no intermediate colour. public sealed class FadeTransition : Transition Inheritance object Transition FadeTransition Inherited Members Transition.From Transition.To Transition.Duration object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Methods Duplicate() public override Transition Duplicate() Returns Transition"
  },
  "api/EditSharp.Components.Transitions.SlideTransition.html": {
    "href": "api/EditSharp.Components.Transitions.SlideTransition.html",
    "title": "Class SlideTransition | EditSharp",
    "summary": "Class SlideTransition Namespace EditSharp.Components.Transitions Assembly EditSharp.dll Both clips translate together across the canvas at Angle degrees. public sealed class SlideTransition : Transition Inheritance object Transition SlideTransition Inherited Members Transition.From Transition.To Transition.Duration object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Angle [Editable(\"Angle\", Editor = PropertyEditor.Angle)] public float Angle { get; set; } Property Value float Methods Duplicate() public override Transition Duplicate() Returns Transition"
  },
  "api/EditSharp.Components.Transitions.Transition.html": {
    "href": "api/EditSharp.Components.Transitions.Transition.html",
    "title": "Class Transition | EditSharp",
    "summary": "Class Transition Namespace EditSharp.Components.Transitions Assembly EditSharp.dll One channel-level transition between two adjacent clips. Abstract — the CLASS is the type, no separate TransitionType enum, matching Effect/EffectNode's own \"the class is the type\" convention elsewhere in this schema. NEW in this rewrite: carries its own From/To directly (previously a Channel held a separate (Clip, Transition) tuple list) — see the schema doc's Channel section. This is what lets Channel.Transitions simply be a List<Transition>. public abstract class Transition Inheritance object Transition Derived FadeToColorTransition FadeTransition SlideTransition Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties Duration [Editable(\"Duration\")] public TimeSpan Duration { get; set; } Property Value TimeSpan From public Clip From { get; } Property Value Clip To public Clip To { get; } Property Value Clip Methods Duplicate() public abstract Transition Duplicate() Returns Transition"
  },
  "api/EditSharp.Components.Transitions.html": {
    "href": "api/EditSharp.Components.Transitions.html",
    "title": "Namespace EditSharp.Components.Transitions | EditSharp",
    "summary": "Namespace EditSharp.Components.Transitions Classes FadeToColorTransition Fades OUT to Color, then fades IN from Color. FadeTransition Direct crossfade between the two clips — no intermediate colour. SlideTransition Both clips translate together across the canvas at Angle degrees. Transition One channel-level transition between two adjacent clips. Abstract — the CLASS is the type, no separate TransitionType enum, matching Effect/EffectNode's own \"the class is the type\" convention elsewhere in this schema. NEW in this rewrite: carries its own From/To directly (previously a Channel held a separate (Clip, Transition) tuple list) — see the schema doc's Channel section. This is what lets Channel.Transitions simply be a List<Transition>."
  },
  "api/EditSharp.Components.html": {
    "href": "api/EditSharp.Components.html",
    "title": "Namespace EditSharp.Components | EditSharp",
    "summary": "Namespace EditSharp.Components Namespaces EditSharp.Components.Channels EditSharp.Components.Clips EditSharp.Components.Nodes EditSharp.Components.Sources EditSharp.Components.Transitions Classes Animatable<T> FontFamilies The font families installed on this machine, by name. Interpolators Resolves the IInterpolator<T> for a payload type. Covers the three types this schema actually keyframes today (float, Vector2, SKColor — see the Effects/Keyframes sections of the schema doc). A new Animatable<T> payload type needs one line registered here; that is the entire cost of the \"new properties can easily become animatable\" goal for anything that isn't one of these three already. KeyframeHandle<T> One end of a keyframe's bezier handle: a 2D offset from the keyframe itself (time offset + value offset), not a single scalar \"how much speed to remove\" the way the old EaseInStrength/EaseOutStrength were. See the migration note on Keyframe.cs's old shape. CONVENTION: TimeOffset is always stored as a non-negative magnitude — \"how far this handle reaches toward the OTHER keyframe of the segment,\" regardless of whether it's an in-handle or an out-handle. An out-handle is applied by ADDING TimeOffset to its own keyframe's Start; an in-handle is applied by SUBTRACTING TimeOffset from its own keyframe's Start. This is what makes \"clamp so it can never cross the neighboring keyframe\" a single one-directional clamp (TimeOffset can never exceed the gap to the neighbor) instead of needing a sign check at every call site. KeyframeTrack<T> One animatable property's own timeline of keyframes. See the schema doc's Keyframes section for the full design — this is \"one track per animatable property,\" replacing the old whole-ClipTransform-per- keyframe list. Encapsulation principle applies here same as everywhere else in the schema: Keyframes is read-only externally, all mutation goes through this class's own methods — which is what makes handle-clamping and Auto-tangent-recompute-on-neighbor-change actually enforceable rather than something a caller could bypass by editing a Keyframe directly. Keyframe<T> LinkGroup A real object owning coordination logic, not just a shared Guid — see the schema doc. Carries no state beyond Id and which Timeline to derive membership from; Members is always a live scan (matches the doc: \"IReadOnlyList<Clip> Members; // derived from Clip.LinkGroupId == Id\"), so Timeline.GetLinkGroup handing back a fresh instance on every call is correct, not wasteful. Membership is flat — a Clip belongs to at most one LinkGroup (a single nullable LinkGroupId, not a collection); merge-on-link is how two groups combine instead of nesting. PositionTrack Position's own KeyframeTrack<Vector2> subclass. Evaluate is deliberately two-stage rather than sharing the base class's plain value-cubic: Temporal stage: the ordinary Hold/Linear/Bezier handle logic (this class's inherited InHandle/OutHandle, via the base's shared SolveU) produces a scalar progress u in [0,1] between the two bounding keyframes — exactly the same computation every other KeyframeTrack<T> does to find its cubic parameter. Spatial stage: u is fed as the parametric input to the cubic bezier PATH defined by (left.Value, left.SpatialOutHandle, right.SpatialInHandle, right.Value). Easing each axis independently (the base class's own approach, if it were used unmodified here) is the classic bug that produces visibly curved motion on an intended straight line — see the schema doc for why \"how fast\" and \"which way\" have to be two curves composed through one shared progress value instead. SpatialKeyframe A position keyframe adds a SECOND, independent kind of handle beyond the temporal one every Keyframe<T> already has: the shape of the motion path itself in canvas space, separate from how speed eases along that path over time. See the schema doc's \"Spatial handles for position\" section for the full AE/Resolve-style reasoning. Timeline A sequence of Channels. Also the seat of nested-timeline bookkeeping (cycle prevention, UsedBy tracking) — see the remarks on ValidateNoCycle/RegisterEmbeddedTimelines/NotifyClipDetached below. REWRITE (\"clips are graphs\"): the old dedicated AddTimelineClip/RippleAddTimelineClip/AddTimelineClipCore methods are GONE. Embedding a nested Timeline used to require a special add path because it was a distinct Clip subtype (TimelineVideoClip/ TimelineAudioClip) that only Timeline knew how to construct correctly (cycle-checked, UsedBy-registered). Now that embedding a nested Timeline is just wiring a TimelineVideoInputNode/ TimelineAudioInputNode into an ordinary VideoClip's/AudioClip's graph (built via VideoClip.CreateTimelineEmbed/AudioClip.CreateTimelineEmbed), there's nothing left for a separate Timeline-level add method to do — build the clip, then place it with the SAME Channel.AddClip/ RippleAddClip every other clip uses. Channel.AddClipCore calls back into ValidateNoCycle/RegisterEmbeddedTimelines itself (see Channel.cs), generalized to scan the clip's graph for however many embed nodes it actually has (zero, one, or several) rather than assuming exactly one. REWRITE (\"channels split by kind\"): VideoChannels and AudioChannels used to live together in one mixed _channels list, ordered however they were added. They're now two entirely separate lists. The reason is LinkGroup.MoveChannel/RippleMoveChannel (see LinkGroup.cs): asking a linked group to \"move up/down a channel\" only has an unambiguous meaning per member if each member's own channel index is counted among channels of its OWN kind — a video clip and its linked audio clip can never share a channel, so there's no single mixed index that means the same thing for both of them at once. With two separate lists, \"move this video clip's channel index by delta\" and \"move this audio clip's channel index by the SAME delta\" are just two independent, symmetric lookups — see ResolveChannelDelta below, which also creates whichever new channels (of the matching kind) a delta past the current top requires. VideoChannels keeps its own bottom-to-top compositing order exactly as before; AudioChannels' order still carries no acoustic meaning (see AudioMixer). Interfaces IAnimatable IInterpolator<T> The arithmetic Animatable<T>/KeyframeTrack<T> need to do their job: add/subtract two values (for handle offsets, which are stored as deltas from their keyframe's own value) and blend two values by a normalized progress (for the cubic evaluation itself). Deliberately a small strategy interface resolved per-T, NOT a self-referential generic constraint (T : IInterpolatable<T>) — the three real payload types this schema keyframes (float, Vector2, SKColor) are all BCL/SkiaSharp types this package doesn't own, so they can't be made to declare an interface themselves. See Interpolators below for the registry that resolves one of these per T. IKeyframe The general per-property wrapper — see the schema doc's Keyframes section. Wrapping a property in Animatable<T> is the only step required to make it keyframeable; nothing about KeyframeTrack<T> itself needs to change for a new property type (as long as an IInterpolator<T> is registered — see Interpolators.Resolve). ITimelineEditable The core behavior contract shared by Clip and LinkGroup — see the schema doc's \"The core behavior rule\" section. Called on a Clip, every member here affects only that clip, exactly like an unlinked clip's behavior always has, regardless of whether it belongs to a LinkGroup. Called on a LinkGroup, the same member coordinates the edit across every member, handling type/constraint mismatches internally. This is a universal contract — any operation added here in the future automatically gets that same scoping from both implementations, not a per-operation special case. Exact member shapes are this rewrite's own call (the schema doc explicitly deferred \"exact ITimelineEditable signatures\"); Split deliberately returns void rather than the new fragments — the encapsulation principle means a channel's Clips collection is the source of truth for what exists after a split, not a value handed back from the call that caused it. Enums InterpolationType TangentMode"
  },
  "api/EditSharp.EditSharpConfig.html": {
    "href": "api/EditSharp.EditSharpConfig.html",
    "title": "Class EditSharpConfig | EditSharp",
    "summary": "Class EditSharpConfig Namespace EditSharp Assembly EditSharp.dll Process-wide configuration for EditSharp. Set these once at startup, before the first call into TimelineAssembler — nothing here is per-render, so a value changed mid-flight applies to every render sharing the process from that point on, not just the next one. public static class EditSharpConfig Inheritance object EditSharpConfig Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties AudioBlockFrames Frames of audio processed per engine tick. Parameter changes land within one block (512 frames is about 10.7 ms at 48 kHz). Read when a session starts. public static int AudioBlockFrames { get; set; } Property Value int AudioLatency How far ahead of the speakers playback renders audio, which is also how long an edit takes to be heard. Read when a session starts. public static TimeSpan AudioLatency { get; set; } Property Value TimeSpan EsrpCompressionLevel Zstd level for .esrp proxy frames (1-22). Higher is smaller and slower to build: on 1280x720 frames, 9 is about 18x faster than 19 for about 10% more disk. Reading is the same speed at any level. public static int EsrpCompressionLevel { get; set; } Property Value int EsrpCompressionScheme How .esrp proxies compress each frame. Applies to newly built proxies only. public static EsrpCompressionScheme EsrpCompressionScheme { get; set; } Property Value EsrpCompressionScheme FfmpegPath Executable name or full path used to launch ffmpeg. Defaults to \"ffmpeg\", resolved from PATH. public static string FfmpegPath { get; set; } Property Value string FfprobePath Executable name or full path used to launch ffprobe. Defaults to \"ffprobe\", resolved from PATH. public static string FfprobePath { get; set; } Property Value string FilterThreads How many threads libavfilter may use to execute a filter graph (ffmpeg's -filter_threads / -filter_complex_threads). Defaults to 1 — deliberately NOT the processor count ffmpeg would otherwise pick on its own. THIS DEFAULT IS A CORRECTNESS FIX, NOT A TUNING CHOICE. Rendering on a 16-logical-core machine produced exactly 15 evenly spaced horizontal black lines across every frame — 15 being precisely the number of INTERIOR boundaries between 16 slices. Measured off a real affected frame: the lines sat at rows 60, 129, 198 ... 1026 (a dead-uniform 69px pitch, 15 of them), and spanned x=960 to x=1919 — exactly the right half of a 1920-wide canvas, to the pixel. Both numbers are structural rather than incidental: a count equal to threads-1, and a boundary at exactly width/2, is what a per-thread work partition looks like when the seams between its regions don't reconstruct cleanly. The artifact is a one-pixel-tall row of ZEROED ALPHA at each seam, which then lets the opaque black backdrop FrameFilterChain.Flatten composites onto show through — hence \"black lines\" rather than discoloured ones. Three independent observations ruled out every non-threading explanation that was considered: The lines DON'T move or change count when the canvas resolution changes. A frame-size-derived partition (an FFV1 slice grid, a buffer-size seam) necessarily would; a thread-count-derived one doesn't, because thread count is a property of the machine and not of the frame. They appear with NO NoiseClip present, and on a blueprint of a single GeneratorClip with no effects on a Normal-blend channel — which rules out perlin, FFV1 (that graph decodes nothing at all; its content is a color source), and every effect chain. They are already in the RAW ACCUMULATOR, before FinalizeOutputAsync's H.264/yuv420p encode ever runs, so the final encode is not introducing them either. What's left is the per-frame filter graph itself, executed multi-threaded. Every clip runs perspective unconditionally (ClipVideoChain.Build always calls ApplyTransform, identity transform or not), scale several times including the MaskSupersample up/down pair, and the accumulator's own starting frame (FfmpegArgs.BuildTransparentBlank) is built fresh on every single render regardless of blueprint — all filters libavfilter is free to slice across threads. Worth knowing for anyone revisiting this: ffmpeg 8.0 (this project's target) ships a rewritten, multi-threaded swscale, so a scale that was effectively serial on the ffmpeg this pipeline was first written against is not serial any more. A PER-FILTER version of this fix (a threads=1 pin on only the specific filters implicated, leaving the rest of the graph free to thread) was built, tested, and reverted — worth recording why, since it looked like the right call at the time. An early A/B seemed to show the global pin costing roughly 3x per-frame render time on a real blueprint (4-7s baseline vs 15s+ pinned), which justified the extra complexity. That comparison turned out to be invalid: the baseline was measured at Blueprint.FrameRenderConcurrency=1 and the pinned case at FrameRenderConcurrency=4 — an unrelated variable left inconsistent between the two runs, not a cost of the pin itself. Re-measured at matched concurrency, the global pin's actual cost was negligible on this blueprint and every other one tested. The per-filter mechanism (FfmpegArgs.ThreadPin and a threads=1 suffix threaded through every filter emission in ClipVideoChain and FrameFilterChain) was real code paid for under a false premise, so it was removed rather than kept \"just in case\" — simplicity won once the actual numbers were in. If a genuine need to reclaim filter-level parallelism ever shows up, EditSharp-Handoff.md / project history has the reverted version to start from rather than rebuilding it from scratch. The single-frame render design is what makes 1 an acceptable default rather than a painful one: parallelism here is better spent at the PROCESS level (Blueprint.FrameRenderConcurrency, which renders whole frames concurrently) than inside one frame's graph, since whole frames are perfectly independent while filter slices have to stitch back together — which is the very thing going wrong. If you raise this, raise it while watching for the lines to come back. public static int FilterThreads { get; set; } Property Value int Logger Where EditSharp reports render progress and diagnostics. Defaults to a no-op logger, so a consumer who never sets this gets silence rather than an exception or unexpected console output. public static IEditSharpLogger Logger { get; set; } Property Value IEditSharpLogger MaxConcurrentProxyBuilds How many proxies may build at once; further BuildAsync calls queue (ProxyState.Queued) in order. Read when a build is dequeued, so a change applies to builds that haven't started yet. public static int MaxConcurrentProxyBuilds { get; set; } Property Value int ProxyDirectory Root directory ProxyCache reads and writes proxies in (and where MediaHasher keeps its hash index). Persistent across runs: nothing in EditSharp ever deletes it, so a consumer can point it somewhere durable and reopen projects against the same proxies later. public static string ProxyDirectory { get; set; } Property Value string ProxyFormat The format ProxyCache.BuildAsync builds when none is named. The .esrp formats read with no subprocess at all (true random access); DNxHR/ProRes are for compatibility and read through ffmpeg. public static ProxyFormat ProxyFormat { get; set; } Property Value ProxyFormat ProxyMaxDimension The longest side a proxy is built at (never upscaled). Proxies keep the source's own frame rate, so one proxy serves scrubbing and playback alike. public static int ProxyMaxDimension { get; set; } Property Value int ReaderBufferFrames How many frames each open reader decodes ahead of (behind, in reverse) the one being shown. Memory is roughly this many decoded frames per visible or upcoming media input. public static int ReaderBufferFrames { get; set; } Property Value int SourceLookahead How far ahead of the playhead (behind, in reverse) playback and export prepare a clip's sources and open their readers, so a clip arriving on screen already has frames waiting instead of stalling. public static TimeSpan SourceLookahead { get; set; } Property Value TimeSpan SourceRetryInterval How long a preview waits before retrying a source that failed with MediaOffline or DecodeError (a drive may be reconnected, a file re-exported). Renders never retry. public static TimeSpan SourceRetryInterval { get; set; } Property Value TimeSpan SwsBackends Which swscale backend(s) ffmpeg 9.0+ is allowed to use for scale/ format conversions (the scaler's -sws_backends flag). Defaults to \"x86\" — explicitly opting in to the new x86 SIMD kernel backend from 9.0's swscale rewrite. THIS DEFAULT IS DELIBERATE, NOT FFMPEG'S OWN DEFAULT. Per FFmpeg's scaler docs, sws_backends defaults to 'auto', which resolves to 'stable' unless the 'unstable' flag is set — and SWS_BACKEND_X86 is defined as part of SWS_BACKEND_UNSTABLE, not SWS_BACKEND_STABLE. So on a stock 9.0 build, every scale/format conversion in this pipeline is running the plain C reference path, not the SIMD path, unless this is set explicitly. This is orthogonal to FilterThreads, not a rerun of the same bug: FilterThreads controls libavfilter's cross-thread SLICING of a frame into pieces (the thing that produced the black-line seams — see that property's remarks), while sws_backends controls which SIMD kernel swscale uses to do the per-pixel math WITHIN a single thread's execution. FilterThreads=1 means there is only one slice/one thread; sws_backends=x86 just makes that one thread faster. Nothing here re-partitions the frame, so the seam mechanism the black-line fix addressed shouldn't apply. That said, this has not yet been verified against a real render on the actual 9.0 build — ffmpeg's own docs describe x86 as an \"unstable\" backend (their word, likely meaning \"newer/less battle-tested\", not literally unsafe), and the swscale rewrite itself is very new. If a render regresses in a way that smells at all like the black-line bug (seams, banding, wrong alpha) after this is turned on, that is the first thing to revert and would mean the two are not as orthogonal in practice as they are on paper. public static string SwsBackends { get; set; } Property Value string TempDirectory Root directory EditSharp writes scratch files into — rasterized text PNGs, oversized filter-graph scripts, segmented-render intermediates. Defaults to AppContext.BaseDirectory (next to the host app's own executable), NOT the OS temp path — do not change this default to Path.GetTempPath(). Testing traced a native ffmpeg crash (0xc0000409, a stack buffer overrun) specifically to scratch files living under Windows %TEMP%, most likely because that path's length (deep under AppData\\Local\\Temp, plus a GUID filename) trips a bug in the comparatively less-mature \"-/option\" file-loading mechanism ffmpeg uses for large filter graphs. A consumer is free to point TempDirectory at %TEMP% anyway, but it's an opt-in regression, not the shipped default. EditSharp creates its own subfolders under this directory lazily, on first write — the host app does not need to pre-create anything, and setting this to a path that doesn't exist yet at all is fine too. public static string TempDirectory { get; set; } Property Value string"
  },
  "api/EditSharp.Editing.Choice.html": {
    "href": "api/EditSharp.Editing.Choice.html",
    "title": "Struct Choice | EditSharp",
    "summary": "Struct Choice Namespace EditSharp.Editing Assembly EditSharp.dll One value a property may take, and what a picker shows for it. public readonly record struct Choice : IEquatable<Choice> Implements IEquatable<Choice> Inherited Members ValueType.Equals(object) ValueType.GetHashCode() ValueType.ToString() object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) Constructors Choice(object, string) One value a property may take, and what a picker shows for it. public Choice(object Value, string Label) Parameters Value object Label string Properties Label public string Label { get; init; } Property Value string Value public object Value { get; init; } Property Value object"
  },
  "api/EditSharp.Editing.ClipFingerprint.html": {
    "href": "api/EditSharp.Editing.ClipFingerprint.html",
    "title": "Class ClipFingerprint | EditSharp",
    "summary": "Class ClipFingerprint Namespace EditSharp.Editing Assembly EditSharp.dll A number that changes when, and only when, a clip would render differently: its speed, the shape of its graph, and every value in it. public static class ClipFingerprint Inheritance object ClipFingerprint Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Remarks What it leaves out is where the clip sits on the timeline, and the head in-point of its media. A head trim moves the in-point and slides every keyframe by the same amount, and the pictures along the content are the same pictures under a different clip-relative time - so keyframe times are hashed against Anchor(Clip), where they stay put. A thumbnail cache keys its frames by anchored time for the same reason, and compares this after every history entry to learn which clips to drop. Values are found through the Editable metadata, plus every Animatables whether attributed or not; a nested timeline hashes as its reference, not its contents. Methods Anchor(Clip) The head in-point the clip's content is anchored to: the first trimmable input's, since every trimmable input in a graph shifts together. A generator clip has none and anchors at zero. public static TimeSpan Anchor(Clip clip) Parameters clip Clip Returns TimeSpan Of(Clip) public static int Of(Clip clip) Parameters clip Clip Returns int"
  },
  "api/EditSharp.Editing.EditableAttribute.html": {
    "href": "api/EditSharp.Editing.EditableAttribute.html",
    "title": "Class EditableAttribute | EditSharp",
    "summary": "Class EditableAttribute Namespace EditSharp.Editing Assembly EditSharp.dll Marks a property as something an inspector or graph editor shows and edits, and says how. Opt-in: a property without this stays out of every inspector, however public it is. Everything here is a hint layered over what the property's type already says — see PropertyDescriptor for what the two resolve to together. [AttributeUsage(AttributeTargets.Property, Inherited = true, AllowMultiple = false)] public sealed class EditableAttribute : Attribute Inheritance object Attribute EditableAttribute Inherited Members Attribute.Equals(object) Attribute.GetCustomAttribute(Assembly, Type) Attribute.GetCustomAttribute(Assembly, Type, bool) Attribute.GetCustomAttribute(MemberInfo, Type) Attribute.GetCustomAttribute(MemberInfo, Type, bool) Attribute.GetCustomAttribute(Module, Type) Attribute.GetCustomAttribute(Module, Type, bool) Attribute.GetCustomAttribute(ParameterInfo, Type) Attribute.GetCustomAttribute(ParameterInfo, Type, bool) Attribute.GetCustomAttributes(Assembly) Attribute.GetCustomAttributes(Assembly, bool) Attribute.GetCustomAttributes(Assembly, Type) Attribute.GetCustomAttributes(Assembly, Type, bool) Attribute.GetCustomAttributes(MemberInfo) Attribute.GetCustomAttributes(MemberInfo, bool) Attribute.GetCustomAttributes(MemberInfo, Type) Attribute.GetCustomAttributes(MemberInfo, Type, bool) Attribute.GetCustomAttributes(Module) Attribute.GetCustomAttributes(Module, bool) Attribute.GetCustomAttributes(Module, Type) Attribute.GetCustomAttributes(Module, Type, bool) Attribute.GetCustomAttributes(ParameterInfo) Attribute.GetCustomAttributes(ParameterInfo, bool) Attribute.GetCustomAttributes(ParameterInfo, Type) Attribute.GetCustomAttributes(ParameterInfo, Type, bool) Attribute.GetHashCode() Attribute.IsDefaultAttribute() Attribute.IsDefined(Assembly, Type) Attribute.IsDefined(Assembly, Type, bool) Attribute.IsDefined(MemberInfo, Type) Attribute.IsDefined(MemberInfo, Type, bool) Attribute.IsDefined(Module, Type) Attribute.IsDefined(Module, Type, bool) Attribute.IsDefined(ParameterInfo, Type) Attribute.IsDefined(ParameterInfo, Type, bool) Attribute.Match(object) Attribute.TypeId object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors EditableAttribute(string?) public EditableAttribute(string? displayName = null) Parameters displayName string Properties Default The value a reset returns the property to. Attributes only take constants, so this covers numbers, strings, bools and enums; a property without one falls back to the value a freshly constructed object has - see PropertyDescriptor.GetDefault. public object? Default { get; set; } Property Value object DisplayName The label shown. Null derives one from the property name. public string? DisplayName { get; } Property Value string Editor public PropertyEditor Editor { get; set; } Property Value PropertyEditor Frame How the value measures against the frame; see FrameMeasure. Applies to a list's items too. public FrameMeasure Frame { get; set; } Property Value FrameMeasure Group Properties sharing a group are shown together under it. public string? Group { get; set; } Property Value string Max public double Max { get; set; } Property Value double Min Range and step for numeric editors, in the property's own units. NaN leaves it unbounded. public double Min { get; set; } Property Value double Order Lower comes first. Equal orders keep declaration order. public int Order { get; set; } Property Value int ReadOnly Shown but not editable. public bool ReadOnly { get; set; } Property Value bool Step public double Step { get; set; } Property Value double Tooltip public string? Tooltip { get; set; } Property Value string Unit Shown after the value — \"dB\", \"Hz\", \"ms\". public string? Unit { get; set; } Property Value string"
  },
  "api/EditSharp.Editing.FrameMeasure.html": {
    "href": "api/EditSharp.Editing.FrameMeasure.html",
    "title": "Enum FrameMeasure | EditSharp",
    "summary": "Enum FrameMeasure Namespace EditSharp.Editing Assembly EditSharp.dll How a value measures against the frame, so an editor can show it in pixels of the render resolution. Stored values stay frame-relative, which keeps a project independent of its canvas size. public enum FrameMeasure Fields Frame = 2 A size: x as a fraction of the frame width, y of its height. HalfFrame = 3 A position or offset: x in half frame widths, y in half heights, up positive; 0 is the centre. None = 0 Width = 1 A length, as a fraction of the frame width."
  },
  "api/EditSharp.Editing.IChoiceProvider.html": {
    "href": "api/EditSharp.Editing.IChoiceProvider.html",
    "title": "Interface IChoiceProvider | EditSharp",
    "summary": "Interface IChoiceProvider Namespace EditSharp.Editing Assembly EditSharp.dll An object that offers the values one of its properties may take, shown as a dropdown. public interface IChoiceProvider Methods ChoicesFor(string) Null when the property is free to take any value. IReadOnlyList<Choice>? ChoicesFor(string property) Parameters property string Returns IReadOnlyList<Choice>"
  },
  "api/EditSharp.Editing.IInspectable.html": {
    "href": "api/EditSharp.Editing.IInspectable.html",
    "title": "Interface IInspectable | EditSharp",
    "summary": "Interface IInspectable Namespace EditSharp.Editing Assembly EditSharp.dll An object that describes its own editable properties instead of leaving it to its type — a composite node, whose properties are whichever inner ones it chose to expose. public interface IInspectable Properties Properties IReadOnlyList<PropertyDescriptor> Properties { get; } Property Value IReadOnlyList<PropertyDescriptor>"
  },
  "api/EditSharp.Editing.Inspect.html": {
    "href": "api/EditSharp.Editing.Inspect.html",
    "title": "Class Inspect | EditSharp",
    "summary": "Class Inspect Namespace EditSharp.Editing Assembly EditSharp.dll Where editors ask what a thing has to edit. Of(Type) reflects the EditableAttributes once and caches the descriptors; Of(object) lets an IInspectable answer for itself instead. public static class Inspect Inheritance object Inspect Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods Find(object, string) public static PropertyDescriptor? Find(object target, string name) Parameters target object name string Returns PropertyDescriptor Of(object) public static IReadOnlyList<PropertyDescriptor> Of(object target) Parameters target object Returns IReadOnlyList<PropertyDescriptor> Of(Type) public static IReadOnlyList<PropertyDescriptor> Of(Type type) Parameters type Type Returns IReadOnlyList<PropertyDescriptor>"
  },
  "api/EditSharp.Editing.PropertyDescriptor.html": {
    "href": "api/EditSharp.Editing.PropertyDescriptor.html",
    "title": "Class PropertyDescriptor | EditSharp",
    "summary": "Class PropertyDescriptor Namespace EditSharp.Editing Assembly EditSharp.dll Everything an editor needs to know to show and change one property on one object: what to call it, what control to use, what it accepts, whether it can be keyframed, when it applies, and how to read and write it. Resolved once per type from the EditableAttribute and the property's own type (see Inspect), so a new node or clip property becomes editable by carrying the attribute and nothing else. Writes go through the property's own setter, so they are recorded for undo like any other write. List edits are recorded here, since a list's own Add/Remove are not. A descriptor can be redirected (Through) so that a composite node can present an inner node's property as its own, under its own name: the descriptor reads and writes the inner node while the inspector only ever hands it the composite. public sealed class PropertyDescriptor Inheritance object PropertyDescriptor Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Properties Conditions public IReadOnlyList<VisibleWhenAttribute> Conditions { get; } Property Value IReadOnlyList<VisibleWhenAttribute> DisplayName public string DisplayName { get; } Property Value string Editor public PropertyEditor Editor { get; } Property Value PropertyEditor Frame public FrameMeasure Frame { get; } Property Value FrameMeasure Group public string? Group { get; } Property Value string IsAnimatable True for an Animatable<T> — the value can carry keyframes. See GetAnimatable. public bool IsAnimatable { get; } Property Value bool IsCollection True for a List<T>. ItemType/ItemEditor say what the items are; see GetList/AddItem/RemoveItem. public bool IsCollection { get; } Property Value bool IsNullable public bool IsNullable { get; } Property Value bool IsReadOnly public bool IsReadOnly { get; } Property Value bool ItemEditor public PropertyEditor ItemEditor { get; } Property Value PropertyEditor ItemIsAnimatable public bool ItemIsAnimatable { get; } Property Value bool ItemType public Type? ItemType { get; } Property Value Type ItemValueType public Type? ItemValueType { get; } Property Value Type Max public double? Max { get; } Property Value double? Min public double? Min { get; } Property Value double? Name public string Name { get; } Property Value string Order public int Order { get; } Property Value int PropertyType The declared property type, before any unwrapping. public Type PropertyType { get; } Property Value Type Step public double? Step { get; } Property Value double? Tooltip public string? Tooltip { get; } Property Value string Unit public string? Unit { get; } Property Value string ValueType The type an editor works with: Animatable<T> and T? both read as T. public Type ValueType { get; } Property Value Type Methods AddItem(object, object) public void AddItem(object target, object item) Parameters target object item object CreateItem() A fresh item of the list's item type, ready to add. public object CreateItem() Returns object GetAnimatable(object) The Animatable behind an animatable property, for keyframe editors. Null for anything else. public IAnimatable? GetAnimatable(object target) Parameters target object Returns IAnimatable GetList(object) public IList? GetList(object target) Parameters target object Returns IList GetValue(object) The value an editor shows: an Animatable's static value, otherwise the property itself. public object? GetValue(object target) Parameters target object Returns object InsertItem(object, int, object) public void InsertItem(object target, int index, object item) Parameters target object index int item object IsDefault(object) True when the property holds its default on this object. public bool IsDefault(object target) Parameters target object Returns bool IsVisible(object) Whether the property applies right now, per its VisibleWhen conditions. public bool IsVisible(object target) Parameters target object Returns bool RemoveItem(object, int) public void RemoveItem(object target, int index) Parameters target object index int SetValue(object, object?) Writes the value, coercing numbers and enums from whatever an editor hands over. Recorded for undo by the setter it goes through. public void SetValue(object target, object? value) Parameters target object value object Through(Func<object, object>, string?, string?) The same property, reached on another object: resolve turns the target an editor holds into the object that actually carries the property. public PropertyDescriptor Through(Func<object, object> resolve, string? alias = null, string? group = null) Parameters resolve Func<object, object> alias string group string Returns PropertyDescriptor TryGetDefault(object, out object?) The value a reset returns this property to on this object, if there is one: the attribute's Default, else the value the same property has on a freshly constructed object of the holder's type. False when neither exists - an abstract holder with no attribute default, say. public bool TryGetDefault(object target, out object? value) Parameters target object value object Returns bool"
  },
  "api/EditSharp.Editing.PropertyEditor.html": {
    "href": "api/EditSharp.Editing.PropertyEditor.html",
    "title": "Enum PropertyEditor | EditSharp",
    "summary": "Enum PropertyEditor Namespace EditSharp.Editing Assembly EditSharp.dll What kind of control edits a property. Auto lets the type decide — see Inspect.Infer — and the rest override it where the type alone cannot tell (a float that is an angle, a string that is a path). public enum PropertyEditor Fields Angle = 3 Auto = 0 Color = 9 Dropdown = 8 List = 15 Media = 12 Multiline = 7 Number = 1 Object = 16 Path = 13 Percent = 4 Slider = 2 Text = 6 Time = 11 Timeline = 14 Toggle = 5 Vector = 10"
  },
  "api/EditSharp.Editing.VisibleWhenAttribute.html": {
    "href": "api/EditSharp.Editing.VisibleWhenAttribute.html",
    "title": "Class VisibleWhenAttribute | EditSharp",
    "summary": "Class VisibleWhenAttribute Namespace EditSharp.Editing Assembly EditSharp.dll Hides the property unless a sibling property on the same object currently holds one of the given values — a polygon's points are only worth showing while the shape is a polygon. Several of these on one property must all hold. [AttributeUsage(AttributeTargets.Property, Inherited = true, AllowMultiple = true)] public sealed class VisibleWhenAttribute : Attribute Inheritance object Attribute VisibleWhenAttribute Inherited Members Attribute.Equals(object) Attribute.GetCustomAttribute(Assembly, Type) Attribute.GetCustomAttribute(Assembly, Type, bool) Attribute.GetCustomAttribute(MemberInfo, Type) Attribute.GetCustomAttribute(MemberInfo, Type, bool) Attribute.GetCustomAttribute(Module, Type) Attribute.GetCustomAttribute(Module, Type, bool) Attribute.GetCustomAttribute(ParameterInfo, Type) Attribute.GetCustomAttribute(ParameterInfo, Type, bool) Attribute.GetCustomAttributes(Assembly) Attribute.GetCustomAttributes(Assembly, bool) Attribute.GetCustomAttributes(Assembly, Type) Attribute.GetCustomAttributes(Assembly, Type, bool) Attribute.GetCustomAttributes(MemberInfo) Attribute.GetCustomAttributes(MemberInfo, bool) Attribute.GetCustomAttributes(MemberInfo, Type) Attribute.GetCustomAttributes(MemberInfo, Type, bool) Attribute.GetCustomAttributes(Module) Attribute.GetCustomAttributes(Module, bool) Attribute.GetCustomAttributes(Module, Type) Attribute.GetCustomAttributes(Module, Type, bool) Attribute.GetCustomAttributes(ParameterInfo) Attribute.GetCustomAttributes(ParameterInfo, bool) Attribute.GetCustomAttributes(ParameterInfo, Type) Attribute.GetCustomAttributes(ParameterInfo, Type, bool) Attribute.GetHashCode() Attribute.IsDefaultAttribute() Attribute.IsDefined(Assembly, Type) Attribute.IsDefined(Assembly, Type, bool) Attribute.IsDefined(MemberInfo, Type) Attribute.IsDefined(MemberInfo, Type, bool) Attribute.IsDefined(Module, Type) Attribute.IsDefined(Module, Type, bool) Attribute.IsDefined(ParameterInfo, Type) Attribute.IsDefined(ParameterInfo, Type, bool) Attribute.Match(object) Attribute.TypeId object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors VisibleWhenAttribute(string, params object?[]) public VisibleWhenAttribute(string property, params object?[] anyOf) Parameters property string anyOf object[] Properties AnyOf public IReadOnlyList<object?> AnyOf { get; } Property Value IReadOnlyList<object> Property public string Property { get; } Property Value string"
  },
  "api/EditSharp.Editing.html": {
    "href": "api/EditSharp.Editing.html",
    "title": "Namespace EditSharp.Editing | EditSharp",
    "summary": "Namespace EditSharp.Editing Classes ClipFingerprint A number that changes when, and only when, a clip would render differently: its speed, the shape of its graph, and every value in it. EditableAttribute Marks a property as something an inspector or graph editor shows and edits, and says how. Opt-in: a property without this stays out of every inspector, however public it is. Everything here is a hint layered over what the property's type already says — see PropertyDescriptor for what the two resolve to together. Inspect Where editors ask what a thing has to edit. Of(Type) reflects the EditableAttributes once and caches the descriptors; Of(object) lets an IInspectable answer for itself instead. PropertyDescriptor Everything an editor needs to know to show and change one property on one object: what to call it, what control to use, what it accepts, whether it can be keyframed, when it applies, and how to read and write it. Resolved once per type from the EditableAttribute and the property's own type (see Inspect), so a new node or clip property becomes editable by carrying the attribute and nothing else. Writes go through the property's own setter, so they are recorded for undo like any other write. List edits are recorded here, since a list's own Add/Remove are not. A descriptor can be redirected (Through) so that a composite node can present an inner node's property as its own, under its own name: the descriptor reads and writes the inner node while the inspector only ever hands it the composite. VisibleWhenAttribute Hides the property unless a sibling property on the same object currently holds one of the given values — a polygon's points are only worth showing while the shape is a polygon. Several of these on one property must all hold. Structs Choice One value a property may take, and what a picker shows for it. Interfaces IChoiceProvider An object that offers the values one of its properties may take, shown as a dropdown. IInspectable An object that describes its own editable properties instead of leaving it to its type — a composite node, whose properties are whichever inner ones it chose to expose. Enums FrameMeasure How a value measures against the frame, so an editor can show it in pixels of the render resolution. Stored values stay frame-relative, which keeps a project independent of its canvas size. PropertyEditor What kind of control edits a property. Auto lets the type decide — see Inspect.Infer — and the rest override it where the type alone cannot tell (a float that is an angle, a string that is a path)."
  },
  "api/EditSharp.History.ChangeSet.html": {
    "href": "api/EditSharp.History.ChangeSet.html",
    "title": "Class ChangeSet | EditSharp",
    "summary": "Class ChangeSet Namespace EditSharp.History Assembly EditSharp.dll Everything one user action wrote, in order: one History entry. public sealed class ChangeSet : IChange Inheritance object ChangeSet Implements IChange Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors ChangeSet(string) Everything one user action wrote, in order: one History entry. public ChangeSet(string description) Parameters description string The entry's name, shown for undo and redo. Properties Count How many writes were recorded. public int Count { get; } Property Value int Description The entry's name, shown for undo and redo. public string Description { get; } Property Value string Steps The recorded writes, in the order they happened. public IReadOnlyList<IChange> Steps { get; } Property Value IReadOnlyList<IChange> Methods Redo() Reapplies every step, first first. public void Redo() Undo() Reverses every step, last first. public void Undo()"
  },
  "api/EditSharp.History.History.html": {
    "href": "api/EditSharp.History.History.html",
    "title": "Class History | EditSharp",
    "summary": "Class History Namespace EditSharp.History Assembly EditSharp.dll A project's undo chain: one ChangeSet per user action, and a position in it. public sealed class History Inheritance object History Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Remarks Undo steps the position back and reverses the entry it passes; redo steps forward and reapplies it. Committing while redo entries remain drops them. Entries hold only the writes that were made (see Transaction), and Limit caps how many are kept. Properties Active The history that writes made outside any transaction are recorded into; set when a project opens. public static History? Active { get; set; } Property Value History CanRedo Whether there is an undone entry to redo. public bool CanRedo { get; } Property Value bool CanUndo Whether there is an applied entry to undo. public bool CanUndo { get; } Property Value bool Entries Every entry, oldest first, including undone ones still available to redo. public IReadOnlyList<ChangeSet> Entries { get; } Property Value IReadOnlyList<ChangeSet> Limit How many entries are kept; the oldest are dropped past this. public int Limit { get; set; } Property Value int Position How many entries are applied. Entries before it can be undone; entries from it on can be redone. public int Position { get; } Property Value int RedoDescription The description of the entry Redo() would reapply; null when there is none. public string? RedoDescription { get; } Property Value string UndoDescription The description of the entry Undo() would reverse; null when there is none. public string? UndoDescription { get; } Property Value string Methods Begin(string) Opens a transaction that commits into this history. public Transaction.Scope Begin(string description) Parameters description string The entry's name, shown for undo and redo. Returns Transaction.Scope The scope; call Commit() before disposing it to keep the writes. Clear() Drops every entry. The model is left as it is. public void Clear() Redo() Reapplies the next undone entry. public bool Redo() Returns bool False when there is nothing to redo, or a transaction is open. Undo() Reverses the last applied entry. public bool Undo() Returns bool False when there is nothing to undo, or a transaction is open. Events Changed Raised after an entry is committed, undone or redone, and after Clear(). public event EventHandler<HistoryEventArgs>? Changed Event Type EventHandler<HistoryEventArgs>"
  },
  "api/EditSharp.History.HistoryAction.html": {
    "href": "api/EditSharp.History.HistoryAction.html",
    "title": "Enum HistoryAction | EditSharp",
    "summary": "Enum HistoryAction Namespace EditSharp.History Assembly EditSharp.dll What happened to a History, reported by Changed. public enum HistoryAction Fields Clear = 3 Every entry was dropped. Commit = 0 A new entry was added. Redo = 2 The next undone entry was applied again. Undo = 1 The last applied entry was reversed."
  },
  "api/EditSharp.History.HistoryEventArgs.html": {
    "href": "api/EditSharp.History.HistoryEventArgs.html",
    "title": "Class HistoryEventArgs | EditSharp",
    "summary": "Class HistoryEventArgs Namespace EditSharp.History Assembly EditSharp.dll Details of one Changed event. public sealed class HistoryEventArgs : EventArgs Inheritance object EventArgs HistoryEventArgs Inherited Members EventArgs.Empty object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors HistoryEventArgs(HistoryAction, ChangeSet?) Details of one Changed event. public HistoryEventArgs(HistoryAction action, ChangeSet? entry) Parameters action HistoryAction What happened. entry ChangeSet The entry committed, undone or redone; null for Clear. Properties Action What happened. public HistoryAction Action { get; } Property Value HistoryAction Entry The entry committed, undone or redone; null for Clear. public ChangeSet? Entry { get; } Property Value ChangeSet"
  },
  "api/EditSharp.History.IChange.html": {
    "href": "api/EditSharp.History.IChange.html",
    "title": "Interface IChange | EditSharp",
    "summary": "Interface IChange Namespace EditSharp.History Assembly EditSharp.dll One reversible step, recorded by the model at each write while a transaction is open. public interface IChange Properties Description What was written, for display and logs. string Description { get; } Property Value string Methods Redo() Writes the change again. void Redo() Undo() Puts back what was there before. void Undo()"
  },
  "api/EditSharp.History.Transaction.Scope.html": {
    "href": "api/EditSharp.History.Transaction.Scope.html",
    "title": "Class Transaction.Scope | EditSharp",
    "summary": "Class Transaction.Scope Namespace EditSharp.History Assembly EditSharp.dll One opening of a transaction; the entry is committed when the outermost scope is disposed. public sealed class Transaction.Scope : IDisposable Inheritance object Transaction.Scope Implements IDisposable Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Methods Commit() Marks this scope's work as finished, so disposing it keeps the writes. public void Commit() Dispose() Closes the scope. The outermost scope commits the entry, or rolls it back if any scope wasn't committed. public void Dispose()"
  },
  "api/EditSharp.History.Transaction.html": {
    "href": "api/EditSharp.History.Transaction.html",
    "title": "Class Transaction | EditSharp",
    "summary": "Class Transaction Namespace EditSharp.History Assembly EditSharp.dll Records the model's writes so a user action can be undone as one entry. public static class Transaction Inheritance object Transaction Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Remarks The model reports each primitive write as it happens (Set<TOwner, T>(TOwner, ref T, T, Action<TOwner, T>, string) for a property, Apply(Action, Action, string) for anything else). A user action opens a transaction with Begin(string), makes its edits and commits; the writes in between become one entry, and undo replays them backwards on the same objects. A write with no transaction open becomes an entry of its own in Active, or goes unrecorded when there is no active history. Writes made while building objects that aren't in the project yet go through Suppress(). Write from the main thread only. Properties IsOpen Whether a transaction is open. public static bool IsOpen { get; } Property Value bool IsReplaying Whether an undo or redo is being replayed right now. public static bool IsReplaying { get; } Property Value bool IsSuppressed Whether recording is switched off by Suppress(). public static bool IsSuppressed { get; } Property Value bool Methods Apply(Action, Action, string) Runs redo now and records undo as its reverse; for collection edits and anything else that isn't a property. public static void Apply(Action redo, Action undo, string description) Parameters redo Action Makes the change. undo Action Reverses it. description string What was changed, for logs. Begin(History, string) Opens a transaction, or joins the one already open. public static Transaction.Scope Begin(History history, string description) Parameters history History Where the entry is committed. description string The entry's name, shown for undo and redo. Returns Transaction.Scope The scope; call Commit() before disposing it to keep the writes. Remarks A nested call shares the outer entry and its description. Disposing any scope without committing it rolls back everything since the outermost call. Set<TOwner, T>(TOwner, ref T, T, Action<TOwner, T>, string) Writes a property's backing field and records how to put the old value back. public static void Set<TOwner, T>(TOwner owner, ref T field, T value, Action<TOwner, T> write, string name = \"\") where TOwner : class Parameters owner TOwner The object the property belongs to. field T The backing field. value T The new value. Nothing happens if it equals the current one. write Action<TOwner, T> Assigns the backing field on an owner. It must not call the property, so replaying can't record again. name string The property's name, filled in by the compiler. Type Parameters TOwner The object the property belongs to. T The property's type. Suppress() Stops recording until the returned object is disposed. public static IDisposable Suppress() Returns IDisposable Dispose it to resume recording. Suppressed<T>(Func<T>) Runs make with recording off; for factories and Duplicate. public static T Suppressed<T>(Func<T> make) Parameters make Func<T> Builds the object. Returns T What make returned. Type Parameters T What make builds."
  },
  "api/EditSharp.History.html": {
    "href": "api/EditSharp.History.html",
    "title": "Namespace EditSharp.History | EditSharp",
    "summary": "Namespace EditSharp.History Classes ChangeSet Everything one user action wrote, in order: one History entry. History A project's undo chain: one ChangeSet per user action, and a position in it. HistoryEventArgs Details of one Changed event. Transaction Records the model's writes so a user action can be undone as one entry. Transaction.Scope One opening of a transaction; the entry is committed when the outermost scope is disposed. Interfaces IChange One reversible step, recorded by the model at each write while a transaction is open. Enums HistoryAction What happened to a History, reported by Changed."
  },
  "api/EditSharp.IEditSharpLogger.html": {
    "href": "api/EditSharp.IEditSharpLogger.html",
    "title": "Interface IEditSharpLogger | EditSharp",
    "summary": "Interface IEditSharpLogger Namespace EditSharp Assembly EditSharp.dll Minimal logging seam so EditSharp doesn't assume Console, a specific logging framework, or any other ambient logger exists in the host process. Adapt to Microsoft.Extensions.Logging, Serilog, etc. with a one-line wrapper — e.g. msg => _logger.LogInformation(msg). public interface IEditSharpLogger Methods Log(string) void Log(string message) Parameters message string LogError(string) void LogError(string message) Parameters message string LogVerbose(string) void LogVerbose(string message) Parameters message string LogWarning(string) void LogWarning(string message) Parameters message string"
  },
  "api/EditSharp.Playback.AudioSampleEventArgs.html": {
    "href": "api/EditSharp.Playback.AudioSampleEventArgs.html",
    "title": "Class AudioSampleEventArgs | EditSharp",
    "summary": "Class AudioSampleEventArgs Namespace EditSharp.Playback Assembly EditSharp.dll One chunk (~100ms) of mixed timeline audio, raw s16le interleaved PCM at SampleRate/ChannelCount. Buffer OWNERSHIP: same contract as VideoFrameEventArgs — Buffer is reused by PlaybackAudioEngine on the very next chunk, so a subscriber that needs the samples past the handler returning (queuing onto an audio device buffer on another thread, etc.) MUST copy out of Buffer[..Length] synchronously. public sealed class AudioSampleEventArgs : EventArgs Inheritance object EventArgs AudioSampleEventArgs Inherited Members EventArgs.Empty object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors AudioSampleEventArgs(byte[], int, int, int, TimeSpan) public AudioSampleEventArgs(byte[] buffer, int length, int sampleRate, int channelCount, TimeSpan position) Parameters buffer byte[] length int sampleRate int channelCount int position TimeSpan Properties Buffer public byte[] Buffer { get; } Property Value byte[] ChannelCount public int ChannelCount { get; } Property Value int Length public int Length { get; } Property Value int Position public TimeSpan Position { get; } Property Value TimeSpan SampleRate public int SampleRate { get; } Property Value int"
  },
  "api/EditSharp.Playback.ClipFrame.html": {
    "href": "api/EditSharp.Playback.ClipFrame.html",
    "title": "Class ClipFrame | EditSharp",
    "summary": "Class ClipFrame Namespace EditSharp.Playback Assembly EditSharp.dll Audio-based playback for timelines. Built directly on top of Renderer's Skia compositor primitives (ContentPreparation, ClipContentSource, FrameCompositor, GpuContext, SurfacePool) rather than re-deriving them. \"CLIPS ARE GRAPHS\" REWRITE: ContentPreparation's nativeSizes/decodePlans/decodeSourcePaths dictionaries are now keyed by InputNode Id (Guid), not by Clip — a VideoClip's graph can contain more than one VideoSourceNode; staticImagePaths is gone entirely (text rasterization moved into ClipContentSource itself). The two pattern-match sites that used to match VideoClip { Source.Type: SourceType.Video } directly (AllVideoClipsRedirectedToCache, ComputeSeekOffsets) now walk each VideoClip's own graph for its Video-type VideoSourceNode(s) instead, since Source no longer lives directly on VideoClip. FrameStateResolver.Resolve no longer takes a nativeSizes parameter — see its own remarks. REWRITE (\"channels split by kind\"): AllVideoClipsRedirectedToCache and ComputeSeekOffsets walk timeline.VideoChannels directly now, rather than timeline.Channels filtered by is not VideoClip — Timeline keeps VideoChannel and AudioChannel as two separate lists (see Timeline.cs's own remarks). The two SurfacePool seedCount call sites below (VideoLoopAsync, ReverseVideoLoopAsync) now seed off Timeline.VideoChannels.Count specifically rather than the old mixed Timeline.Channels.Count — an AudioChannel never needs a GPU-backed canvas surface, so counting it toward the warm-start heuristic never bought anything (see SurfacePool's own remarks: this is a warm-start guess, not a hard cap, so this is a correctness/clarity fix, not a behavior-changing one). CONFIG: Timeline + RenderSettings, not a Blueprint — an end consumer shouldn't have to construct a full render blueprint (with an unused OutputDirectory) just to play a timeline back. RenderSettings is shared with Renderer so the two don't carry duplicate, potentially drifting copies of resolution/framerate/hardware-accelerator config. Position IS DELIBERATELY READ-ONLY FROM OUTSIDE. It exists for a consumer to ask \"where is playback actually at right now,\" not as an input — a public setter would mean the video loop has to keep re-checking whether something external changed it out from under a running session. Starting from a nonzero position is a PARAMETER TO Play(), not a pre-set on Position — Seek's use case is fully covered by Play(TimeSpan?) and was removed as a separate method deliberately, not by accident. Backed by PlaybackReferenceClock while a session is active, and a locally-held last-known value otherwise — see the property itself. ScrubToAsync also writes into the SAME PlaybackReferenceClock when a session exists (paused included), not only the locally-held fallback — see SCRUBBING WHILE PAUSED UPDATES Position below. A scrub taken while paused is no longer just a preview, either — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below for how a subsequent Play() actually resumes from it now. AND a scrub call sets Position FIRST, before it does anything else at all — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY below. SYNCHRONIZED STARTUP (PlaybackStartGate): the video loop and the audio engine each need real setup time before either can start pacing itself, and that setup is asymmetric (video's is heavier — see PlaybackStartGate's own remarks). Both loops finish their own setup, signal the gate, and only THEN start pacing. AUDIO DELIVERY IS \"PLAY THIS NOW,\" DELIBERATELY NOT PRE-BUFFERED. PLAYBACKMODE / LEADER-FOLLOWER (PlaybackReferenceClock): exactly one of video/audio is the LEADER for a given session — it paces itself on its own real Stopwatch and reports its delivered position into a shared PlaybackReferenceClock. The other stream (if any) is the FOLLOWER — instead of its own Stopwatch, it polls the reference clock and only delivers once the leader has actually reached that content position. Reverse playback never has a follower — audio never participates in a reverse session (see REVERSE PLAYBACK below), so ReverseVideoLoopAsync is always the sole leader of its own session. PAUSE VS STOP (PlaybackPauseGate): genuinely different operations. A paused session's GpuContext/SurfacePool/decoders stay fully alive (see EAGER RELEASE ON PAUSE, REVERTED below) — pausing is meant to be, and is, an essentially free, instantly-resumable operation UNLESS a scrub actually moved the position during that pause — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below. STATE (PlaybackState) — REPLACED THE EARLIER IsPlaying/IsPaused BOOLEAN PAIR (decided in conversation): this class used to expose two independently-readable booleans (IsPlaying, and IsPaused derived from _pauseGate?.IsPaused), which meant a caller wanting \"is this actually actively playing right now\" had to combine them itself (IsPlaying && !IsPaused) — and internally, this class had its own _isPlaying bool tracking \"a session exists\" completely separately from _pauseGate's own paused/not-paused bit, two pieces of state that could only ever legally combine into three real situations (no session; session, unpaused; session, paused) despite the boolean pair's own state space technically allowing four. A single PlaybackState State (see that enum's own remarks) replaces both: Inactive/Playing/Paused cover exactly those three real situations with no illegal combination possible, and Scrubbing is now a real, first-class fourth state (see SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG below) rather than something that used to happen ambiently underneath whatever IsPlaying/IsPaused said. _pauseGate itself is UNCHANGED and still exists — it remains the actual mechanism VideoLoopAsync/ReverseVideoLoopAsync poll to block producing new frames while paused; _state is the coarse, externally- visible reflection of what's currently true, kept in sync with every place this class used to flip _isPlaying or touch _pauseGate. SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG (part of the STATE change above): ScrubToAsync's own guard — previously _isPlaying && !IsPaused — is now simply State == PlaybackState.Playing (throws in that one case, exactly the same set of situations as before: scrubbing is allowed whenever no session exists at all, OR the existing session is paused). For the duration of composing and delivering ONE scrubbed-to frame, _state is set to PlaybackState.Scrubbing (saving whatever it was immediately before — Inactive or Paused — under _stateLock) and restored to that saved value once the render finishes, whether it succeeds or fails. Because actual scrub RENDERING is already serialized through _scrubGate (a plain SemaphoreSlim — see SCRUB COALESCING below), only one ScrubToAsync call is ever inside that bracket at a time, so this save/restore can never race a second concurrent one. SCRUB/REVERSE VIA RAW SCRUB PROXIES (ScrubProxyCache / ScrubProxyReader / ScrubFrameSource) — REWRITTEN IN CONVERSATION, REPLACING AN EARLIER ffmpeg-KEYFRAME-DECODE APPROACH ENTIRELY, after two rounds of real-world testing on real hardware showed that spawning ANY ffmpeg process per scrub tick — GPU-hwaccel or forced software — could not be made both fast and crash-safe under a fast scrub drag (see the two superseded rounds summarized below). Both ScrubToAsync and reverse playback (Speed < 0) share ONE mechanism, deliberately — both need \"show me approximately this arbitrary position, instantly, with zero per-tick decode.\" Every video source referenced by the Timeline gets a small, pre-built, RAW, decoder-less scrub proxy — a fixed-size-frame file a scrub/reverse tick reads directly (one positioned file read, no subprocess, no decode at all — see ScrubProxyFormat's own remarks for the file shape and why). The trade-off, named not hidden: what's delivered during scrub/reverse is accurate to the proxy's own fixed, LOW resolution and fixed SAMPLE RATE (EditSharpConfig.ScrubProxyTargetShortSide/ScrubProxySampleRate), not the clip's real decode resolution or the exact requested frame — normal for a scrub/rewind preview, wrong for a final render (this mechanism is never used by Render/* or by VideoLoopAsync's normal forward playback). For reverse playback specifically this means visual character is a smooth, evenly-paced backward step every 1/ScrubProxySampleRate seconds of content — a real improvement over the earlier keyframe-snapped \"fast rewind\" look, since a proxy's frame density is no longer tied to the source's own (often several- seconds-apart) keyframe spacing at all. WHY NO ffmpeg PROCESS EVER RUNS DURING A SCRUB/REVERSE TICK, AND WHY THAT MATTERS BEYOND SPEED: it also means the persistent GPU decoder backing REAL forward playback (SourceDecoder.Start/NextFrame, and its GpuContext) is never touched, paused, or contended for while a scrub/reverse session is active — it can stay warm and simply resume smoothly the moment scrubbing ends, since nothing about scrubbing ever shared a process, a decode session, or a GPU context with it in the first place. Scrubbing NEVER touches ffmpeg or any file ffmpeg has open — it only ever reads pre-built .esrp proxy files via ScrubProxyReader (System.IO.RandomAccess) — see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; this remains true after every fix below. TWO SUPERSEDED ROUNDS OF FIXES, KEPT HERE AS HISTORY SINCE THE LESSON EACH ONE TAUGHT SHAPED THIS DESIGN — the mechanism itself (an ffmpeg one-shot decode per tick) is gone, but the request-coalescing fix from round 1 is NOT superseded and is still exactly how ScrubToAsync behaves (see SCRUB COALESCING below): ROUND 1 (fixed, real bug): a fast scrub drag firing many ScrubToAsync calls used to serialize them FIFO — every call ran a real ffmpeg decode to completion for a frame nobody wanted by the time it finished. Worse, the one-time per-session setup used to be gated by each call's own cancellation token, so a request superseded mid-setup killed setup itself and the next request restarted it from scratch — under fast enough scrubbing setup could never finish at all. Fixed with \"latest request wins\" coalescing plus memoized setup — see SCRUB COALESCING below, which still applies unchanged to this rewrite. ROUND 2 (found after round 1, itself now superseded by this rewrite, not by a further tweak of the same mechanism): even with coalescing fixed, each SURVIVING scrub/reverse tick was still slow (\"wait a few seconds\") and a fast enough drag could still lock the whole process up irrecoverably. Root cause was GPU-hwaccel context/ session overhead paid per one-shot tick (and a real risk of exhausting the GPU's own concurrent decode-session budget under rapid concurrent one-shot GPU decodes — a driver-level hang outside any cancellation/process-kill this process could reach). The fix tried was forcing SOFTWARE decode for every tick instead — which, per direct real-hardware feedback, was NOT the right fix: CPU decode of the clip's real native resolution is orders of magnitude slower than GPU decode (established earlier, during this project's own GPU migration work), so forcing it per-tick just traded one flavor of slow/unstable for another (this time crashing outright under load, rather than merely locking up). The actual fix wasn't \"which decode backend runs per tick\" at all — it was removing the per-tick decode requirement entirely, which is this rewrite. SCRUB COALESCING (\"LATEST REQUEST WINS\") — STILL IN EFFECT, UNCHANGED BY THE PROXY REWRITE: a fast scrub drag firing many ScrubToAsync calls still coalesces to only the latest one actually completing — even a proxy read is not literally free (a file read plus a full frame composite), and there is no reason to do that work for a position that's already stale by the time it would finish. ScrubToAsync cancels any still-in-flight/queued PREVIOUS scrub request the moment a new one arrives (_scrubSupersedeCts), linked with the caller's own ct — a superseded request's OperationCanceledException is swallowed (not the caller's own cancellation, so nothing to propagate). The one-time scrub-session setup — its own GPU context/pool (persistent, see SCRUB GPU CONTEXT IS NOW PERSISTENT below) plus a fresh ScrubFrameSource — is MEMOIZED (_scrubSetupTask, mirroring ScrubProxyCache's own cached-Task build coalescing) rather than restarted inside every call. Once started it always runs to completion regardless of which caller kicked it off or whether that caller is later superseded; every call just awaits (cancellably, via WaitAsync) whatever the current attempt is — the same \"cancel the wait, not the shared work\" split ScrubProxyCache.GetOrBuildAsync itself already uses. Individual SOURCE proxy builds are no longer part of what this setup waits ON at all — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP below. PrewarmScrubProxiesAsync — the opt-in \"generate proxies beforehand\" entry point: builds every video source's scrub proxy ahead of need (e.g. right after a project loads), so the FIRST scrub/reverse session doesn't pay any build cost at all. Entirely optional — the first scrub/reverse session builds whatever's still missing on demand either way (see EnsureScrubSessionBaseAsync). UNLIKE the session-startup path below, THIS entry point is still fully awaited/blocking for its own caller — that's the point of calling it explicitly ahead of time. SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP (fixed here, real-world regression found in testing): a scrub/reverse session's own setup used to AWAIT every referenced source's scrub proxy build INLINE (the old PrepareScrubProxiesAsync -> Task.WhenAll -> per-source ScrubProxyCache.GetOrBuildAsync) before EnsureScrubSessionBaseAsync (and therefore ScrubToAsync/ReverseVideoLoopAsync's own startup) would ever complete — for a source with no cached proxy yet, that's a real, potentially multi-second decode+encode pass (see ScrubProxyCache.BuildAsync), and testing found it visibly HUNG THE CALLING THREAD for that whole duration, not merely responded slowly. NEW EVIDENCE surfaced by that same test run, worth recording since it contradicts an earlier conclusion in this file: TWO DEAD-END HYPOTHESES below, point (a), concluded Godot's C# integration installs NO SynchronizationContext at all. A production crash log captured DURING THIS ROUND showed Godot.GodotSynchronizationContext / Godot.GodotTaskScheduler (ExecutePendingContinuations/ Activate, both driven from the engine's own per-frame ScriptManagerBridge.FrameCallback) directly in the call stack — meaning that earlier conclusion was WRONG (or true only of an older Godot version): Godot DOES capture and use its own SynchronizationContext, and it resumes await continuations from the MAIN THREAD's own per-frame callback. That reconciles this hang exactly: every await in the old PrepareScrubProxiesAsync chain (this file, ScrubProxyCache, MediaProbe, FfmpegRunner — none of it ConfigureAwait(false), see (a) below) posted its continuation back through that captured context, so a CPU-heavy decode+encode pass ended up executing in chunks ON THE MAIN THREAD itself, between/ within frame callbacks — visually indistinguishable from a genuine hang for as long as it took. FIX: proxy resolution no longer runs inline as part of session setup at all — KickOffScrubProxyResolution/KickOffProxyResolution fire each referenced source's resolution via Task.Run(...), which unconditionally schedules onto the real ThreadPool regardless of any captured SynchronizationContext, and BuildScrubSessionAsync/ ReverseVideoLoopAsync no longer await them at all — a scrub/reverse session is considered \"ready\" (EnsureScrubSessionBaseAsync returns, ReverseVideoLoopAsync proceeds) the moment its OWN GPU context exists, regardless of whether every referenced source's proxy has actually finished building yet. See MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA below for what fills the gap in the meantime. ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE (fixed here, real-world regression found in testing directly after the fix above: \"it attempted to build 7 identical files\"): once proxy resolution moved onto a fire-and-forget Task.Run per VideoSourceNode (see directly above), a Timeline that references the SAME underlying source path from several distinct VideoSourceNode instances (the same footage cut into multiple clips, or reused across channels) fired one INDEPENDENT Task.Run per NODE, all racing to build that one source's proxy at once. ScrubProxyCache.GetOrBuildAsync's own in-flight-build coalescing (InFlight.GetOrAdd, keyed by content hash) looks like it should have caught this, but each caller must first await MediaHasher.ComputeAsync(...) (real, non-instant I/O) BEFORE it ever reaches that GetOrAdd call — several callers hashing the same file at once reliably finish within the same small window of each other, and .NET's ConcurrentDictionary.GetOrAdd does NOT guarantee its valueFactory runs only once under that kind of near-simultaneous contention: multiple racing calls can each invoke BuildAndTrackAsync (a real async method that starts running the instant it's invoked, not merely queued), with only one of the resulting Tasks actually kept in the dictionary — the others run to completion anyway, each against its own temp file, each ending in its own File.Move(tempPath, finalPath, overwrite: true) racing the others for the same final path. That is exactly \"N identical builds\" for one source, for N nodes that reference it. FIX, at the call site (the actual root cause — see also the cache-level hardening immediately below): KickOffScrubProxyResolution now groups every referenced VideoSourceNode by its own Source.Path FIRST, and KickOffProxyResolution fires exactly ONE Task.Run per DISTINCT path — its result is then written into proxies for every node Id that shares that path, once the one build/lookup completes. A Timeline with 7 clips cut from the same source file now resolves that source exactly once, not 7 times. ScrubProxyCache.GetOrBuildAsync ALSO HARDENED AGAINST THE SAME RACE, AS DEFENSE-IN-DEPTH (paired with the fix directly above): the call-site dedup above is the actual fix for THIS reported bug (one Timeline, resolved through Playback), but ScrubProxyCache is a process-wide static cache other call sites can reach too (e.g. PrewarmScrubProxiesAsync running concurrently with a live scrub session's own on-demand resolution for the same source) — so the underlying GetOrAdd race is fixed at the cache itself too, not just papered over here. See ScrubProxyCache's own remarks for the actual mechanism (wrapping the in-flight Task in a Lazy so only the winning caller's factory ever actually executes). MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA (fixed here, paired with the fix directly above — direct user request: \"the placeholder should just be used anytime any media comes up empty\"): ScrubFrameSource now falls back to MediaPlaceholder.Get(...) — a small, cached, drawn-not-decoded \"MEDIA OFFLINE\" image — for ANY input node it can't currently resolve real content for: a video proxy that's still building in the background (see directly above), one whose build failed outright, a missing/corrupt static image, or a text node that fails to rasterize. See ScrubFrameSource's own class remarks, MEDIA-OFFLINE PLACEHOLDER, for the full per-case behavior. DELIBERATELY SCOPED TO SCRUB/REVERSE ONLY, NOT ClipContentSource, per direct user decision — forward Playback and Render/* keep throwing on broken/missing media; silently masking that in a real export or live playback could hide a real problem, whereas scrub/reverse is already an approximate preview by nature (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above). ScrubProxyReady EVENT (new): once a background-kicked-off proxy build completes successfully, proxies (the SAME mutable dictionary ScrubFrameSource reads from) gains that source's entry, but nothing automatically re-renders the CURRENT scrub/reverse position with it — the next tick picks it up naturally (ScrubFrameSource reads proxies fresh every call), but until then whatever's already on screen (a placeholder, or a still-missing frame) keeps showing. ScrubProxyReady fires (from whatever background thread the build completed on — same \"consumer marshals it themselves\" convention as every other event on this class) so a consumer that wants to eagerly replace a placeholder can re-issue ScrubToAsync(Position) itself once notified; entirely optional, nothing internally depends on anyone handling it. REVERSE PLAYBACK (Speed < 0): Play() branches to ReverseVideoLoopAsync instead of VideoLoopAsync. Audio never participates (audioParticipates requires Speed == 1, which negative Speed never satisfies) — reverse is video-only for now; revisit once arbitrary-speed forward audio exists and there's a real reversed-PCM delivery path to build on. Speed == 0 remains unsupported (that's Pause()/Stop(), not a playback rate) — only that one case still throws from Play(); any negative Speed is now a normal input. OPTIMIZED-MEDIA CACHE (OptimizedMediaCache, EditSharp.Caching): a video clip's decoder in the FORWARD playback path may open against a persistent, content-addressed proxy instead of the clip's true original source file, whenever ContentPreparation. ProbeVideoAsync finds one already built and big enough. Scrubbing and reverse playback consult a COMPLETELY SEPARATE cache (ScrubProxyCache) instead — see the section above. GATE-HELD AWAITS NEVER RESUME ON THE CALLER'S CONTEXT (fixed here): every await made while _scrubGate is held (ScrubToAsync, RenderClipFrameAsync, and BuildScrubSessionAsync, which runs under the gate via _scrubSetupTask) uses ConfigureAwait(false). EndScrubbing() — and therefore Play() — blocks SYNCHRONOUSLY on _scrubGate, and the Godot host DOES install a SynchronizationContext on its main thread. Found in the field: resuming playback right after a quick scrub (clicking the timeline ruler repeatedly while playing) called Play() on the main thread while a fire-and-forget ScrubToAsync still held the gate with its next continuation posted back to that same main thread — neither could proceed, and the app hard-froze. With the gate-held continuations free of the caller's context, the gate always releases on its own. Consequence: a scrub's VideoFrame is raised from a background thread, exactly like forward playback's already are. SUPERSEDED SESSIONS ARE ABANDONED, NOT AWAITED (fixed here): Stop() used to block its caller on the session's video task, and a session's setup (content probing, decoder spawn, GPU context, warm-up render) never checked its token until the start gate — so every Play() waited out the ENTIRE setup of the session before it. Found in the field: clicking the timeline repeatedly during playback resumes playback on every click, and each resume queued seconds of setup behind the last on Godot's main thread. Now Stop() only cancels; each new session awaits the previous one's task on the thread pool (WaitForRetiredSessionAsync), so sessions still never hold GPU contexts/decoders at the same time, and a session superseded while it waits never starts setup at all. Setup checks its token between steps, so a cancelled session exits at the next step instead of finishing. A retired session's exit paths (the loops' final state reset, TearDownAfterNaturalEnd) only touch state while its token is still the installed _cts (IsCurrentSession), so it can never clobber the session that replaced it. Dispose() is the one place that still waits synchronously. The audio engine's start (the whole-timeline master mix) also runs via Task.Run now, off the caller's thread and SynchronizationContext. SWITCHING BETWEEN SCRUB AND PLAYBACK (fixed, KEPT): Play() calls EndScrubbing() as its very first action, unconditionally, on BOTH the \"resume an existing paused session\" path and the \"start a brand-new session\" path, BEFORE taking _stateLock. Found in the field: scrubbing is explicitly allowed while playback is merely paused (ScrubToAsync's own guard is State != PlaybackState.Playing, not State == PlaybackState.Inactive — see STATE above) — Play() itself used to never tear an active scrub session down before (re)starting its own forward-playback session. Runs OUTSIDE _stateLock deliberately: EndScrubbing() blocks synchronously on _scrubGate, and ScrubToAsync can be holding _scrubGate while briefly needing _stateLock itself — calling EndScrubbing() while already holding _stateLock would risk a lock-order inversion deadlock. NOTE this fix now ONLY tears down _scrubContentSource (see SCRUB GPU CONTEXT IS NOW PERSISTENT below) — it no longer touches the scrub GpuContext at all, since that is now a persistent, once-created resource for this Playback instance's whole life. EAGER RELEASE ON PAUSE, REVERTED — TRIED, THEN EXPLICITLY UNDONE PER USER DIRECTION: a real GPU-contention concern was identified where scrubbing while paused stood up a scrub session's own GpuContext alongside a paused forward-playback session's still-alive one (two live GPU contexts at once). A first fix had each loop eagerly dispose its own GpuContext/SurfacePool/decoders the moment it noticed pauseGate.IsPaused, reacquiring only once actually resumed. That did NOT resolve the freezes still being reported, and it cost real playback smoothness — resuming from a pause stopped being free (it paid roughly a fresh Play()-setup cost every time), which the user explicitly called out as not worth it. REVERTED per direct instruction: VideoLoopAsync/ReverseVideoLoopAsync are back to a single GpuContext/SurfacePool/decoder set for the WHOLE forward- playback or reverse session, created once and torn down only at Stop()/natural end — pausing is, again, a cheap, instantly-resumable no-op for these resources UNLESS a scrub actually moved the position during the pause (see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below — that is a NEW, separate, narrowly-scoped mechanism, not a reintroduction of this reverted one: it only pays a restart cost when the position genuinely changed, never on every pause). SourceDecoder.Dispose()'s own WaitForExit-after-Kill fix (see that class's own remarks) is UNRELATED and STAYS — it's a real correctness fix regardless of when/whether a decoder gets disposed. TWO DEAD-END HYPOTHESES, TRIED AND EITHER REVERTED OR KEPT ON THEIR OWN MERITS ONLY — NEITHER WAS THE ACTUAL SCRUB LOCKUP: chasing the same real, reproducible scrub freeze reported repeatedly on real hardware, two further fixes were tried and BOTH were confirmed, on real hardware, to change nothing about the freeze: (a) Every await in the scrub-reachable async chain (this file, ScrubProxyCache, MediaHasher, MediaProbe, FfmpegRunner) was given ConfigureAwait(false), on the theory that a caller blocking synchronously on ScrubToAsync's Task (e.g. .Wait()/.GetAwaiter().GetResult() from a UI thread) could deadlock against a captured SynchronizationContext. REVERTED — the real consumer app (a Godot C# game) calls ScrubToAsync fire-and-forget from a slider's ValueChanged signal, never blocking on it, and Godot's C# integration was believed at the time to install NO SynchronizationContext at all — so this theory was believed not to apply here, and the fix was treated as a no-op for this app's real behavior. Removed for cleanliness rather than left in as dead weight. SEE SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP above — later evidence shows the \"no SynchronizationContext at all\" premise here was actually WRONG; this revert's conclusion about ITS OWN theorized deadlock risk is unaffected (a plain .Wait()-on-UI-thread deadlock is a different mechanism than the main-thread-hang bug that new evidence explains), but the \"this app has no SynchronizationContext, full stop\" framing above it was never actually true and should not be trusted at face value by a future investigator. (b) GpuContext.Dispose() was changed to wait for the GPU to idle (GRContext.Flush() + Submit(syncCpu: true)) before releasing the D3D12 device/queue/adapter/factory, on the theory that recreating a device shortly after disposing the previous one could race that previous device's own driver-side teardown. KEPT — it is a real, independent, low-risk correctness improvement (see GpuContext's own remarks) — but real-hardware testing showed the reported freeze was completely UNCHANGED by it, so it is not credited as fixing this bug. The real cause turned out to be a third, different thing — see GPU WORK MUST STAY ON ONE THREAD below. GPU WORK MUST STAY ON ONE THREAD — THE ACTUAL ROOT CAUSE OF THE EARLIER SCRUB LOCKUP (unrelated to the main-thread-hang fixed above, which is a separate bug with a separate mechanism — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD above): regardless of whether Godot captures a SynchronizationContext (see (a) above for why that question turned out to matter, just not for THIS bug), EVERY await anywhere in this file could already land its continuation on a DIFFERENT OS thread than the call immediately before or after it — whether that continuation is scheduled by a captured SynchronizationContext's own queue or by the plain ThreadPool, the specific thread it actually runs on from one await to the next is not guaranteed to be the same one. That means every GPU-touching call this file makes (GpuContext.Create, SurfacePool's Rent/CreateSurface, FrameCompositor.RenderFrame, GpuContext.Dispose) could land on a different thread than the call before/after it, purely as an artifact of scheduling — true from the very first version of the scrub rewrite, independent of every fix tried above. Skia's GrDirectContext (GRContext in SkiaSharp) is not documented as safe for that usage pattern: sequential-but-cross-thread access to one GRContext, its SKSurfaces, and the D3D12 command queue backing it, with nothing pinning it to one thread, is a real, plausible source of a driver-level hang — more likely to actually manifest the more real GPU work a given call submits, which lines up with the reported pattern (a cheap/short first scrub tends to survive; a slower/longer one is more likely to get unlucky; once ANY frame has succeeded, later calls in the same process keep landing on threads already warmed up for this workload, which is why it then \"stays fixed\"). FIX: GpuThreadDispatcher (Composite/GpuThreadDispatcher.cs) confines every GPU-touching call for a given GpuContext/SurfacePool pair to ONE dedicated background thread, for as long as that pair is alive — used for the scrub GPU context/pool (_scrubGpuThread, persistent — see SCRUB GPU CONTEXT IS NOW PERSISTENT below) and for a session-scoped dispatcher inside both VideoLoopAsync and ReverseVideoLoopAsync (created and disposed alongside that session's own GpuContext/SurfacePool). Every PrefetchAsync/RenderFrame/ Create/Dispose call for a given context now happens on that context's own single thread, every time, structurally — not by hoping the scheduler behaves favorably. CONFIRMED ON REAL HARDWARE — the user reported the reported freeze/lockup is completely gone after this fix. SCRUB GPU CONTEXT IS NOW PERSISTENT, PER EXPLICIT USER REQUEST: _scrubGpuContext/_scrubSurfacePool/_scrubGpuThread are created AT MOST ONCE per Playback instance (lazily, on first scrub/reverse session) and reused by every scrub session after that — EndScrubbing no longer disposes them at all; only Playback.Dispose() does, once, for real. This has two independent benefits: it directly avoids ever repeatedly creating and destroying a D3D12 device for scrubbing at all (so the earlier dispose-then-recreate race window this investigation chased in (b) above simply cannot occur for scrubbing any more, regardless of whether it was ever real), and it means a scrub session's own setup (EnsureScrubSessionBaseAsync/ BuildScrubSessionAsync) only needs to rebuild _scrubContentSource (proxy readers/static images/nested renderers — plain file handles, no GPU) on each EndScrubbing()/rebuild cycle, which is both cheaper and has no GPU-lifetime implications at all. ASSUMES RenderSettings. Resolution/Framerate stay constant for this Playback instance's whole life — true for how this class is actually constructed today (see EditSharp's own usage), but flagged here since a persisted SurfacePool sized for the FIRST scrub session's width/height would silently be wrong for a later session at a different resolution; revisit if RenderSettings ever needs to change on a live instance. SCRUBBING WHILE PAUSED UPDATES Position: ScrubToAsync reports the scrubbed-to position into the SAME PlaybackReferenceClock a live/paused session uses (_referenceClock?.Report(position), in addition to _lastKnownPosition) — previously it only wrote _lastKnownPosition, which the public Position getter never actually reads while _referenceClock is non-null (i.e. for the entire lifetime of a session, paused included). ALSO bumps _scrubGeneration under the same lock — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME immediately below, which is what actually makes a paused scrub \"count\" the next time Play() is called. SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME (root cause of two reported bugs, both fixed): previously, a scrub taken while paused updated _referenceClock/Position (see directly above) but NOTHING ELSE — the video loop's persistent decode pipe (ClipContentSource/ SourceDecoder, which can only move FORWARD) and the audio engine's own byte-offset pump both kept whatever position they were at before the pause, completely unaware a scrub had ever happened. A plain Play() resume just released the pause gate and resumed the wall clock in place, so BOTH streams simply continued from the PRE-scrub position — the scrub was a preview only, never an actual seek (bug 1: \"scrubbing should change playback position, but it doesn't\"). Worse, the stale _referenceClock value left behind by the scrub caused a SEPARATE, visible symptom for a follower stream: PlaybackReferenceClock.Position resumes extrapolating forward, in real time, from wherever it was last Report()'d — if a scrub landed EARLIER than the actual pre-pause stopped position, that value starts BELOW the follower's own fixed target position, so the follower's gap = target - referenceClock.Position computes a large POSITIVE gap and sleeps (in one big Task.Delay, not a poll loop — see VideoLoopAsync's/PlaybackAudioEngine.PumpAsync's own wait logic) for approximately the real-time distance between the scrub position and the actual resume position, before the leader's own next report ever gets a chance to correct it — a real, reproducible multi-second stall between the leader (audio, in the default SyncToAudio mode) resuming essentially immediately and the follower (video) resuming only after that stale gap has fully elapsed (bug 2: \"significant delay between audio starting back up and video starting back up ... ending a scrub at an earlier position [than where playback stopped]\"). A scrub to a LATER position than the stopped position produces a negative/zero gap instead, so the follower never waits — which is exactly why the user observed the delay ONLY for the \"earlier\" case and an instant (but still wrong-position, per bug 1) resume for the \"later\" case: both symptoms trace to this one gap. FIX: _scrubGeneration (bumped by ScrubToAsync under _stateLock) and _scrubGenerationAtPause (a snapshot of _scrubGeneration taken by Pause()) together detect \"did an actual scrub happen since this pause started\" — a plain int-equality check, immune to any timing race with the leader's own periodic Report() calls (which never touch _scrubGeneration). Play()'s resume branch (session already active and startPosition == null) now checks this first: NO scrub happened (generations match): unchanged, cheap, instant in-place resume — _pauseGate?.Resume() + _referenceClock?.ResumeWallClock() + _state = PlaybackState.Playing, exactly as before this fix. A scrub DID happen (generations differ): the persistent decode pipe genuinely cannot jump to the new position on its own, so this falls through to the SAME stop+restart path a fresh Play(startPosition) call already uses — startPosition is set to _referenceClock.Position (the scrubbed-to position, frozen since the wall clock never resumed) and the existing Stop() + rebuild-from-scratch machinery below takes it from there: a brand-new PlaybackReferenceClock initialized directly to that position, a brand-new audio pump starting its byte offset from it, and a brand-new video/reverse loop with decoders seeked via ComputeSeekOffsets(position) — the exact same well-tested startup path the very first Play() call already goes through, so there is no stale-clock/gap artifact left to produce bug 2 either. Named trade-off: resuming after an actual scrub-during- pause now costs roughly a fresh Play() setup (new decoders, new GpuContext-dispatcher work) — but ONLY when the position genuinely changed. A plain pause/resume with no scrub in between is completely unaffected and stays exactly as cheap as it always was; this is a narrowly-scoped fix, not a reintroduction of EAGER RELEASE ON PAUSE, REVERTED above. SCRUB SETS POSITION FIRST, UNCONDITIONALLY (fixed here, a SEPARATE bug from SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above): ScrubToAsync used to update _lastKnownPosition/_referenceClock/ _scrubGeneration only AFTER a frame had successfully rendered and been delivered — meaning a scrub that failed to build/render for any reason (a proxy build error, a superseded/cancelled render, or even just calling ScrubToAsync before Play() has EVER been called once) left Position completely unmoved, silently. Position now updates as the FIRST thing ScrubToAsync does, right after validating the requested position is in range and BEFORE any of the session-setup/ rendering machinery runs — a caller's scrub request is reflected in Position immediately and unconditionally, whether or not a visible frame ever actually renders for it. This also means Position now moves even on a session that has never played once — previously Position could only ever be driven by a successful render, which (see the gap below) tended to silently fail on a first-ever scrub against an empty proxy cache in some configurations. SCRUB FAILURES ARE NOW LOGGED, NOT SILENT (fixed here): ScrubToAsync is, in practice, a FIRE-AND-FORGET API for most real callers (the Godot consumer app calls it from a Slider.ValueChanged handler with no await at all) — before this fix, the only exception type this method ever caught was a superseded-request OperationCanceledException; any OTHER exception (a scrub-proxy build failure, a corrupt/missing source, anything thrown out of BuildScrubSessionAsync or the actual composite) propagated straight out of the async method as a faulted Task that a fire-and-forget caller never observes — completely silent, no log line, no visible symptom beyond \"nothing rendered.\" This is very likely what was actually happening in the reported \"an empty scrub proxy cache builds the containing folder but never the actual .esrp file, and scrubbing shows no video, with no error displayed\" bug — BuildAsync creates the target directory before it ever calls EncodeAsync, so a failure partway through encoding leaves exactly that signature (folder exists, temp file cleaned up by BuildAsync's own catch, real exception never seen by anyone). FIX: ScrubToAsync now has a catch-all for any exception that ISN'T the caller's own cancellation, and logs it via EditSharpConfig.Logger.LogError with the full exception detail before swallowing it (deliberately not rethrown — there is no synchronous caller left by the time this runs to catch it, and an unobserved faulted Task's finalization can itself crash a process via TaskScheduler.UnobservedTaskException in some hosts, which swallowing here avoids). Position has already been set (see SCRUB SETS POSITION FIRST, UNCONDITIONALLY above) regardless of whether this catch ever fires, so a logged-and-swallowed failure here only means no new frame rendered for that request, not that Position silently failed to move too. KNOWN GAPS — tracked, not hidden: REVERSE AUDIO is not implemented — reverse is video-only, see REVERSE PLAYBACK above. Video itself IS supported. ARBITRARY SPEED (audio tracking Speed != 1, Speed > 0) remains a MUST-HAVE, not deferred-maybe. Seeking to a nonzero start position may need the audio composition itself to carry a seek offset. TRUE FRAME-SKIPPING remains open. PlaybackMode WIRING — CLOSED (SyncToAudio/EveryFrame both real). SCRUBBING — mechanism CLOSED (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; SupportsScrubbing is always true now). The real, repeatedly-reproduced lockup is CONFIRMED FIXED (by the user, on real hardware) via GPU WORK MUST STAY ON ONE THREAD above (GpuThreadDispatcher + a persistent scrub GpuContext). Two earlier fixes in this same investigation (ConfigureAwait(false) throughout the scrub async chain; a GPU-idle wait in GpuContext.Dispose()) were both confirmed on real hardware NOT to change this freeze — the first was reverted as unnecessary, the second was kept on its own independent merits only (see TWO DEAD-END HYPOTHESES above). CLOSED — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above. Resuming playback after scrubbing while paused now actually resumes from the scrubbed position, and the A/V startup-delay artifact that came along with the old bug is gone too. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY and SCRUB FAILURES ARE NOW LOGGED, NOT SILENT above. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP and MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA above. A scrub/reverse session no longer blocks the calling thread while a missing proxy builds; ScrubFrameSource shows the offline placeholder for whatever isn't ready yet. REGRESSION FOUND ON REAL HARDWARE (\"attempted to build 7 identical files\" / \"No scrub proxy registered\" errors) AND FIXED — see ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE above (the duplicate-build race) and ScrubFrameSource's own MEDIA-OFFLINE PLACEHOLDER remarks (the \"No scrub proxy registered\" throw is gone — that path now falls back to the placeholder instead, which is what should have shipped with this gap's original fix but did not actually make it into the pushed file the first time). NOT YET CONFIRMED ON REAL HARDWARE. GPU DECODE FOR IndexedDelta7 — TRIED, THEN REMOVED PER EXPLICIT USER CORRECTION: an earlier round of GPU work wired a ScrubProxyGpuDecoder into every ScrubFrameSource this file constructs (BuildScrubSessionAsync's persistent scrub session and ReverseVideoLoopAsync's session-scoped one), attempting a GPU decode of each IndexedDelta7 proxy frame before falling back to IndexedDelta7Codec.Decode. The user clarified that request was actually about the BUILD/ENCODE side, not decode — a proxy read is already a cheap, single positioned file read regardless of pixel format, so GPU-accelerating it bought little, while the ENCODE side (see ScrubProxyCache/ ScrubProxyGpuEncoder) is the genuinely expensive, worth- accelerating step. The decoder and its wiring here were removed entirely as unneeded complexity; every scrub-proxy frame this file resolves is read on the CPU now, exactly as it was before that detour — see ScrubFrameSource's own remarks. public sealed record ClipFrame : IEquatable<ClipFrame> Inheritance object ClipFrame Implements IEquatable<ClipFrame> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors ClipFrame(byte[], int, int, bool) Audio-based playback for timelines. Built directly on top of Renderer's Skia compositor primitives (ContentPreparation, ClipContentSource, FrameCompositor, GpuContext, SurfacePool) rather than re-deriving them. \"CLIPS ARE GRAPHS\" REWRITE: ContentPreparation's nativeSizes/decodePlans/decodeSourcePaths dictionaries are now keyed by InputNode Id (Guid), not by Clip — a VideoClip's graph can contain more than one VideoSourceNode; staticImagePaths is gone entirely (text rasterization moved into ClipContentSource itself). The two pattern-match sites that used to match VideoClip { Source.Type: SourceType.Video } directly (AllVideoClipsRedirectedToCache, ComputeSeekOffsets) now walk each VideoClip's own graph for its Video-type VideoSourceNode(s) instead, since Source no longer lives directly on VideoClip. FrameStateResolver.Resolve no longer takes a nativeSizes parameter — see its own remarks. REWRITE (\"channels split by kind\"): AllVideoClipsRedirectedToCache and ComputeSeekOffsets walk timeline.VideoChannels directly now, rather than timeline.Channels filtered by is not VideoClip — Timeline keeps VideoChannel and AudioChannel as two separate lists (see Timeline.cs's own remarks). The two SurfacePool seedCount call sites below (VideoLoopAsync, ReverseVideoLoopAsync) now seed off Timeline.VideoChannels.Count specifically rather than the old mixed Timeline.Channels.Count — an AudioChannel never needs a GPU-backed canvas surface, so counting it toward the warm-start heuristic never bought anything (see SurfacePool's own remarks: this is a warm-start guess, not a hard cap, so this is a correctness/clarity fix, not a behavior-changing one). CONFIG: Timeline + RenderSettings, not a Blueprint — an end consumer shouldn't have to construct a full render blueprint (with an unused OutputDirectory) just to play a timeline back. RenderSettings is shared with Renderer so the two don't carry duplicate, potentially drifting copies of resolution/framerate/hardware-accelerator config. Position IS DELIBERATELY READ-ONLY FROM OUTSIDE. It exists for a consumer to ask \"where is playback actually at right now,\" not as an input — a public setter would mean the video loop has to keep re-checking whether something external changed it out from under a running session. Starting from a nonzero position is a PARAMETER TO Play(), not a pre-set on Position — Seek's use case is fully covered by Play(TimeSpan?) and was removed as a separate method deliberately, not by accident. Backed by PlaybackReferenceClock while a session is active, and a locally-held last-known value otherwise — see the property itself. ScrubToAsync also writes into the SAME PlaybackReferenceClock when a session exists (paused included), not only the locally-held fallback — see SCRUBBING WHILE PAUSED UPDATES Position below. A scrub taken while paused is no longer just a preview, either — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below for how a subsequent Play() actually resumes from it now. AND a scrub call sets Position FIRST, before it does anything else at all — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY below. SYNCHRONIZED STARTUP (PlaybackStartGate): the video loop and the audio engine each need real setup time before either can start pacing itself, and that setup is asymmetric (video's is heavier — see PlaybackStartGate's own remarks). Both loops finish their own setup, signal the gate, and only THEN start pacing. AUDIO DELIVERY IS \"PLAY THIS NOW,\" DELIBERATELY NOT PRE-BUFFERED. PLAYBACKMODE / LEADER-FOLLOWER (PlaybackReferenceClock): exactly one of video/audio is the LEADER for a given session — it paces itself on its own real Stopwatch and reports its delivered position into a shared PlaybackReferenceClock. The other stream (if any) is the FOLLOWER — instead of its own Stopwatch, it polls the reference clock and only delivers once the leader has actually reached that content position. Reverse playback never has a follower — audio never participates in a reverse session (see REVERSE PLAYBACK below), so ReverseVideoLoopAsync is always the sole leader of its own session. PAUSE VS STOP (PlaybackPauseGate): genuinely different operations. A paused session's GpuContext/SurfacePool/decoders stay fully alive (see EAGER RELEASE ON PAUSE, REVERTED below) — pausing is meant to be, and is, an essentially free, instantly-resumable operation UNLESS a scrub actually moved the position during that pause — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below. STATE (PlaybackState) — REPLACED THE EARLIER IsPlaying/IsPaused BOOLEAN PAIR (decided in conversation): this class used to expose two independently-readable booleans (IsPlaying, and IsPaused derived from _pauseGate?.IsPaused), which meant a caller wanting \"is this actually actively playing right now\" had to combine them itself (IsPlaying && !IsPaused) — and internally, this class had its own _isPlaying bool tracking \"a session exists\" completely separately from _pauseGate's own paused/not-paused bit, two pieces of state that could only ever legally combine into three real situations (no session; session, unpaused; session, paused) despite the boolean pair's own state space technically allowing four. A single PlaybackState State (see that enum's own remarks) replaces both: Inactive/Playing/Paused cover exactly those three real situations with no illegal combination possible, and Scrubbing is now a real, first-class fourth state (see SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG below) rather than something that used to happen ambiently underneath whatever IsPlaying/IsPaused said. _pauseGate itself is UNCHANGED and still exists — it remains the actual mechanism VideoLoopAsync/ReverseVideoLoopAsync poll to block producing new frames while paused; _state is the coarse, externally- visible reflection of what's currently true, kept in sync with every place this class used to flip _isPlaying or touch _pauseGate. SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG (part of the STATE change above): ScrubToAsync's own guard — previously _isPlaying && !IsPaused — is now simply State == PlaybackState.Playing (throws in that one case, exactly the same set of situations as before: scrubbing is allowed whenever no session exists at all, OR the existing session is paused). For the duration of composing and delivering ONE scrubbed-to frame, _state is set to PlaybackState.Scrubbing (saving whatever it was immediately before — Inactive or Paused — under _stateLock) and restored to that saved value once the render finishes, whether it succeeds or fails. Because actual scrub RENDERING is already serialized through _scrubGate (a plain SemaphoreSlim — see SCRUB COALESCING below), only one ScrubToAsync call is ever inside that bracket at a time, so this save/restore can never race a second concurrent one. SCRUB/REVERSE VIA RAW SCRUB PROXIES (ScrubProxyCache / ScrubProxyReader / ScrubFrameSource) — REWRITTEN IN CONVERSATION, REPLACING AN EARLIER ffmpeg-KEYFRAME-DECODE APPROACH ENTIRELY, after two rounds of real-world testing on real hardware showed that spawning ANY ffmpeg process per scrub tick — GPU-hwaccel or forced software — could not be made both fast and crash-safe under a fast scrub drag (see the two superseded rounds summarized below). Both ScrubToAsync and reverse playback (Speed < 0) share ONE mechanism, deliberately — both need \"show me approximately this arbitrary position, instantly, with zero per-tick decode.\" Every video source referenced by the Timeline gets a small, pre-built, RAW, decoder-less scrub proxy — a fixed-size-frame file a scrub/reverse tick reads directly (one positioned file read, no subprocess, no decode at all — see ScrubProxyFormat's own remarks for the file shape and why). The trade-off, named not hidden: what's delivered during scrub/reverse is accurate to the proxy's own fixed, LOW resolution and fixed SAMPLE RATE (EditSharpConfig.ScrubProxyTargetShortSide/ScrubProxySampleRate), not the clip's real decode resolution or the exact requested frame — normal for a scrub/rewind preview, wrong for a final render (this mechanism is never used by Render/* or by VideoLoopAsync's normal forward playback). For reverse playback specifically this means visual character is a smooth, evenly-paced backward step every 1/ScrubProxySampleRate seconds of content — a real improvement over the earlier keyframe-snapped \"fast rewind\" look, since a proxy's frame density is no longer tied to the source's own (often several- seconds-apart) keyframe spacing at all. WHY NO ffmpeg PROCESS EVER RUNS DURING A SCRUB/REVERSE TICK, AND WHY THAT MATTERS BEYOND SPEED: it also means the persistent GPU decoder backing REAL forward playback (SourceDecoder.Start/NextFrame, and its GpuContext) is never touched, paused, or contended for while a scrub/reverse session is active — it can stay warm and simply resume smoothly the moment scrubbing ends, since nothing about scrubbing ever shared a process, a decode session, or a GPU context with it in the first place. Scrubbing NEVER touches ffmpeg or any file ffmpeg has open — it only ever reads pre-built .esrp proxy files via ScrubProxyReader (System.IO.RandomAccess) — see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; this remains true after every fix below. TWO SUPERSEDED ROUNDS OF FIXES, KEPT HERE AS HISTORY SINCE THE LESSON EACH ONE TAUGHT SHAPED THIS DESIGN — the mechanism itself (an ffmpeg one-shot decode per tick) is gone, but the request-coalescing fix from round 1 is NOT superseded and is still exactly how ScrubToAsync behaves (see SCRUB COALESCING below): ROUND 1 (fixed, real bug): a fast scrub drag firing many ScrubToAsync calls used to serialize them FIFO — every call ran a real ffmpeg decode to completion for a frame nobody wanted by the time it finished. Worse, the one-time per-session setup used to be gated by each call's own cancellation token, so a request superseded mid-setup killed setup itself and the next request restarted it from scratch — under fast enough scrubbing setup could never finish at all. Fixed with \"latest request wins\" coalescing plus memoized setup — see SCRUB COALESCING below, which still applies unchanged to this rewrite. ROUND 2 (found after round 1, itself now superseded by this rewrite, not by a further tweak of the same mechanism): even with coalescing fixed, each SURVIVING scrub/reverse tick was still slow (\"wait a few seconds\") and a fast enough drag could still lock the whole process up irrecoverably. Root cause was GPU-hwaccel context/ session overhead paid per one-shot tick (and a real risk of exhausting the GPU's own concurrent decode-session budget under rapid concurrent one-shot GPU decodes — a driver-level hang outside any cancellation/process-kill this process could reach). The fix tried was forcing SOFTWARE decode for every tick instead — which, per direct real-hardware feedback, was NOT the right fix: CPU decode of the clip's real native resolution is orders of magnitude slower than GPU decode (established earlier, during this project's own GPU migration work), so forcing it per-tick just traded one flavor of slow/unstable for another (this time crashing outright under load, rather than merely locking up). The actual fix wasn't \"which decode backend runs per tick\" at all — it was removing the per-tick decode requirement entirely, which is this rewrite. SCRUB COALESCING (\"LATEST REQUEST WINS\") — STILL IN EFFECT, UNCHANGED BY THE PROXY REWRITE: a fast scrub drag firing many ScrubToAsync calls still coalesces to only the latest one actually completing — even a proxy read is not literally free (a file read plus a full frame composite), and there is no reason to do that work for a position that's already stale by the time it would finish. ScrubToAsync cancels any still-in-flight/queued PREVIOUS scrub request the moment a new one arrives (_scrubSupersedeCts), linked with the caller's own ct — a superseded request's OperationCanceledException is swallowed (not the caller's own cancellation, so nothing to propagate). The one-time scrub-session setup — its own GPU context/pool (persistent, see SCRUB GPU CONTEXT IS NOW PERSISTENT below) plus a fresh ScrubFrameSource — is MEMOIZED (_scrubSetupTask, mirroring ScrubProxyCache's own cached-Task build coalescing) rather than restarted inside every call. Once started it always runs to completion regardless of which caller kicked it off or whether that caller is later superseded; every call just awaits (cancellably, via WaitAsync) whatever the current attempt is — the same \"cancel the wait, not the shared work\" split ScrubProxyCache.GetOrBuildAsync itself already uses. Individual SOURCE proxy builds are no longer part of what this setup waits ON at all — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP below. PrewarmScrubProxiesAsync — the opt-in \"generate proxies beforehand\" entry point: builds every video source's scrub proxy ahead of need (e.g. right after a project loads), so the FIRST scrub/reverse session doesn't pay any build cost at all. Entirely optional — the first scrub/reverse session builds whatever's still missing on demand either way (see EnsureScrubSessionBaseAsync). UNLIKE the session-startup path below, THIS entry point is still fully awaited/blocking for its own caller — that's the point of calling it explicitly ahead of time. SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP (fixed here, real-world regression found in testing): a scrub/reverse session's own setup used to AWAIT every referenced source's scrub proxy build INLINE (the old PrepareScrubProxiesAsync -> Task.WhenAll -> per-source ScrubProxyCache.GetOrBuildAsync) before EnsureScrubSessionBaseAsync (and therefore ScrubToAsync/ReverseVideoLoopAsync's own startup) would ever complete — for a source with no cached proxy yet, that's a real, potentially multi-second decode+encode pass (see ScrubProxyCache.BuildAsync), and testing found it visibly HUNG THE CALLING THREAD for that whole duration, not merely responded slowly. NEW EVIDENCE surfaced by that same test run, worth recording since it contradicts an earlier conclusion in this file: TWO DEAD-END HYPOTHESES below, point (a), concluded Godot's C# integration installs NO SynchronizationContext at all. A production crash log captured DURING THIS ROUND showed Godot.GodotSynchronizationContext / Godot.GodotTaskScheduler (ExecutePendingContinuations/ Activate, both driven from the engine's own per-frame ScriptManagerBridge.FrameCallback) directly in the call stack — meaning that earlier conclusion was WRONG (or true only of an older Godot version): Godot DOES capture and use its own SynchronizationContext, and it resumes await continuations from the MAIN THREAD's own per-frame callback. That reconciles this hang exactly: every await in the old PrepareScrubProxiesAsync chain (this file, ScrubProxyCache, MediaProbe, FfmpegRunner — none of it ConfigureAwait(false), see (a) below) posted its continuation back through that captured context, so a CPU-heavy decode+encode pass ended up executing in chunks ON THE MAIN THREAD itself, between/ within frame callbacks — visually indistinguishable from a genuine hang for as long as it took. FIX: proxy resolution no longer runs inline as part of session setup at all — KickOffScrubProxyResolution/KickOffProxyResolution fire each referenced source's resolution via Task.Run(...), which unconditionally schedules onto the real ThreadPool regardless of any captured SynchronizationContext, and BuildScrubSessionAsync/ ReverseVideoLoopAsync no longer await them at all — a scrub/reverse session is considered \"ready\" (EnsureScrubSessionBaseAsync returns, ReverseVideoLoopAsync proceeds) the moment its OWN GPU context exists, regardless of whether every referenced source's proxy has actually finished building yet. See MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA below for what fills the gap in the meantime. ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE (fixed here, real-world regression found in testing directly after the fix above: \"it attempted to build 7 identical files\"): once proxy resolution moved onto a fire-and-forget Task.Run per VideoSourceNode (see directly above), a Timeline that references the SAME underlying source path from several distinct VideoSourceNode instances (the same footage cut into multiple clips, or reused across channels) fired one INDEPENDENT Task.Run per NODE, all racing to build that one source's proxy at once. ScrubProxyCache.GetOrBuildAsync's own in-flight-build coalescing (InFlight.GetOrAdd, keyed by content hash) looks like it should have caught this, but each caller must first await MediaHasher.ComputeAsync(...) (real, non-instant I/O) BEFORE it ever reaches that GetOrAdd call — several callers hashing the same file at once reliably finish within the same small window of each other, and .NET's ConcurrentDictionary.GetOrAdd does NOT guarantee its valueFactory runs only once under that kind of near-simultaneous contention: multiple racing calls can each invoke BuildAndTrackAsync (a real async method that starts running the instant it's invoked, not merely queued), with only one of the resulting Tasks actually kept in the dictionary — the others run to completion anyway, each against its own temp file, each ending in its own File.Move(tempPath, finalPath, overwrite: true) racing the others for the same final path. That is exactly \"N identical builds\" for one source, for N nodes that reference it. FIX, at the call site (the actual root cause — see also the cache-level hardening immediately below): KickOffScrubProxyResolution now groups every referenced VideoSourceNode by its own Source.Path FIRST, and KickOffProxyResolution fires exactly ONE Task.Run per DISTINCT path — its result is then written into proxies for every node Id that shares that path, once the one build/lookup completes. A Timeline with 7 clips cut from the same source file now resolves that source exactly once, not 7 times. ScrubProxyCache.GetOrBuildAsync ALSO HARDENED AGAINST THE SAME RACE, AS DEFENSE-IN-DEPTH (paired with the fix directly above): the call-site dedup above is the actual fix for THIS reported bug (one Timeline, resolved through Playback), but ScrubProxyCache is a process-wide static cache other call sites can reach too (e.g. PrewarmScrubProxiesAsync running concurrently with a live scrub session's own on-demand resolution for the same source) — so the underlying GetOrAdd race is fixed at the cache itself too, not just papered over here. See ScrubProxyCache's own remarks for the actual mechanism (wrapping the in-flight Task in a Lazy so only the winning caller's factory ever actually executes). MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA (fixed here, paired with the fix directly above — direct user request: \"the placeholder should just be used anytime any media comes up empty\"): ScrubFrameSource now falls back to MediaPlaceholder.Get(...) — a small, cached, drawn-not-decoded \"MEDIA OFFLINE\" image — for ANY input node it can't currently resolve real content for: a video proxy that's still building in the background (see directly above), one whose build failed outright, a missing/corrupt static image, or a text node that fails to rasterize. See ScrubFrameSource's own class remarks, MEDIA-OFFLINE PLACEHOLDER, for the full per-case behavior. DELIBERATELY SCOPED TO SCRUB/REVERSE ONLY, NOT ClipContentSource, per direct user decision — forward Playback and Render/* keep throwing on broken/missing media; silently masking that in a real export or live playback could hide a real problem, whereas scrub/reverse is already an approximate preview by nature (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above). ScrubProxyReady EVENT (new): once a background-kicked-off proxy build completes successfully, proxies (the SAME mutable dictionary ScrubFrameSource reads from) gains that source's entry, but nothing automatically re-renders the CURRENT scrub/reverse position with it — the next tick picks it up naturally (ScrubFrameSource reads proxies fresh every call), but until then whatever's already on screen (a placeholder, or a still-missing frame) keeps showing. ScrubProxyReady fires (from whatever background thread the build completed on — same \"consumer marshals it themselves\" convention as every other event on this class) so a consumer that wants to eagerly replace a placeholder can re-issue ScrubToAsync(Position) itself once notified; entirely optional, nothing internally depends on anyone handling it. REVERSE PLAYBACK (Speed < 0): Play() branches to ReverseVideoLoopAsync instead of VideoLoopAsync. Audio never participates (audioParticipates requires Speed == 1, which negative Speed never satisfies) — reverse is video-only for now; revisit once arbitrary-speed forward audio exists and there's a real reversed-PCM delivery path to build on. Speed == 0 remains unsupported (that's Pause()/Stop(), not a playback rate) — only that one case still throws from Play(); any negative Speed is now a normal input. OPTIMIZED-MEDIA CACHE (OptimizedMediaCache, EditSharp.Caching): a video clip's decoder in the FORWARD playback path may open against a persistent, content-addressed proxy instead of the clip's true original source file, whenever ContentPreparation. ProbeVideoAsync finds one already built and big enough. Scrubbing and reverse playback consult a COMPLETELY SEPARATE cache (ScrubProxyCache) instead — see the section above. GATE-HELD AWAITS NEVER RESUME ON THE CALLER'S CONTEXT (fixed here): every await made while _scrubGate is held (ScrubToAsync, RenderClipFrameAsync, and BuildScrubSessionAsync, which runs under the gate via _scrubSetupTask) uses ConfigureAwait(false). EndScrubbing() — and therefore Play() — blocks SYNCHRONOUSLY on _scrubGate, and the Godot host DOES install a SynchronizationContext on its main thread. Found in the field: resuming playback right after a quick scrub (clicking the timeline ruler repeatedly while playing) called Play() on the main thread while a fire-and-forget ScrubToAsync still held the gate with its next continuation posted back to that same main thread — neither could proceed, and the app hard-froze. With the gate-held continuations free of the caller's context, the gate always releases on its own. Consequence: a scrub's VideoFrame is raised from a background thread, exactly like forward playback's already are. SUPERSEDED SESSIONS ARE ABANDONED, NOT AWAITED (fixed here): Stop() used to block its caller on the session's video task, and a session's setup (content probing, decoder spawn, GPU context, warm-up render) never checked its token until the start gate — so every Play() waited out the ENTIRE setup of the session before it. Found in the field: clicking the timeline repeatedly during playback resumes playback on every click, and each resume queued seconds of setup behind the last on Godot's main thread. Now Stop() only cancels; each new session awaits the previous one's task on the thread pool (WaitForRetiredSessionAsync), so sessions still never hold GPU contexts/decoders at the same time, and a session superseded while it waits never starts setup at all. Setup checks its token between steps, so a cancelled session exits at the next step instead of finishing. A retired session's exit paths (the loops' final state reset, TearDownAfterNaturalEnd) only touch state while its token is still the installed _cts (IsCurrentSession), so it can never clobber the session that replaced it. Dispose() is the one place that still waits synchronously. The audio engine's start (the whole-timeline master mix) also runs via Task.Run now, off the caller's thread and SynchronizationContext. SWITCHING BETWEEN SCRUB AND PLAYBACK (fixed, KEPT): Play() calls EndScrubbing() as its very first action, unconditionally, on BOTH the \"resume an existing paused session\" path and the \"start a brand-new session\" path, BEFORE taking _stateLock. Found in the field: scrubbing is explicitly allowed while playback is merely paused (ScrubToAsync's own guard is State != PlaybackState.Playing, not State == PlaybackState.Inactive — see STATE above) — Play() itself used to never tear an active scrub session down before (re)starting its own forward-playback session. Runs OUTSIDE _stateLock deliberately: EndScrubbing() blocks synchronously on _scrubGate, and ScrubToAsync can be holding _scrubGate while briefly needing _stateLock itself — calling EndScrubbing() while already holding _stateLock would risk a lock-order inversion deadlock. NOTE this fix now ONLY tears down _scrubContentSource (see SCRUB GPU CONTEXT IS NOW PERSISTENT below) — it no longer touches the scrub GpuContext at all, since that is now a persistent, once-created resource for this Playback instance's whole life. EAGER RELEASE ON PAUSE, REVERTED — TRIED, THEN EXPLICITLY UNDONE PER USER DIRECTION: a real GPU-contention concern was identified where scrubbing while paused stood up a scrub session's own GpuContext alongside a paused forward-playback session's still-alive one (two live GPU contexts at once). A first fix had each loop eagerly dispose its own GpuContext/SurfacePool/decoders the moment it noticed pauseGate.IsPaused, reacquiring only once actually resumed. That did NOT resolve the freezes still being reported, and it cost real playback smoothness — resuming from a pause stopped being free (it paid roughly a fresh Play()-setup cost every time), which the user explicitly called out as not worth it. REVERTED per direct instruction: VideoLoopAsync/ReverseVideoLoopAsync are back to a single GpuContext/SurfacePool/decoder set for the WHOLE forward- playback or reverse session, created once and torn down only at Stop()/natural end — pausing is, again, a cheap, instantly-resumable no-op for these resources UNLESS a scrub actually moved the position during the pause (see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below — that is a NEW, separate, narrowly-scoped mechanism, not a reintroduction of this reverted one: it only pays a restart cost when the position genuinely changed, never on every pause). SourceDecoder.Dispose()'s own WaitForExit-after-Kill fix (see that class's own remarks) is UNRELATED and STAYS — it's a real correctness fix regardless of when/whether a decoder gets disposed. TWO DEAD-END HYPOTHESES, TRIED AND EITHER REVERTED OR KEPT ON THEIR OWN MERITS ONLY — NEITHER WAS THE ACTUAL SCRUB LOCKUP: chasing the same real, reproducible scrub freeze reported repeatedly on real hardware, two further fixes were tried and BOTH were confirmed, on real hardware, to change nothing about the freeze: (a) Every await in the scrub-reachable async chain (this file, ScrubProxyCache, MediaHasher, MediaProbe, FfmpegRunner) was given ConfigureAwait(false), on the theory that a caller blocking synchronously on ScrubToAsync's Task (e.g. .Wait()/.GetAwaiter().GetResult() from a UI thread) could deadlock against a captured SynchronizationContext. REVERTED — the real consumer app (a Godot C# game) calls ScrubToAsync fire-and-forget from a slider's ValueChanged signal, never blocking on it, and Godot's C# integration was believed at the time to install NO SynchronizationContext at all — so this theory was believed not to apply here, and the fix was treated as a no-op for this app's real behavior. Removed for cleanliness rather than left in as dead weight. SEE SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP above — later evidence shows the \"no SynchronizationContext at all\" premise here was actually WRONG; this revert's conclusion about ITS OWN theorized deadlock risk is unaffected (a plain .Wait()-on-UI-thread deadlock is a different mechanism than the main-thread-hang bug that new evidence explains), but the \"this app has no SynchronizationContext, full stop\" framing above it was never actually true and should not be trusted at face value by a future investigator. (b) GpuContext.Dispose() was changed to wait for the GPU to idle (GRContext.Flush() + Submit(syncCpu: true)) before releasing the D3D12 device/queue/adapter/factory, on the theory that recreating a device shortly after disposing the previous one could race that previous device's own driver-side teardown. KEPT — it is a real, independent, low-risk correctness improvement (see GpuContext's own remarks) — but real-hardware testing showed the reported freeze was completely UNCHANGED by it, so it is not credited as fixing this bug. The real cause turned out to be a third, different thing — see GPU WORK MUST STAY ON ONE THREAD below. GPU WORK MUST STAY ON ONE THREAD — THE ACTUAL ROOT CAUSE OF THE EARLIER SCRUB LOCKUP (unrelated to the main-thread-hang fixed above, which is a separate bug with a separate mechanism — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD above): regardless of whether Godot captures a SynchronizationContext (see (a) above for why that question turned out to matter, just not for THIS bug), EVERY await anywhere in this file could already land its continuation on a DIFFERENT OS thread than the call immediately before or after it — whether that continuation is scheduled by a captured SynchronizationContext's own queue or by the plain ThreadPool, the specific thread it actually runs on from one await to the next is not guaranteed to be the same one. That means every GPU-touching call this file makes (GpuContext.Create, SurfacePool's Rent/CreateSurface, FrameCompositor.RenderFrame, GpuContext.Dispose) could land on a different thread than the call before/after it, purely as an artifact of scheduling — true from the very first version of the scrub rewrite, independent of every fix tried above. Skia's GrDirectContext (GRContext in SkiaSharp) is not documented as safe for that usage pattern: sequential-but-cross-thread access to one GRContext, its SKSurfaces, and the D3D12 command queue backing it, with nothing pinning it to one thread, is a real, plausible source of a driver-level hang — more likely to actually manifest the more real GPU work a given call submits, which lines up with the reported pattern (a cheap/short first scrub tends to survive; a slower/longer one is more likely to get unlucky; once ANY frame has succeeded, later calls in the same process keep landing on threads already warmed up for this workload, which is why it then \"stays fixed\"). FIX: GpuThreadDispatcher (Composite/GpuThreadDispatcher.cs) confines every GPU-touching call for a given GpuContext/SurfacePool pair to ONE dedicated background thread, for as long as that pair is alive — used for the scrub GPU context/pool (_scrubGpuThread, persistent — see SCRUB GPU CONTEXT IS NOW PERSISTENT below) and for a session-scoped dispatcher inside both VideoLoopAsync and ReverseVideoLoopAsync (created and disposed alongside that session's own GpuContext/SurfacePool). Every PrefetchAsync/RenderFrame/ Create/Dispose call for a given context now happens on that context's own single thread, every time, structurally — not by hoping the scheduler behaves favorably. CONFIRMED ON REAL HARDWARE — the user reported the reported freeze/lockup is completely gone after this fix. SCRUB GPU CONTEXT IS NOW PERSISTENT, PER EXPLICIT USER REQUEST: _scrubGpuContext/_scrubSurfacePool/_scrubGpuThread are created AT MOST ONCE per Playback instance (lazily, on first scrub/reverse session) and reused by every scrub session after that — EndScrubbing no longer disposes them at all; only Playback.Dispose() does, once, for real. This has two independent benefits: it directly avoids ever repeatedly creating and destroying a D3D12 device for scrubbing at all (so the earlier dispose-then-recreate race window this investigation chased in (b) above simply cannot occur for scrubbing any more, regardless of whether it was ever real), and it means a scrub session's own setup (EnsureScrubSessionBaseAsync/ BuildScrubSessionAsync) only needs to rebuild _scrubContentSource (proxy readers/static images/nested renderers — plain file handles, no GPU) on each EndScrubbing()/rebuild cycle, which is both cheaper and has no GPU-lifetime implications at all. ASSUMES RenderSettings. Resolution/Framerate stay constant for this Playback instance's whole life — true for how this class is actually constructed today (see EditSharp's own usage), but flagged here since a persisted SurfacePool sized for the FIRST scrub session's width/height would silently be wrong for a later session at a different resolution; revisit if RenderSettings ever needs to change on a live instance. SCRUBBING WHILE PAUSED UPDATES Position: ScrubToAsync reports the scrubbed-to position into the SAME PlaybackReferenceClock a live/paused session uses (_referenceClock?.Report(position), in addition to _lastKnownPosition) — previously it only wrote _lastKnownPosition, which the public Position getter never actually reads while _referenceClock is non-null (i.e. for the entire lifetime of a session, paused included). ALSO bumps _scrubGeneration under the same lock — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME immediately below, which is what actually makes a paused scrub \"count\" the next time Play() is called. SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME (root cause of two reported bugs, both fixed): previously, a scrub taken while paused updated _referenceClock/Position (see directly above) but NOTHING ELSE — the video loop's persistent decode pipe (ClipContentSource/ SourceDecoder, which can only move FORWARD) and the audio engine's own byte-offset pump both kept whatever position they were at before the pause, completely unaware a scrub had ever happened. A plain Play() resume just released the pause gate and resumed the wall clock in place, so BOTH streams simply continued from the PRE-scrub position — the scrub was a preview only, never an actual seek (bug 1: \"scrubbing should change playback position, but it doesn't\"). Worse, the stale _referenceClock value left behind by the scrub caused a SEPARATE, visible symptom for a follower stream: PlaybackReferenceClock.Position resumes extrapolating forward, in real time, from wherever it was last Report()'d — if a scrub landed EARLIER than the actual pre-pause stopped position, that value starts BELOW the follower's own fixed target position, so the follower's gap = target - referenceClock.Position computes a large POSITIVE gap and sleeps (in one big Task.Delay, not a poll loop — see VideoLoopAsync's/PlaybackAudioEngine.PumpAsync's own wait logic) for approximately the real-time distance between the scrub position and the actual resume position, before the leader's own next report ever gets a chance to correct it — a real, reproducible multi-second stall between the leader (audio, in the default SyncToAudio mode) resuming essentially immediately and the follower (video) resuming only after that stale gap has fully elapsed (bug 2: \"significant delay between audio starting back up and video starting back up ... ending a scrub at an earlier position [than where playback stopped]\"). A scrub to a LATER position than the stopped position produces a negative/zero gap instead, so the follower never waits — which is exactly why the user observed the delay ONLY for the \"earlier\" case and an instant (but still wrong-position, per bug 1) resume for the \"later\" case: both symptoms trace to this one gap. FIX: _scrubGeneration (bumped by ScrubToAsync under _stateLock) and _scrubGenerationAtPause (a snapshot of _scrubGeneration taken by Pause()) together detect \"did an actual scrub happen since this pause started\" — a plain int-equality check, immune to any timing race with the leader's own periodic Report() calls (which never touch _scrubGeneration). Play()'s resume branch (session already active and startPosition == null) now checks this first: NO scrub happened (generations match): unchanged, cheap, instant in-place resume — _pauseGate?.Resume() + _referenceClock?.ResumeWallClock() + _state = PlaybackState.Playing, exactly as before this fix. A scrub DID happen (generations differ): the persistent decode pipe genuinely cannot jump to the new position on its own, so this falls through to the SAME stop+restart path a fresh Play(startPosition) call already uses — startPosition is set to _referenceClock.Position (the scrubbed-to position, frozen since the wall clock never resumed) and the existing Stop() + rebuild-from-scratch machinery below takes it from there: a brand-new PlaybackReferenceClock initialized directly to that position, a brand-new audio pump starting its byte offset from it, and a brand-new video/reverse loop with decoders seeked via ComputeSeekOffsets(position) — the exact same well-tested startup path the very first Play() call already goes through, so there is no stale-clock/gap artifact left to produce bug 2 either. Named trade-off: resuming after an actual scrub-during- pause now costs roughly a fresh Play() setup (new decoders, new GpuContext-dispatcher work) — but ONLY when the position genuinely changed. A plain pause/resume with no scrub in between is completely unaffected and stays exactly as cheap as it always was; this is a narrowly-scoped fix, not a reintroduction of EAGER RELEASE ON PAUSE, REVERTED above. SCRUB SETS POSITION FIRST, UNCONDITIONALLY (fixed here, a SEPARATE bug from SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above): ScrubToAsync used to update _lastKnownPosition/_referenceClock/ _scrubGeneration only AFTER a frame had successfully rendered and been delivered — meaning a scrub that failed to build/render for any reason (a proxy build error, a superseded/cancelled render, or even just calling ScrubToAsync before Play() has EVER been called once) left Position completely unmoved, silently. Position now updates as the FIRST thing ScrubToAsync does, right after validating the requested position is in range and BEFORE any of the session-setup/ rendering machinery runs — a caller's scrub request is reflected in Position immediately and unconditionally, whether or not a visible frame ever actually renders for it. This also means Position now moves even on a session that has never played once — previously Position could only ever be driven by a successful render, which (see the gap below) tended to silently fail on a first-ever scrub against an empty proxy cache in some configurations. SCRUB FAILURES ARE NOW LOGGED, NOT SILENT (fixed here): ScrubToAsync is, in practice, a FIRE-AND-FORGET API for most real callers (the Godot consumer app calls it from a Slider.ValueChanged handler with no await at all) — before this fix, the only exception type this method ever caught was a superseded-request OperationCanceledException; any OTHER exception (a scrub-proxy build failure, a corrupt/missing source, anything thrown out of BuildScrubSessionAsync or the actual composite) propagated straight out of the async method as a faulted Task that a fire-and-forget caller never observes — completely silent, no log line, no visible symptom beyond \"nothing rendered.\" This is very likely what was actually happening in the reported \"an empty scrub proxy cache builds the containing folder but never the actual .esrp file, and scrubbing shows no video, with no error displayed\" bug — BuildAsync creates the target directory before it ever calls EncodeAsync, so a failure partway through encoding leaves exactly that signature (folder exists, temp file cleaned up by BuildAsync's own catch, real exception never seen by anyone). FIX: ScrubToAsync now has a catch-all for any exception that ISN'T the caller's own cancellation, and logs it via EditSharpConfig.Logger.LogError with the full exception detail before swallowing it (deliberately not rethrown — there is no synchronous caller left by the time this runs to catch it, and an unobserved faulted Task's finalization can itself crash a process via TaskScheduler.UnobservedTaskException in some hosts, which swallowing here avoids). Position has already been set (see SCRUB SETS POSITION FIRST, UNCONDITIONALLY above) regardless of whether this catch ever fires, so a logged-and-swallowed failure here only means no new frame rendered for that request, not that Position silently failed to move too. KNOWN GAPS — tracked, not hidden: REVERSE AUDIO is not implemented — reverse is video-only, see REVERSE PLAYBACK above. Video itself IS supported. ARBITRARY SPEED (audio tracking Speed != 1, Speed > 0) remains a MUST-HAVE, not deferred-maybe. Seeking to a nonzero start position may need the audio composition itself to carry a seek offset. TRUE FRAME-SKIPPING remains open. PlaybackMode WIRING — CLOSED (SyncToAudio/EveryFrame both real). SCRUBBING — mechanism CLOSED (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; SupportsScrubbing is always true now). The real, repeatedly-reproduced lockup is CONFIRMED FIXED (by the user, on real hardware) via GPU WORK MUST STAY ON ONE THREAD above (GpuThreadDispatcher + a persistent scrub GpuContext). Two earlier fixes in this same investigation (ConfigureAwait(false) throughout the scrub async chain; a GPU-idle wait in GpuContext.Dispose()) were both confirmed on real hardware NOT to change this freeze — the first was reverted as unnecessary, the second was kept on its own independent merits only (see TWO DEAD-END HYPOTHESES above). CLOSED — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above. Resuming playback after scrubbing while paused now actually resumes from the scrubbed position, and the A/V startup-delay artifact that came along with the old bug is gone too. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY and SCRUB FAILURES ARE NOW LOGGED, NOT SILENT above. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP and MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA above. A scrub/reverse session no longer blocks the calling thread while a missing proxy builds; ScrubFrameSource shows the offline placeholder for whatever isn't ready yet. REGRESSION FOUND ON REAL HARDWARE (\"attempted to build 7 identical files\" / \"No scrub proxy registered\" errors) AND FIXED — see ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE above (the duplicate-build race) and ScrubFrameSource's own MEDIA-OFFLINE PLACEHOLDER remarks (the \"No scrub proxy registered\" throw is gone — that path now falls back to the placeholder instead, which is what should have shipped with this gap's original fix but did not actually make it into the pushed file the first time). NOT YET CONFIRMED ON REAL HARDWARE. GPU DECODE FOR IndexedDelta7 — TRIED, THEN REMOVED PER EXPLICIT USER CORRECTION: an earlier round of GPU work wired a ScrubProxyGpuDecoder into every ScrubFrameSource this file constructs (BuildScrubSessionAsync's persistent scrub session and ReverseVideoLoopAsync's session-scoped one), attempting a GPU decode of each IndexedDelta7 proxy frame before falling back to IndexedDelta7Codec.Decode. The user clarified that request was actually about the BUILD/ENCODE side, not decode — a proxy read is already a cheap, single positioned file read regardless of pixel format, so GPU-accelerating it bought little, while the ENCODE side (see ScrubProxyCache/ ScrubProxyGpuEncoder) is the genuinely expensive, worth- accelerating step. The decoder and its wiring here were removed entirely as unneeded complexity; every scrub-proxy frame this file resolves is read on the CPU now, exactly as it was before that detour — see ScrubFrameSource's own remarks. public ClipFrame(byte[] Pixels, int Width, int Height, bool Complete) Parameters Pixels byte[] Width int Height int Complete bool False when some media in the clip had no proxy yet and rendered as the offline placeholder. Properties Complete False when some media in the clip had no proxy yet and rendered as the offline placeholder. public bool Complete { get; init; } Property Value bool Height public int Height { get; init; } Property Value int Pixels public byte[] Pixels { get; init; } Property Value byte[] Width public int Width { get; init; } Property Value int"
  },
  "api/EditSharp.Playback.Playback.html": {
    "href": "api/EditSharp.Playback.Playback.html",
    "title": "Class Playback | EditSharp",
    "summary": "Class Playback Namespace EditSharp.Playback Assembly EditSharp.dll public class Playback : IDisposable Inheritance object Playback Implements IDisposable Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Fields PlaybackMode public PlaybackMode PlaybackMode Field Value PlaybackMode PreservePitch How audio keeps its pitch when Speed isn't 1 (including reverse). public PitchPreservation PreservePitch Field Value PitchPreservation RenderSettings public required RenderSettings RenderSettings Field Value RenderSettings Speed public float Speed Field Value float Timeline public required Timeline Timeline Field Value Timeline Properties Position public TimeSpan Position { get; } Property Value TimeSpan State This Playback instance's current coarse state — see PlaybackState's own remarks and this class's STATE section above for the full reasoning behind replacing the earlier IsPlaying/IsPaused boolean pair with this single enum. public PlaybackState State { get; } Property Value PlaybackState Methods Dispose() Tears down EVERYTHING, including the persistent scrub GPU resources EndScrubbing() deliberately leaves alone — see class remarks, SCRUB GPU CONTEXT IS NOW PERSISTENT. The final GpuContext/SurfacePool disposal still runs on _scrubGpuThread itself (consistent with every other GPU call in this file — see GPU WORK MUST STAY ON ONE THREAD), blocked on synchronously since Dispose() is conventionally synchronous; this is the one place in this file where blocking on a dispatcher Task is appropriate, since the dispatcher's thread isn't waiting on the calling thread for anything. public void Dispose() EndScrubbing() public void EndScrubbing() OnAudioSample(AudioSampleEventArgs) protected virtual void OnAudioSample(AudioSampleEventArgs e) Parameters e AudioSampleEventArgs OnEndReached(EventArgs) protected virtual void OnEndReached(EventArgs e) Parameters e EventArgs OnPlaybackStarted(EventArgs) protected virtual void OnPlaybackStarted(EventArgs e) Parameters e EventArgs OnVideoFrame(VideoFrameEventArgs) protected virtual void OnVideoFrame(VideoFrameEventArgs e) Parameters e VideoFrameEventArgs Pause() public void Pause() Play(TimeSpan?) public void Play(TimeSpan? startPosition = null) Parameters startPosition TimeSpan? RenderClipFrameAsync(VideoClip, TimeSpan, int, int, CancellationToken) One clip composited on its own at contentTime (thumbnails). Media is read one-shot (VideoSource.GetFrameAtAsync, in RenderSettings.SourceMode) so nothing stays open between thumbnails. Complete is false when any of it showed a placeholder for something still on its way — a proxy not built that far yet, a source still opening — so the frame is worth asking for again later. public Task<ClipFrame> RenderClipFrameAsync(VideoClip clip, TimeSpan contentTime, int width, int height, CancellationToken ct = default) Parameters clip VideoClip contentTime TimeSpan width int height int ct CancellationToken Returns Task<ClipFrame> ScrubToAsync(TimeSpan, CancellationToken) Renders and delivers one frame at position, via VideoFrame. SAFE TO CALL RAPIDLY — e.g. once per pointer-move during a scrubber drag: each call supersedes (cancels) whatever previous call hasn't finished yet, so only the LATEST requested position ever actually completes and gets delivered — see class remarks, SCRUB COALESCING. A superseded call's Task completes normally (no exception) rather than throwing — only ct (if the CALLER explicitly cancels it) propagates as a real cancellation. SETS Position (both _lastKnownPosition and, when a session is active/paused, the shared PlaybackReferenceClock), AND bumps _scrubGeneration, AS THE VERY FIRST THING THIS METHOD DOES — before session setup, before rendering, unconditionally — see class remarks, SCRUB SETS POSITION FIRST, UNCONDITIONALLY. Any failure in the render/session-setup work that follows is caught and logged rather than left to vanish into an unobserved Task — see class remarks, SCRUB FAILURES ARE NOW LOGGED, NOT SILENT. NEVER BLOCKS ON A MISSING SOURCE'S SCRUB PROXY BUILD ANY MORE — see class remarks, SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP; a node whose proxy isn't ready yet renders as the offline placeholder instead (see MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA). SETS State TO Scrubbing FOR THE DURATION OF THE ACTUAL RENDER — see class remarks, SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG: whatever State was immediately before this call entered its gated section (Inactive, or Paused if a play session is paused) is saved and restored once the render finishes, succeeds or fails. public Task ScrubToAsync(TimeSpan position, CancellationToken ct = default) Parameters position TimeSpan ct CancellationToken Returns Task Stop() public void Stop() TapAudio(Guid, Action<AudioTapBlock>) Calls onBlock with every block of audio passing id: any audio node's Id, a channel's Id, or AudioTap.Master. Stays in place across sessions until disposed. Called on the audio thread, and the samples are only valid during the call. public IDisposable TapAudio(Guid id, Action<AudioTapBlock> onBlock) Parameters id Guid onBlock Action<AudioTapBlock> Returns IDisposable Events AudioSample public event EventHandler<AudioSampleEventArgs>? AudioSample Event Type EventHandler<AudioSampleEventArgs> EndReached public event EventHandler? EndReached Event Type EventHandler PlaybackStarted public event EventHandler? PlaybackStarted Event Type EventHandler VideoFrame public event EventHandler<VideoFrameEventArgs>? VideoFrame Event Type EventHandler<VideoFrameEventArgs>"
  },
  "api/EditSharp.Playback.PlaybackMode.html": {
    "href": "api/EditSharp.Playback.PlaybackMode.html",
    "title": "Enum PlaybackMode | EditSharp",
    "summary": "Enum PlaybackMode Namespace EditSharp.Playback Assembly EditSharp.dll public enum PlaybackMode Fields EveryFrame = 0 FrameDropping = 1 SyncToAudio = 2"
  },
  "api/EditSharp.Playback.PlaybackState.html": {
    "href": "api/EditSharp.Playback.PlaybackState.html",
    "title": "Enum PlaybackState | EditSharp",
    "summary": "Enum PlaybackState Namespace EditSharp.Playback Assembly EditSharp.dll The coarse, mutually-exclusive state a Playback instance is in at any given moment — REPLACES the earlier IsPlaying/IsPaused boolean pair (decided in conversation: \"a Playback should only be able to be in one of these states at a time,\" so a single enum is the correct shape, not two independently-settable booleans a caller had to combine themselves — e.g. state == PlaybackState.Playing instead of IsPlaying && !IsPaused). Scrubbing is included as a real, first-class state rather than an orthogonal flag layered on top of the other three: Playback.ScrubToAsync is only ever allowed when State is NOT Playing (see that method's own guard), and for the duration of composing/delivering one scrubbed-to frame, State is Scrubbing — reverting back to whatever it was immediately before (Inactive, or Paused if a play session was paused at the time) once that single scrub render finishes, succeeds or fails. public enum PlaybackState Fields Inactive = 0 No play session exists — never played, or Stop() was called (or natural end was reached). Paused = 2 A play session exists but is paused — everything it owns stays alive, nothing new is produced. Playing = 1 A play session exists and is actively advancing (video/audio loop running, not paused). Scrubbing = 3 A ScrubToAsync call is currently composing and delivering one frame at an arbitrary position."
  },
  "api/EditSharp.Playback.VideoFrameEventArgs.html": {
    "href": "api/EditSharp.Playback.VideoFrameEventArgs.html",
    "title": "Class VideoFrameEventArgs | EditSharp",
    "summary": "Class VideoFrameEventArgs Namespace EditSharp.Playback Assembly EditSharp.dll One rendered output frame, raw RGBA8888 (OutputFormat), straight alpha, no row padding — the exact same byte layout Renderer's accumulator writes. Buffer OWNERSHIP: Buffer is rented from ArrayPool<byte>.Shared and is only valid for the duration of the VideoFrame event handler. It is returned to the pool immediately after the handler returns, so a subscriber that needs the pixels past that point (uploading to a GPU texture on another thread, queuing for display, etc.) MUST copy out of Buffer[..Length] synchronously, before returning from the handler. public sealed class VideoFrameEventArgs : EventArgs Inheritance object EventArgs VideoFrameEventArgs Inherited Members EventArgs.Empty object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors VideoFrameEventArgs(byte[], int, int, int, TimeSpan) public VideoFrameEventArgs(byte[] buffer, int length, int width, int height, TimeSpan position) Parameters buffer byte[] length int width int height int position TimeSpan Properties Buffer public byte[] Buffer { get; } Property Value byte[] Height public int Height { get; } Property Value int Length public int Length { get; } Property Value int Position public TimeSpan Position { get; } Property Value TimeSpan Width public int Width { get; } Property Value int"
  },
  "api/EditSharp.Playback.html": {
    "href": "api/EditSharp.Playback.html",
    "title": "Namespace EditSharp.Playback | EditSharp",
    "summary": "Namespace EditSharp.Playback Classes AudioSampleEventArgs One chunk (~100ms) of mixed timeline audio, raw s16le interleaved PCM at SampleRate/ChannelCount. Buffer OWNERSHIP: same contract as VideoFrameEventArgs — Buffer is reused by PlaybackAudioEngine on the very next chunk, so a subscriber that needs the samples past the handler returning (queuing onto an audio device buffer on another thread, etc.) MUST copy out of Buffer[..Length] synchronously. ClipFrame Audio-based playback for timelines. Built directly on top of Renderer's Skia compositor primitives (ContentPreparation, ClipContentSource, FrameCompositor, GpuContext, SurfacePool) rather than re-deriving them. \"CLIPS ARE GRAPHS\" REWRITE: ContentPreparation's nativeSizes/decodePlans/decodeSourcePaths dictionaries are now keyed by InputNode Id (Guid), not by Clip — a VideoClip's graph can contain more than one VideoSourceNode; staticImagePaths is gone entirely (text rasterization moved into ClipContentSource itself). The two pattern-match sites that used to match VideoClip { Source.Type: SourceType.Video } directly (AllVideoClipsRedirectedToCache, ComputeSeekOffsets) now walk each VideoClip's own graph for its Video-type VideoSourceNode(s) instead, since Source no longer lives directly on VideoClip. FrameStateResolver.Resolve no longer takes a nativeSizes parameter — see its own remarks. REWRITE (\"channels split by kind\"): AllVideoClipsRedirectedToCache and ComputeSeekOffsets walk timeline.VideoChannels directly now, rather than timeline.Channels filtered by is not VideoClip — Timeline keeps VideoChannel and AudioChannel as two separate lists (see Timeline.cs's own remarks). The two SurfacePool seedCount call sites below (VideoLoopAsync, ReverseVideoLoopAsync) now seed off Timeline.VideoChannels.Count specifically rather than the old mixed Timeline.Channels.Count — an AudioChannel never needs a GPU-backed canvas surface, so counting it toward the warm-start heuristic never bought anything (see SurfacePool's own remarks: this is a warm-start guess, not a hard cap, so this is a correctness/clarity fix, not a behavior-changing one). CONFIG: Timeline + RenderSettings, not a Blueprint — an end consumer shouldn't have to construct a full render blueprint (with an unused OutputDirectory) just to play a timeline back. RenderSettings is shared with Renderer so the two don't carry duplicate, potentially drifting copies of resolution/framerate/hardware-accelerator config. Position IS DELIBERATELY READ-ONLY FROM OUTSIDE. It exists for a consumer to ask \"where is playback actually at right now,\" not as an input — a public setter would mean the video loop has to keep re-checking whether something external changed it out from under a running session. Starting from a nonzero position is a PARAMETER TO Play(), not a pre-set on Position — Seek's use case is fully covered by Play(TimeSpan?) and was removed as a separate method deliberately, not by accident. Backed by PlaybackReferenceClock while a session is active, and a locally-held last-known value otherwise — see the property itself. ScrubToAsync also writes into the SAME PlaybackReferenceClock when a session exists (paused included), not only the locally-held fallback — see SCRUBBING WHILE PAUSED UPDATES Position below. A scrub taken while paused is no longer just a preview, either — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below for how a subsequent Play() actually resumes from it now. AND a scrub call sets Position FIRST, before it does anything else at all — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY below. SYNCHRONIZED STARTUP (PlaybackStartGate): the video loop and the audio engine each need real setup time before either can start pacing itself, and that setup is asymmetric (video's is heavier — see PlaybackStartGate's own remarks). Both loops finish their own setup, signal the gate, and only THEN start pacing. AUDIO DELIVERY IS \"PLAY THIS NOW,\" DELIBERATELY NOT PRE-BUFFERED. PLAYBACKMODE / LEADER-FOLLOWER (PlaybackReferenceClock): exactly one of video/audio is the LEADER for a given session — it paces itself on its own real Stopwatch and reports its delivered position into a shared PlaybackReferenceClock. The other stream (if any) is the FOLLOWER — instead of its own Stopwatch, it polls the reference clock and only delivers once the leader has actually reached that content position. Reverse playback never has a follower — audio never participates in a reverse session (see REVERSE PLAYBACK below), so ReverseVideoLoopAsync is always the sole leader of its own session. PAUSE VS STOP (PlaybackPauseGate): genuinely different operations. A paused session's GpuContext/SurfacePool/decoders stay fully alive (see EAGER RELEASE ON PAUSE, REVERTED below) — pausing is meant to be, and is, an essentially free, instantly-resumable operation UNLESS a scrub actually moved the position during that pause — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below. STATE (PlaybackState) — REPLACED THE EARLIER IsPlaying/IsPaused BOOLEAN PAIR (decided in conversation): this class used to expose two independently-readable booleans (IsPlaying, and IsPaused derived from _pauseGate?.IsPaused), which meant a caller wanting \"is this actually actively playing right now\" had to combine them itself (IsPlaying && !IsPaused) — and internally, this class had its own _isPlaying bool tracking \"a session exists\" completely separately from _pauseGate's own paused/not-paused bit, two pieces of state that could only ever legally combine into three real situations (no session; session, unpaused; session, paused) despite the boolean pair's own state space technically allowing four. A single PlaybackState State (see that enum's own remarks) replaces both: Inactive/Playing/Paused cover exactly those three real situations with no illegal combination possible, and Scrubbing is now a real, first-class fourth state (see SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG below) rather than something that used to happen ambiently underneath whatever IsPlaying/IsPaused said. _pauseGate itself is UNCHANGED and still exists — it remains the actual mechanism VideoLoopAsync/ReverseVideoLoopAsync poll to block producing new frames while paused; _state is the coarse, externally- visible reflection of what's currently true, kept in sync with every place this class used to flip _isPlaying or touch _pauseGate. SCRUB STATE IS NOW A REAL STATE, NOT AN ORTHOGONAL FLAG (part of the STATE change above): ScrubToAsync's own guard — previously _isPlaying && !IsPaused — is now simply State == PlaybackState.Playing (throws in that one case, exactly the same set of situations as before: scrubbing is allowed whenever no session exists at all, OR the existing session is paused). For the duration of composing and delivering ONE scrubbed-to frame, _state is set to PlaybackState.Scrubbing (saving whatever it was immediately before — Inactive or Paused — under _stateLock) and restored to that saved value once the render finishes, whether it succeeds or fails. Because actual scrub RENDERING is already serialized through _scrubGate (a plain SemaphoreSlim — see SCRUB COALESCING below), only one ScrubToAsync call is ever inside that bracket at a time, so this save/restore can never race a second concurrent one. SCRUB/REVERSE VIA RAW SCRUB PROXIES (ScrubProxyCache / ScrubProxyReader / ScrubFrameSource) — REWRITTEN IN CONVERSATION, REPLACING AN EARLIER ffmpeg-KEYFRAME-DECODE APPROACH ENTIRELY, after two rounds of real-world testing on real hardware showed that spawning ANY ffmpeg process per scrub tick — GPU-hwaccel or forced software — could not be made both fast and crash-safe under a fast scrub drag (see the two superseded rounds summarized below). Both ScrubToAsync and reverse playback (Speed < 0) share ONE mechanism, deliberately — both need \"show me approximately this arbitrary position, instantly, with zero per-tick decode.\" Every video source referenced by the Timeline gets a small, pre-built, RAW, decoder-less scrub proxy — a fixed-size-frame file a scrub/reverse tick reads directly (one positioned file read, no subprocess, no decode at all — see ScrubProxyFormat's own remarks for the file shape and why). The trade-off, named not hidden: what's delivered during scrub/reverse is accurate to the proxy's own fixed, LOW resolution and fixed SAMPLE RATE (EditSharpConfig.ScrubProxyTargetShortSide/ScrubProxySampleRate), not the clip's real decode resolution or the exact requested frame — normal for a scrub/rewind preview, wrong for a final render (this mechanism is never used by Render/* or by VideoLoopAsync's normal forward playback). For reverse playback specifically this means visual character is a smooth, evenly-paced backward step every 1/ScrubProxySampleRate seconds of content — a real improvement over the earlier keyframe-snapped \"fast rewind\" look, since a proxy's frame density is no longer tied to the source's own (often several- seconds-apart) keyframe spacing at all. WHY NO ffmpeg PROCESS EVER RUNS DURING A SCRUB/REVERSE TICK, AND WHY THAT MATTERS BEYOND SPEED: it also means the persistent GPU decoder backing REAL forward playback (SourceDecoder.Start/NextFrame, and its GpuContext) is never touched, paused, or contended for while a scrub/reverse session is active — it can stay warm and simply resume smoothly the moment scrubbing ends, since nothing about scrubbing ever shared a process, a decode session, or a GPU context with it in the first place. Scrubbing NEVER touches ffmpeg or any file ffmpeg has open — it only ever reads pre-built .esrp proxy files via ScrubProxyReader (System.IO.RandomAccess) — see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; this remains true after every fix below. TWO SUPERSEDED ROUNDS OF FIXES, KEPT HERE AS HISTORY SINCE THE LESSON EACH ONE TAUGHT SHAPED THIS DESIGN — the mechanism itself (an ffmpeg one-shot decode per tick) is gone, but the request-coalescing fix from round 1 is NOT superseded and is still exactly how ScrubToAsync behaves (see SCRUB COALESCING below): ROUND 1 (fixed, real bug): a fast scrub drag firing many ScrubToAsync calls used to serialize them FIFO — every call ran a real ffmpeg decode to completion for a frame nobody wanted by the time it finished. Worse, the one-time per-session setup used to be gated by each call's own cancellation token, so a request superseded mid-setup killed setup itself and the next request restarted it from scratch — under fast enough scrubbing setup could never finish at all. Fixed with \"latest request wins\" coalescing plus memoized setup — see SCRUB COALESCING below, which still applies unchanged to this rewrite. ROUND 2 (found after round 1, itself now superseded by this rewrite, not by a further tweak of the same mechanism): even with coalescing fixed, each SURVIVING scrub/reverse tick was still slow (\"wait a few seconds\") and a fast enough drag could still lock the whole process up irrecoverably. Root cause was GPU-hwaccel context/ session overhead paid per one-shot tick (and a real risk of exhausting the GPU's own concurrent decode-session budget under rapid concurrent one-shot GPU decodes — a driver-level hang outside any cancellation/process-kill this process could reach). The fix tried was forcing SOFTWARE decode for every tick instead — which, per direct real-hardware feedback, was NOT the right fix: CPU decode of the clip's real native resolution is orders of magnitude slower than GPU decode (established earlier, during this project's own GPU migration work), so forcing it per-tick just traded one flavor of slow/unstable for another (this time crashing outright under load, rather than merely locking up). The actual fix wasn't \"which decode backend runs per tick\" at all — it was removing the per-tick decode requirement entirely, which is this rewrite. SCRUB COALESCING (\"LATEST REQUEST WINS\") — STILL IN EFFECT, UNCHANGED BY THE PROXY REWRITE: a fast scrub drag firing many ScrubToAsync calls still coalesces to only the latest one actually completing — even a proxy read is not literally free (a file read plus a full frame composite), and there is no reason to do that work for a position that's already stale by the time it would finish. ScrubToAsync cancels any still-in-flight/queued PREVIOUS scrub request the moment a new one arrives (_scrubSupersedeCts), linked with the caller's own ct — a superseded request's OperationCanceledException is swallowed (not the caller's own cancellation, so nothing to propagate). The one-time scrub-session setup — its own GPU context/pool (persistent, see SCRUB GPU CONTEXT IS NOW PERSISTENT below) plus a fresh ScrubFrameSource — is MEMOIZED (_scrubSetupTask, mirroring ScrubProxyCache's own cached-Task build coalescing) rather than restarted inside every call. Once started it always runs to completion regardless of which caller kicked it off or whether that caller is later superseded; every call just awaits (cancellably, via WaitAsync) whatever the current attempt is — the same \"cancel the wait, not the shared work\" split ScrubProxyCache.GetOrBuildAsync itself already uses. Individual SOURCE proxy builds are no longer part of what this setup waits ON at all — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP below. PrewarmScrubProxiesAsync — the opt-in \"generate proxies beforehand\" entry point: builds every video source's scrub proxy ahead of need (e.g. right after a project loads), so the FIRST scrub/reverse session doesn't pay any build cost at all. Entirely optional — the first scrub/reverse session builds whatever's still missing on demand either way (see EnsureScrubSessionBaseAsync). UNLIKE the session-startup path below, THIS entry point is still fully awaited/blocking for its own caller — that's the point of calling it explicitly ahead of time. SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP (fixed here, real-world regression found in testing): a scrub/reverse session's own setup used to AWAIT every referenced source's scrub proxy build INLINE (the old PrepareScrubProxiesAsync -> Task.WhenAll -> per-source ScrubProxyCache.GetOrBuildAsync) before EnsureScrubSessionBaseAsync (and therefore ScrubToAsync/ReverseVideoLoopAsync's own startup) would ever complete — for a source with no cached proxy yet, that's a real, potentially multi-second decode+encode pass (see ScrubProxyCache.BuildAsync), and testing found it visibly HUNG THE CALLING THREAD for that whole duration, not merely responded slowly. NEW EVIDENCE surfaced by that same test run, worth recording since it contradicts an earlier conclusion in this file: TWO DEAD-END HYPOTHESES below, point (a), concluded Godot's C# integration installs NO SynchronizationContext at all. A production crash log captured DURING THIS ROUND showed Godot.GodotSynchronizationContext / Godot.GodotTaskScheduler (ExecutePendingContinuations/ Activate, both driven from the engine's own per-frame ScriptManagerBridge.FrameCallback) directly in the call stack — meaning that earlier conclusion was WRONG (or true only of an older Godot version): Godot DOES capture and use its own SynchronizationContext, and it resumes await continuations from the MAIN THREAD's own per-frame callback. That reconciles this hang exactly: every await in the old PrepareScrubProxiesAsync chain (this file, ScrubProxyCache, MediaProbe, FfmpegRunner — none of it ConfigureAwait(false), see (a) below) posted its continuation back through that captured context, so a CPU-heavy decode+encode pass ended up executing in chunks ON THE MAIN THREAD itself, between/ within frame callbacks — visually indistinguishable from a genuine hang for as long as it took. FIX: proxy resolution no longer runs inline as part of session setup at all — KickOffScrubProxyResolution/KickOffProxyResolution fire each referenced source's resolution via Task.Run(...), which unconditionally schedules onto the real ThreadPool regardless of any captured SynchronizationContext, and BuildScrubSessionAsync/ ReverseVideoLoopAsync no longer await them at all — a scrub/reverse session is considered \"ready\" (EnsureScrubSessionBaseAsync returns, ReverseVideoLoopAsync proceeds) the moment its OWN GPU context exists, regardless of whether every referenced source's proxy has actually finished building yet. See MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA below for what fills the gap in the meantime. ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE (fixed here, real-world regression found in testing directly after the fix above: \"it attempted to build 7 identical files\"): once proxy resolution moved onto a fire-and-forget Task.Run per VideoSourceNode (see directly above), a Timeline that references the SAME underlying source path from several distinct VideoSourceNode instances (the same footage cut into multiple clips, or reused across channels) fired one INDEPENDENT Task.Run per NODE, all racing to build that one source's proxy at once. ScrubProxyCache.GetOrBuildAsync's own in-flight-build coalescing (InFlight.GetOrAdd, keyed by content hash) looks like it should have caught this, but each caller must first await MediaHasher.ComputeAsync(...) (real, non-instant I/O) BEFORE it ever reaches that GetOrAdd call — several callers hashing the same file at once reliably finish within the same small window of each other, and .NET's ConcurrentDictionary.GetOrAdd does NOT guarantee its valueFactory runs only once under that kind of near-simultaneous contention: multiple racing calls can each invoke BuildAndTrackAsync (a real async method that starts running the instant it's invoked, not merely queued), with only one of the resulting Tasks actually kept in the dictionary — the others run to completion anyway, each against its own temp file, each ending in its own File.Move(tempPath, finalPath, overwrite: true) racing the others for the same final path. That is exactly \"N identical builds\" for one source, for N nodes that reference it. FIX, at the call site (the actual root cause — see also the cache-level hardening immediately below): KickOffScrubProxyResolution now groups every referenced VideoSourceNode by its own Source.Path FIRST, and KickOffProxyResolution fires exactly ONE Task.Run per DISTINCT path — its result is then written into proxies for every node Id that shares that path, once the one build/lookup completes. A Timeline with 7 clips cut from the same source file now resolves that source exactly once, not 7 times. ScrubProxyCache.GetOrBuildAsync ALSO HARDENED AGAINST THE SAME RACE, AS DEFENSE-IN-DEPTH (paired with the fix directly above): the call-site dedup above is the actual fix for THIS reported bug (one Timeline, resolved through Playback), but ScrubProxyCache is a process-wide static cache other call sites can reach too (e.g. PrewarmScrubProxiesAsync running concurrently with a live scrub session's own on-demand resolution for the same source) — so the underlying GetOrAdd race is fixed at the cache itself too, not just papered over here. See ScrubProxyCache's own remarks for the actual mechanism (wrapping the in-flight Task in a Lazy so only the winning caller's factory ever actually executes). MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA (fixed here, paired with the fix directly above — direct user request: \"the placeholder should just be used anytime any media comes up empty\"): ScrubFrameSource now falls back to MediaPlaceholder.Get(...) — a small, cached, drawn-not-decoded \"MEDIA OFFLINE\" image — for ANY input node it can't currently resolve real content for: a video proxy that's still building in the background (see directly above), one whose build failed outright, a missing/corrupt static image, or a text node that fails to rasterize. See ScrubFrameSource's own class remarks, MEDIA-OFFLINE PLACEHOLDER, for the full per-case behavior. DELIBERATELY SCOPED TO SCRUB/REVERSE ONLY, NOT ClipContentSource, per direct user decision — forward Playback and Render/* keep throwing on broken/missing media; silently masking that in a real export or live playback could hide a real problem, whereas scrub/reverse is already an approximate preview by nature (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above). ScrubProxyReady EVENT (new): once a background-kicked-off proxy build completes successfully, proxies (the SAME mutable dictionary ScrubFrameSource reads from) gains that source's entry, but nothing automatically re-renders the CURRENT scrub/reverse position with it — the next tick picks it up naturally (ScrubFrameSource reads proxies fresh every call), but until then whatever's already on screen (a placeholder, or a still-missing frame) keeps showing. ScrubProxyReady fires (from whatever background thread the build completed on — same \"consumer marshals it themselves\" convention as every other event on this class) so a consumer that wants to eagerly replace a placeholder can re-issue ScrubToAsync(Position) itself once notified; entirely optional, nothing internally depends on anyone handling it. REVERSE PLAYBACK (Speed < 0): Play() branches to ReverseVideoLoopAsync instead of VideoLoopAsync. Audio never participates (audioParticipates requires Speed == 1, which negative Speed never satisfies) — reverse is video-only for now; revisit once arbitrary-speed forward audio exists and there's a real reversed-PCM delivery path to build on. Speed == 0 remains unsupported (that's Pause()/Stop(), not a playback rate) — only that one case still throws from Play(); any negative Speed is now a normal input. OPTIMIZED-MEDIA CACHE (OptimizedMediaCache, EditSharp.Caching): a video clip's decoder in the FORWARD playback path may open against a persistent, content-addressed proxy instead of the clip's true original source file, whenever ContentPreparation. ProbeVideoAsync finds one already built and big enough. Scrubbing and reverse playback consult a COMPLETELY SEPARATE cache (ScrubProxyCache) instead — see the section above. GATE-HELD AWAITS NEVER RESUME ON THE CALLER'S CONTEXT (fixed here): every await made while _scrubGate is held (ScrubToAsync, RenderClipFrameAsync, and BuildScrubSessionAsync, which runs under the gate via _scrubSetupTask) uses ConfigureAwait(false). EndScrubbing() — and therefore Play() — blocks SYNCHRONOUSLY on _scrubGate, and the Godot host DOES install a SynchronizationContext on its main thread. Found in the field: resuming playback right after a quick scrub (clicking the timeline ruler repeatedly while playing) called Play() on the main thread while a fire-and-forget ScrubToAsync still held the gate with its next continuation posted back to that same main thread — neither could proceed, and the app hard-froze. With the gate-held continuations free of the caller's context, the gate always releases on its own. Consequence: a scrub's VideoFrame is raised from a background thread, exactly like forward playback's already are. SUPERSEDED SESSIONS ARE ABANDONED, NOT AWAITED (fixed here): Stop() used to block its caller on the session's video task, and a session's setup (content probing, decoder spawn, GPU context, warm-up render) never checked its token until the start gate — so every Play() waited out the ENTIRE setup of the session before it. Found in the field: clicking the timeline repeatedly during playback resumes playback on every click, and each resume queued seconds of setup behind the last on Godot's main thread. Now Stop() only cancels; each new session awaits the previous one's task on the thread pool (WaitForRetiredSessionAsync), so sessions still never hold GPU contexts/decoders at the same time, and a session superseded while it waits never starts setup at all. Setup checks its token between steps, so a cancelled session exits at the next step instead of finishing. A retired session's exit paths (the loops' final state reset, TearDownAfterNaturalEnd) only touch state while its token is still the installed _cts (IsCurrentSession), so it can never clobber the session that replaced it. Dispose() is the one place that still waits synchronously. The audio engine's start (the whole-timeline master mix) also runs via Task.Run now, off the caller's thread and SynchronizationContext. SWITCHING BETWEEN SCRUB AND PLAYBACK (fixed, KEPT): Play() calls EndScrubbing() as its very first action, unconditionally, on BOTH the \"resume an existing paused session\" path and the \"start a brand-new session\" path, BEFORE taking _stateLock. Found in the field: scrubbing is explicitly allowed while playback is merely paused (ScrubToAsync's own guard is State != PlaybackState.Playing, not State == PlaybackState.Inactive — see STATE above) — Play() itself used to never tear an active scrub session down before (re)starting its own forward-playback session. Runs OUTSIDE _stateLock deliberately: EndScrubbing() blocks synchronously on _scrubGate, and ScrubToAsync can be holding _scrubGate while briefly needing _stateLock itself — calling EndScrubbing() while already holding _stateLock would risk a lock-order inversion deadlock. NOTE this fix now ONLY tears down _scrubContentSource (see SCRUB GPU CONTEXT IS NOW PERSISTENT below) — it no longer touches the scrub GpuContext at all, since that is now a persistent, once-created resource for this Playback instance's whole life. EAGER RELEASE ON PAUSE, REVERTED — TRIED, THEN EXPLICITLY UNDONE PER USER DIRECTION: a real GPU-contention concern was identified where scrubbing while paused stood up a scrub session's own GpuContext alongside a paused forward-playback session's still-alive one (two live GPU contexts at once). A first fix had each loop eagerly dispose its own GpuContext/SurfacePool/decoders the moment it noticed pauseGate.IsPaused, reacquiring only once actually resumed. That did NOT resolve the freezes still being reported, and it cost real playback smoothness — resuming from a pause stopped being free (it paid roughly a fresh Play()-setup cost every time), which the user explicitly called out as not worth it. REVERTED per direct instruction: VideoLoopAsync/ReverseVideoLoopAsync are back to a single GpuContext/SurfacePool/decoder set for the WHOLE forward- playback or reverse session, created once and torn down only at Stop()/natural end — pausing is, again, a cheap, instantly-resumable no-op for these resources UNLESS a scrub actually moved the position during the pause (see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME below — that is a NEW, separate, narrowly-scoped mechanism, not a reintroduction of this reverted one: it only pays a restart cost when the position genuinely changed, never on every pause). SourceDecoder.Dispose()'s own WaitForExit-after-Kill fix (see that class's own remarks) is UNRELATED and STAYS — it's a real correctness fix regardless of when/whether a decoder gets disposed. TWO DEAD-END HYPOTHESES, TRIED AND EITHER REVERTED OR KEPT ON THEIR OWN MERITS ONLY — NEITHER WAS THE ACTUAL SCRUB LOCKUP: chasing the same real, reproducible scrub freeze reported repeatedly on real hardware, two further fixes were tried and BOTH were confirmed, on real hardware, to change nothing about the freeze: (a) Every await in the scrub-reachable async chain (this file, ScrubProxyCache, MediaHasher, MediaProbe, FfmpegRunner) was given ConfigureAwait(false), on the theory that a caller blocking synchronously on ScrubToAsync's Task (e.g. .Wait()/.GetAwaiter().GetResult() from a UI thread) could deadlock against a captured SynchronizationContext. REVERTED — the real consumer app (a Godot C# game) calls ScrubToAsync fire-and-forget from a slider's ValueChanged signal, never blocking on it, and Godot's C# integration was believed at the time to install NO SynchronizationContext at all — so this theory was believed not to apply here, and the fix was treated as a no-op for this app's real behavior. Removed for cleanliness rather than left in as dead weight. SEE SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP above — later evidence shows the \"no SynchronizationContext at all\" premise here was actually WRONG; this revert's conclusion about ITS OWN theorized deadlock risk is unaffected (a plain .Wait()-on-UI-thread deadlock is a different mechanism than the main-thread-hang bug that new evidence explains), but the \"this app has no SynchronizationContext, full stop\" framing above it was never actually true and should not be trusted at face value by a future investigator. (b) GpuContext.Dispose() was changed to wait for the GPU to idle (GRContext.Flush() + Submit(syncCpu: true)) before releasing the D3D12 device/queue/adapter/factory, on the theory that recreating a device shortly after disposing the previous one could race that previous device's own driver-side teardown. KEPT — it is a real, independent, low-risk correctness improvement (see GpuContext's own remarks) — but real-hardware testing showed the reported freeze was completely UNCHANGED by it, so it is not credited as fixing this bug. The real cause turned out to be a third, different thing — see GPU WORK MUST STAY ON ONE THREAD below. GPU WORK MUST STAY ON ONE THREAD — THE ACTUAL ROOT CAUSE OF THE EARLIER SCRUB LOCKUP (unrelated to the main-thread-hang fixed above, which is a separate bug with a separate mechanism — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD above): regardless of whether Godot captures a SynchronizationContext (see (a) above for why that question turned out to matter, just not for THIS bug), EVERY await anywhere in this file could already land its continuation on a DIFFERENT OS thread than the call immediately before or after it — whether that continuation is scheduled by a captured SynchronizationContext's own queue or by the plain ThreadPool, the specific thread it actually runs on from one await to the next is not guaranteed to be the same one. That means every GPU-touching call this file makes (GpuContext.Create, SurfacePool's Rent/CreateSurface, FrameCompositor.RenderFrame, GpuContext.Dispose) could land on a different thread than the call before/after it, purely as an artifact of scheduling — true from the very first version of the scrub rewrite, independent of every fix tried above. Skia's GrDirectContext (GRContext in SkiaSharp) is not documented as safe for that usage pattern: sequential-but-cross-thread access to one GRContext, its SKSurfaces, and the D3D12 command queue backing it, with nothing pinning it to one thread, is a real, plausible source of a driver-level hang — more likely to actually manifest the more real GPU work a given call submits, which lines up with the reported pattern (a cheap/short first scrub tends to survive; a slower/longer one is more likely to get unlucky; once ANY frame has succeeded, later calls in the same process keep landing on threads already warmed up for this workload, which is why it then \"stays fixed\"). FIX: GpuThreadDispatcher (Composite/GpuThreadDispatcher.cs) confines every GPU-touching call for a given GpuContext/SurfacePool pair to ONE dedicated background thread, for as long as that pair is alive — used for the scrub GPU context/pool (_scrubGpuThread, persistent — see SCRUB GPU CONTEXT IS NOW PERSISTENT below) and for a session-scoped dispatcher inside both VideoLoopAsync and ReverseVideoLoopAsync (created and disposed alongside that session's own GpuContext/SurfacePool). Every PrefetchAsync/RenderFrame/ Create/Dispose call for a given context now happens on that context's own single thread, every time, structurally — not by hoping the scheduler behaves favorably. CONFIRMED ON REAL HARDWARE — the user reported the reported freeze/lockup is completely gone after this fix. SCRUB GPU CONTEXT IS NOW PERSISTENT, PER EXPLICIT USER REQUEST: _scrubGpuContext/_scrubSurfacePool/_scrubGpuThread are created AT MOST ONCE per Playback instance (lazily, on first scrub/reverse session) and reused by every scrub session after that — EndScrubbing no longer disposes them at all; only Playback.Dispose() does, once, for real. This has two independent benefits: it directly avoids ever repeatedly creating and destroying a D3D12 device for scrubbing at all (so the earlier dispose-then-recreate race window this investigation chased in (b) above simply cannot occur for scrubbing any more, regardless of whether it was ever real), and it means a scrub session's own setup (EnsureScrubSessionBaseAsync/ BuildScrubSessionAsync) only needs to rebuild _scrubContentSource (proxy readers/static images/nested renderers — plain file handles, no GPU) on each EndScrubbing()/rebuild cycle, which is both cheaper and has no GPU-lifetime implications at all. ASSUMES RenderSettings. Resolution/Framerate stay constant for this Playback instance's whole life — true for how this class is actually constructed today (see EditSharp's own usage), but flagged here since a persisted SurfacePool sized for the FIRST scrub session's width/height would silently be wrong for a later session at a different resolution; revisit if RenderSettings ever needs to change on a live instance. SCRUBBING WHILE PAUSED UPDATES Position: ScrubToAsync reports the scrubbed-to position into the SAME PlaybackReferenceClock a live/paused session uses (_referenceClock?.Report(position), in addition to _lastKnownPosition) — previously it only wrote _lastKnownPosition, which the public Position getter never actually reads while _referenceClock is non-null (i.e. for the entire lifetime of a session, paused included). ALSO bumps _scrubGeneration under the same lock — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME immediately below, which is what actually makes a paused scrub \"count\" the next time Play() is called. SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME (root cause of two reported bugs, both fixed): previously, a scrub taken while paused updated _referenceClock/Position (see directly above) but NOTHING ELSE — the video loop's persistent decode pipe (ClipContentSource/ SourceDecoder, which can only move FORWARD) and the audio engine's own byte-offset pump both kept whatever position they were at before the pause, completely unaware a scrub had ever happened. A plain Play() resume just released the pause gate and resumed the wall clock in place, so BOTH streams simply continued from the PRE-scrub position — the scrub was a preview only, never an actual seek (bug 1: \"scrubbing should change playback position, but it doesn't\"). Worse, the stale _referenceClock value left behind by the scrub caused a SEPARATE, visible symptom for a follower stream: PlaybackReferenceClock.Position resumes extrapolating forward, in real time, from wherever it was last Report()'d — if a scrub landed EARLIER than the actual pre-pause stopped position, that value starts BELOW the follower's own fixed target position, so the follower's gap = target - referenceClock.Position computes a large POSITIVE gap and sleeps (in one big Task.Delay, not a poll loop — see VideoLoopAsync's/PlaybackAudioEngine.PumpAsync's own wait logic) for approximately the real-time distance between the scrub position and the actual resume position, before the leader's own next report ever gets a chance to correct it — a real, reproducible multi-second stall between the leader (audio, in the default SyncToAudio mode) resuming essentially immediately and the follower (video) resuming only after that stale gap has fully elapsed (bug 2: \"significant delay between audio starting back up and video starting back up ... ending a scrub at an earlier position [than where playback stopped]\"). A scrub to a LATER position than the stopped position produces a negative/zero gap instead, so the follower never waits — which is exactly why the user observed the delay ONLY for the \"earlier\" case and an instant (but still wrong-position, per bug 1) resume for the \"later\" case: both symptoms trace to this one gap. FIX: _scrubGeneration (bumped by ScrubToAsync under _stateLock) and _scrubGenerationAtPause (a snapshot of _scrubGeneration taken by Pause()) together detect \"did an actual scrub happen since this pause started\" — a plain int-equality check, immune to any timing race with the leader's own periodic Report() calls (which never touch _scrubGeneration). Play()'s resume branch (session already active and startPosition == null) now checks this first: NO scrub happened (generations match): unchanged, cheap, instant in-place resume — _pauseGate?.Resume() + _referenceClock?.ResumeWallClock() + _state = PlaybackState.Playing, exactly as before this fix. A scrub DID happen (generations differ): the persistent decode pipe genuinely cannot jump to the new position on its own, so this falls through to the SAME stop+restart path a fresh Play(startPosition) call already uses — startPosition is set to _referenceClock.Position (the scrubbed-to position, frozen since the wall clock never resumed) and the existing Stop() + rebuild-from-scratch machinery below takes it from there: a brand-new PlaybackReferenceClock initialized directly to that position, a brand-new audio pump starting its byte offset from it, and a brand-new video/reverse loop with decoders seeked via ComputeSeekOffsets(position) — the exact same well-tested startup path the very first Play() call already goes through, so there is no stale-clock/gap artifact left to produce bug 2 either. Named trade-off: resuming after an actual scrub-during- pause now costs roughly a fresh Play() setup (new decoders, new GpuContext-dispatcher work) — but ONLY when the position genuinely changed. A plain pause/resume with no scrub in between is completely unaffected and stays exactly as cheap as it always was; this is a narrowly-scoped fix, not a reintroduction of EAGER RELEASE ON PAUSE, REVERTED above. SCRUB SETS POSITION FIRST, UNCONDITIONALLY (fixed here, a SEPARATE bug from SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above): ScrubToAsync used to update _lastKnownPosition/_referenceClock/ _scrubGeneration only AFTER a frame had successfully rendered and been delivered — meaning a scrub that failed to build/render for any reason (a proxy build error, a superseded/cancelled render, or even just calling ScrubToAsync before Play() has EVER been called once) left Position completely unmoved, silently. Position now updates as the FIRST thing ScrubToAsync does, right after validating the requested position is in range and BEFORE any of the session-setup/ rendering machinery runs — a caller's scrub request is reflected in Position immediately and unconditionally, whether or not a visible frame ever actually renders for it. This also means Position now moves even on a session that has never played once — previously Position could only ever be driven by a successful render, which (see the gap below) tended to silently fail on a first-ever scrub against an empty proxy cache in some configurations. SCRUB FAILURES ARE NOW LOGGED, NOT SILENT (fixed here): ScrubToAsync is, in practice, a FIRE-AND-FORGET API for most real callers (the Godot consumer app calls it from a Slider.ValueChanged handler with no await at all) — before this fix, the only exception type this method ever caught was a superseded-request OperationCanceledException; any OTHER exception (a scrub-proxy build failure, a corrupt/missing source, anything thrown out of BuildScrubSessionAsync or the actual composite) propagated straight out of the async method as a faulted Task that a fire-and-forget caller never observes — completely silent, no log line, no visible symptom beyond \"nothing rendered.\" This is very likely what was actually happening in the reported \"an empty scrub proxy cache builds the containing folder but never the actual .esrp file, and scrubbing shows no video, with no error displayed\" bug — BuildAsync creates the target directory before it ever calls EncodeAsync, so a failure partway through encoding leaves exactly that signature (folder exists, temp file cleaned up by BuildAsync's own catch, real exception never seen by anyone). FIX: ScrubToAsync now has a catch-all for any exception that ISN'T the caller's own cancellation, and logs it via EditSharpConfig.Logger.LogError with the full exception detail before swallowing it (deliberately not rethrown — there is no synchronous caller left by the time this runs to catch it, and an unobserved faulted Task's finalization can itself crash a process via TaskScheduler.UnobservedTaskException in some hosts, which swallowing here avoids). Position has already been set (see SCRUB SETS POSITION FIRST, UNCONDITIONALLY above) regardless of whether this catch ever fires, so a logged-and-swallowed failure here only means no new frame rendered for that request, not that Position silently failed to move too. KNOWN GAPS — tracked, not hidden: REVERSE AUDIO is not implemented — reverse is video-only, see REVERSE PLAYBACK above. Video itself IS supported. ARBITRARY SPEED (audio tracking Speed != 1, Speed > 0) remains a MUST-HAVE, not deferred-maybe. Seeking to a nonzero start position may need the audio composition itself to carry a seek offset. TRUE FRAME-SKIPPING remains open. PlaybackMode WIRING — CLOSED (SyncToAudio/EveryFrame both real). SCRUBBING — mechanism CLOSED (see SCRUB/REVERSE VIA RAW SCRUB PROXIES above; SupportsScrubbing is always true now). The real, repeatedly-reproduced lockup is CONFIRMED FIXED (by the user, on real hardware) via GPU WORK MUST STAY ON ONE THREAD above (GpuThreadDispatcher + a persistent scrub GpuContext). Two earlier fixes in this same investigation (ConfigureAwait(false) throughout the scrub async chain; a GPU-idle wait in GpuContext.Dispose()) were both confirmed on real hardware NOT to change this freeze — the first was reverted as unnecessary, the second was kept on its own independent merits only (see TWO DEAD-END HYPOTHESES above). CLOSED — see SCRUB DURING PAUSE FORCES A REAL SEEK ON RESUME above. Resuming playback after scrubbing while paused now actually resumes from the scrubbed position, and the A/V startup-delay artifact that came along with the old bug is gone too. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB SETS POSITION FIRST, UNCONDITIONALLY and SCRUB FAILURES ARE NOW LOGGED, NOT SILENT above. NOT YET CONFIRMED ON REAL HARDWARE. CLOSED — see SCRUB PROXY BUILDS RUN ON A REAL BACKGROUND THREAD, NEVER BLOCK SESSION STARTUP and MEDIA-OFFLINE PLACEHOLDER FOR NOT-YET-READY MEDIA above. A scrub/reverse session no longer blocks the calling thread while a missing proxy builds; ScrubFrameSource shows the offline placeholder for whatever isn't ready yet. REGRESSION FOUND ON REAL HARDWARE (\"attempted to build 7 identical files\" / \"No scrub proxy registered\" errors) AND FIXED — see ONE BACKGROUND BUILD PER DISTINCT SOURCE PATH, NOT PER NODE above (the duplicate-build race) and ScrubFrameSource's own MEDIA-OFFLINE PLACEHOLDER remarks (the \"No scrub proxy registered\" throw is gone — that path now falls back to the placeholder instead, which is what should have shipped with this gap's original fix but did not actually make it into the pushed file the first time). NOT YET CONFIRMED ON REAL HARDWARE. GPU DECODE FOR IndexedDelta7 — TRIED, THEN REMOVED PER EXPLICIT USER CORRECTION: an earlier round of GPU work wired a ScrubProxyGpuDecoder into every ScrubFrameSource this file constructs (BuildScrubSessionAsync's persistent scrub session and ReverseVideoLoopAsync's session-scoped one), attempting a GPU decode of each IndexedDelta7 proxy frame before falling back to IndexedDelta7Codec.Decode. The user clarified that request was actually about the BUILD/ENCODE side, not decode — a proxy read is already a cheap, single positioned file read regardless of pixel format, so GPU-accelerating it bought little, while the ENCODE side (see ScrubProxyCache/ ScrubProxyGpuEncoder) is the genuinely expensive, worth- accelerating step. The decoder and its wiring here were removed entirely as unneeded complexity; every scrub-proxy frame this file resolves is read on the CPU now, exactly as it was before that detour — see ScrubFrameSource's own remarks. Playback VideoFrameEventArgs One rendered output frame, raw RGBA8888 (OutputFormat), straight alpha, no row padding — the exact same byte layout Renderer's accumulator writes. Buffer OWNERSHIP: Buffer is rented from ArrayPool<byte>.Shared and is only valid for the duration of the VideoFrame event handler. It is returned to the pool immediately after the handler returns, so a subscriber that needs the pixels past that point (uploading to a GPU texture on another thread, queuing for display, etc.) MUST copy out of Buffer[..Length] synchronously, before returning from the handler. Enums PlaybackMode PlaybackState The coarse, mutually-exclusive state a Playback instance is in at any given moment — REPLACES the earlier IsPlaying/IsPaused boolean pair (decided in conversation: \"a Playback should only be able to be in one of these states at a time,\" so a single enum is the correct shape, not two independently-settable booleans a caller had to combine themselves — e.g. state == PlaybackState.Playing instead of IsPlaying && !IsPaused). Scrubbing is included as a real, first-class state rather than an orthogonal flag layered on top of the other three: Playback.ScrubToAsync is only ever allowed when State is NOT Playing (see that method's own guard), and for the duration of composing/delivering one scrubbed-to frame, State is Scrubbing — reverting back to whatever it was immediately before (Inactive, or Paused if a play session was paused at the time) once that single scrub render finishes, succeeds or fails."
  },
  "api/EditSharp.Rendering.Blueprint.html": {
    "href": "api/EditSharp.Rendering.Blueprint.html",
    "title": "Class Blueprint | EditSharp",
    "summary": "Class Blueprint Namespace EditSharp.Rendering Assembly EditSharp.dll public class Blueprint Inheritance object Blueprint Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Properties OutputDirectory public required string OutputDirectory { get; set; } Property Value string RenderSettings public required RenderSettings RenderSettings { get; set; } Property Value RenderSettings Timeline public required Timeline Timeline { get; set; } Property Value Timeline"
  },
  "api/EditSharp.Rendering.RenderReport.html": {
    "href": "api/EditSharp.Rendering.RenderReport.html",
    "title": "Class RenderReport | EditSharp",
    "summary": "Class RenderReport Namespace EditSharp.Rendering Assembly EditSharp.dll What went wrong with sources during a render that still completed. A failing source is rendered as a labeled placeholder rather than aborting the export; this is how the caller finds out. public sealed record RenderReport : IEquatable<RenderReport> Inheritance object RenderReport Implements IEquatable<RenderReport> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors RenderReport(IReadOnlyList<SourceProblem>) What went wrong with sources during a render that still completed. A failing source is rendered as a labeled placeholder rather than aborting the export; this is how the caller finds out. public RenderReport(IReadOnlyList<SourceProblem> Problems) Parameters Problems IReadOnlyList<SourceProblem> Properties HasProblems public bool HasProblems { get; } Property Value bool Problems public IReadOnlyList<SourceProblem> Problems { get; init; } Property Value IReadOnlyList<SourceProblem>"
  },
  "api/EditSharp.Rendering.RenderSettings.html": {
    "href": "api/EditSharp.Rendering.RenderSettings.html",
    "title": "Struct RenderSettings | EditSharp",
    "summary": "Struct RenderSettings Namespace EditSharp.Rendering Assembly EditSharp.dll public struct RenderSettings Inherited Members ValueType.Equals(object) ValueType.GetHashCode() ValueType.ToString() object.Equals(object, object) object.GetType() object.ReferenceEquals(object, object) Constructors RenderSettings() public RenderSettings() Properties AudioCodec public AudioCodec AudioCodec { readonly get; set; } Property Value AudioCodec Framerate public int Framerate { readonly get; set; } Property Value int GpuAdapterIndex Which GPU the Skia compositor should run on, as a DXGI adapter index. NULL (the default) means AUTO: pick the first non-software adapter, which is what every prior version did implicitly. This exists because \"which GPU is this actually running on\" turned out to be a load-bearing question rather than an implementation detail. On a hybrid machine the first non-software adapter may be the integrated GPU or the discrete one depending on enumeration order, system power settings, and BIOS mode — so the same build can silently execute the whole compositor on a different vendor's driver and shader compiler from one boot to the next. Making the choice explicit turns that into a setting instead of a surprise, and gives a direct way to A/B two GPUs with one binary at one moment when something renders differently than expected. Adapter indices are logged at the start of every GPU session (see GpuContext), so the valid values for a given machine are visible without guessing. An index that doesn't exist, or names a software adapter, falls back to software rendering with a logged warning rather than failing the render. Ignored entirely when HardwareAccelerator is None. public int? GpuAdapterIndex { readonly get; set; } Property Value int? HardwareAccelerator public HardwareAccelerator HardwareAccelerator { readonly get; set; } Property Value HardwareAccelerator Resolution public Vector2 Resolution { readonly get; set; } Property Value Vector2 SourceMode Proxies or originals, for sources that have proxies. Defaults to SourceOnly so an export is full quality unless asked otherwise; previews (Playback, thumbnails) opt into ProxiesOnly themselves. Read when a session prepares its sources, so a change applies from the next session. public SourceMode SourceMode { readonly get; set; } Property Value SourceMode VideoCodec public VideoCodec VideoCodec { readonly get; set; } Property Value VideoCodec"
  },
  "api/EditSharp.Rendering.Renderer.html": {
    "href": "api/EditSharp.Rendering.Renderer.html",
    "title": "Class Renderer | EditSharp",
    "summary": "Class Renderer Namespace EditSharp.Rendering Assembly EditSharp.dll Entry point for the render strategy — wires the Skia compositor into an actual end-to-end render. Shape of a render: Probe every Video-type VideoSourceNode's native size (MediaProbe) across the whole timeline — everything about a video clip's media inputs that's constant across its whole life, done once up front (ContentPreparation). TextInputNode/ColorGeneratorInputNode/ NoiseInputNode/TimelineVideoInputNode need no such up-front prep any more — see ContentPreparation's own remarks. Mix the timeline's audio once (AudioMixer.ComposeAsync) and spawn the SINGLE ffmpeg process that will do the real mux + encode for the whole render, with its stdin left open as a raw-video pipe. Render every output frame SEQUENTIALLY directly against an in-process SKCanvas (FrameCompositor), writing each frame's raw RGBA8888 bytes straight into that ffmpeg process's stdin as it's produced — see STREAMING REWRITE below. A Video-type VideoSourceNode's SourceDecoder is opened on its clip's first visible frame and disposed once that clip's visible window ends (ClipContentSource). Close stdin once every frame has been written (ffmpeg's rawvideo demuxer treats that exactly like reaching EOF on a file) and wait for ffmpeg to finish encoding. STREAMING REWRITE — WHY THIS EXISTS: this used to append every frame's raw, uncompressed RGBA8888 bytes to a single growing \"accumulator\" temp file on disk, and only handed that file to ffmpeg for mux/encode once every frame had already been rendered. At 1920x1080 that's 8,294,400 bytes/frame (~7.9MiB); an 18-minute 30fps render is 32,400 frames, i.e. roughly 268GB of raw bytes hitting disk before a single byte of that data was ever compressed — reported as \"hundreds of gigabytes of writes\" and confirmed to match this arithmetic almost exactly, not a leak or a runaway loop. Frame rendering now writes directly into the SAME ffmpeg process's stdin pipe that does the real mux/encode (mirroring how SourceDecoder already pipes raw frames IN from an ffmpeg decode process — this is the same pattern on the encode side), so raw video never touches disk at all: ffmpeg compresses each frame into the target codec as it arrives instead of after the whole timeline has already been written out losslessly. Disk usage for the video side of a render is now effectively just the size of the final encoded output file. AUDIO STILL GOES THROUGH A SMALL TEMP FILE, DELIBERATELY: the mixed master PCM (see AudioMixer.ComposeAsync) is orders of magnitude smaller than raw video (48kHz stereo f32 is 384,000 bytes/sec, so an 18-minute timeline is ~414MB, not hundreds of gigabytes) and ffmpeg needs it as a real, complete, seekable input at process-start time — unlike the video side, it isn't produced incrementally by anything this class does. A second OS pipe/named-pipe could avoid even that, but isn't pursued here: it would need real cross-platform machinery (named pipes on Windows, a FIFO on Unix) for a temp file that was never the actual disk-usage problem. ONE CONSEQUENCE OF STREAMING WORTH FLAGGING: because AudioMixer. ComposeAsync's result has to be written to that temp file and handed to ffmpeg as an -i argument BEFORE ffmpeg can be spawned — and frame rendering can't start streaming into ffmpeg's stdin until ffmpeg exists — audio composition is now awaited before frame rendering begins, rather than run concurrently with it the way this used to work. It still overlaps content preparation and GPU/decoder setup above it, which is normally the larger win of the two; audio mixing itself is a one-shot in-memory computation, not a per-frame cost, so this is expected to be a minor, not a proportional, regression versus however long the frame-by-frame render itself takes. \"CLIPS ARE GRAPHS\" REWRITE: ContentPreparation's nativeSizes/decodePlans/decodeSourcePaths dictionaries are now keyed by InputNode Id (Guid), not by Clip — a VideoClip's graph can contain more than one VideoSourceNode. staticImagePaths and the tempFiles bag PrepareContentAsync used to take are both gone from that call — text rasterization now happens lazily inside ClipContentSource itself, which owns cleaning up its own temp files on Dispose (this file's own tempFiles bag is now used only for the render's OWN raw audio PCM temp file — the raw video accumulator described above is gone entirely, see the STREAMING REWRITE remarks). FrameStateResolver.Resolve no longer takes a nativeSizes parameter at all — see its own remarks. REWRITE (\"channels split by kind\"): the SurfacePool seed count below now uses timeline.VideoChannels.Count specifically (only a VideoChannel's clips ever need a GPU-backed canvas surface — see SurfacePool's own remarks on this being a warm-start heuristic, not a hard cap) instead of the old mixed timeline.Channels.Count. Validate's FadeToColorTransition check below also now walks timeline.VideoChannels specifically — that check is fundamentally about VIDEO channel stacking (an opaque transition blacking out whatever composites beneath it), which AudioChannels were never actually part of; walking VideoChannels directly says what the check means instead of relying on AudioChannel transitions happening to never trip it. public static class Renderer Inheritance object Renderer Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods RenderAsync(Blueprint) Renders blueprint to its output file. A source that can't provide a frame doesn't abort the render: it's drawn as a labeled placeholder, and the returned report says which sources failed, why, and where on the timeline. public static Task<RenderReport> RenderAsync(Blueprint blueprint) Parameters blueprint Blueprint Returns Task<RenderReport>"
  },
  "api/EditSharp.Rendering.SourceProblem.html": {
    "href": "api/EditSharp.Rendering.SourceProblem.html",
    "title": "Class SourceProblem | EditSharp",
    "summary": "Class SourceProblem Namespace EditSharp.Rendering Assembly EditSharp.dll One source input (graph node) that failed for one reason, summarised: where on the timeline it first and last happened and on how many frames. Source describes it for a person (\"media-video: C:/clips/a.mp4\"). public sealed record SourceProblem : IEquatable<SourceProblem> Inheritance object SourceProblem Implements IEquatable<SourceProblem> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.ReferenceEquals(object, object) object.ToString() Constructors SourceProblem(Guid, string, SourceUnavailableReason, string, TimeSpan, TimeSpan, int) One source input (graph node) that failed for one reason, summarised: where on the timeline it first and last happened and on how many frames. Source describes it for a person (\"media-video: C:/clips/a.mp4\"). public SourceProblem(Guid NodeId, string Source, SourceUnavailableReason Reason, string Message, TimeSpan FirstAt, TimeSpan LastAt, int Frames) Parameters NodeId Guid Source string Reason SourceUnavailableReason Message string FirstAt TimeSpan LastAt TimeSpan Frames int Properties FirstAt public TimeSpan FirstAt { get; init; } Property Value TimeSpan Frames public int Frames { get; init; } Property Value int LastAt public TimeSpan LastAt { get; init; } Property Value TimeSpan Message public string Message { get; init; } Property Value string NodeId public Guid NodeId { get; init; } Property Value Guid Reason public SourceUnavailableReason Reason { get; init; } Property Value SourceUnavailableReason Source public string Source { get; init; } Property Value string"
  },
  "api/EditSharp.Rendering.html": {
    "href": "api/EditSharp.Rendering.html",
    "title": "Namespace EditSharp.Rendering | EditSharp",
    "summary": "Namespace EditSharp.Rendering Classes Blueprint RenderReport What went wrong with sources during a render that still completed. A failing source is rendered as a labeled placeholder rather than aborting the export; this is how the caller finds out. Renderer Entry point for the render strategy — wires the Skia compositor into an actual end-to-end render. Shape of a render: Probe every Video-type VideoSourceNode's native size (MediaProbe) across the whole timeline — everything about a video clip's media inputs that's constant across its whole life, done once up front (ContentPreparation). TextInputNode/ColorGeneratorInputNode/ NoiseInputNode/TimelineVideoInputNode need no such up-front prep any more — see ContentPreparation's own remarks. Mix the timeline's audio once (AudioMixer.ComposeAsync) and spawn the SINGLE ffmpeg process that will do the real mux + encode for the whole render, with its stdin left open as a raw-video pipe. Render every output frame SEQUENTIALLY directly against an in-process SKCanvas (FrameCompositor), writing each frame's raw RGBA8888 bytes straight into that ffmpeg process's stdin as it's produced — see STREAMING REWRITE below. A Video-type VideoSourceNode's SourceDecoder is opened on its clip's first visible frame and disposed once that clip's visible window ends (ClipContentSource). Close stdin once every frame has been written (ffmpeg's rawvideo demuxer treats that exactly like reaching EOF on a file) and wait for ffmpeg to finish encoding. STREAMING REWRITE — WHY THIS EXISTS: this used to append every frame's raw, uncompressed RGBA8888 bytes to a single growing \"accumulator\" temp file on disk, and only handed that file to ffmpeg for mux/encode once every frame had already been rendered. At 1920x1080 that's 8,294,400 bytes/frame (~7.9MiB); an 18-minute 30fps render is 32,400 frames, i.e. roughly 268GB of raw bytes hitting disk before a single byte of that data was ever compressed — reported as \"hundreds of gigabytes of writes\" and confirmed to match this arithmetic almost exactly, not a leak or a runaway loop. Frame rendering now writes directly into the SAME ffmpeg process's stdin pipe that does the real mux/encode (mirroring how SourceDecoder already pipes raw frames IN from an ffmpeg decode process — this is the same pattern on the encode side), so raw video never touches disk at all: ffmpeg compresses each frame into the target codec as it arrives instead of after the whole timeline has already been written out losslessly. Disk usage for the video side of a render is now effectively just the size of the final encoded output file. AUDIO STILL GOES THROUGH A SMALL TEMP FILE, DELIBERATELY: the mixed master PCM (see AudioMixer.ComposeAsync) is orders of magnitude smaller than raw video (48kHz stereo f32 is 384,000 bytes/sec, so an 18-minute timeline is ~414MB, not hundreds of gigabytes) and ffmpeg needs it as a real, complete, seekable input at process-start time — unlike the video side, it isn't produced incrementally by anything this class does. A second OS pipe/named-pipe could avoid even that, but isn't pursued here: it would need real cross-platform machinery (named pipes on Windows, a FIFO on Unix) for a temp file that was never the actual disk-usage problem. ONE CONSEQUENCE OF STREAMING WORTH FLAGGING: because AudioMixer. ComposeAsync's result has to be written to that temp file and handed to ffmpeg as an -i argument BEFORE ffmpeg can be spawned — and frame rendering can't start streaming into ffmpeg's stdin until ffmpeg exists — audio composition is now awaited before frame rendering begins, rather than run concurrently with it the way this used to work. It still overlaps content preparation and GPU/decoder setup above it, which is normally the larger win of the two; audio mixing itself is a one-shot in-memory computation, not a per-frame cost, so this is expected to be a minor, not a proportional, regression versus however long the frame-by-frame render itself takes. \"CLIPS ARE GRAPHS\" REWRITE: ContentPreparation's nativeSizes/decodePlans/decodeSourcePaths dictionaries are now keyed by InputNode Id (Guid), not by Clip — a VideoClip's graph can contain more than one VideoSourceNode. staticImagePaths and the tempFiles bag PrepareContentAsync used to take are both gone from that call — text rasterization now happens lazily inside ClipContentSource itself, which owns cleaning up its own temp files on Dispose (this file's own tempFiles bag is now used only for the render's OWN raw audio PCM temp file — the raw video accumulator described above is gone entirely, see the STREAMING REWRITE remarks). FrameStateResolver.Resolve no longer takes a nativeSizes parameter at all — see its own remarks. REWRITE (\"channels split by kind\"): the SurfacePool seed count below now uses timeline.VideoChannels.Count specifically (only a VideoChannel's clips ever need a GPU-backed canvas surface — see SurfacePool's own remarks on this being a warm-start heuristic, not a hard cap) instead of the old mixed timeline.Channels.Count. Validate's FadeToColorTransition check below also now walks timeline.VideoChannels specifically — that check is fundamentally about VIDEO channel stacking (an opaque transition blacking out whatever composites beneath it), which AudioChannels were never actually part of; walking VideoChannels directly says what the check means instead of relying on AudioChannel transitions happening to never trip it. SourceProblem One source input (graph node) that failed for one reason, summarised: where on the timeline it first and last happened and on how many frames. Source describes it for a person (\"media-video: C:/clips/a.mp4\"). Structs RenderSettings"
  },
  "api/EditSharp.Video.AudioCodec.html": {
    "href": "api/EditSharp.Video.AudioCodec.html",
    "title": "Enum AudioCodec | EditSharp",
    "summary": "Enum AudioCodec Namespace EditSharp.Video Assembly EditSharp.dll public enum AudioCodec Fields AAC = 0 FLAC = 2 MP3 = 1"
  },
  "api/EditSharp.Video.HardwareAccelerator.html": {
    "href": "api/EditSharp.Video.HardwareAccelerator.html",
    "title": "Enum HardwareAccelerator | EditSharp",
    "summary": "Enum HardwareAccelerator Namespace EditSharp.Video Assembly EditSharp.dll A single top-level switch covering every stage of the pipeline that has a hardware path at all: source DECODE (SourceDecoder's ffmpeg subprocesses), in-process COMPOSITE (the Skia GRContext the compositor's surfaces are backed by, see GpuContext/SurfacePool), and final ENCODE (FfmpegRunner's mux/encode step). Replaces the old Nvenc-only enum, which named one specific vendor encoder rather than describing an intent — this one says \"use hardware wherever it's available\" and leaves resolving that to the fastest option ACTUALLY present on the machine at each of the three stages independently. None forces software at every single stage, deliberately and unconditionally — not \"prefer software\", an absolute guarantee, since this is the value a consumer reaches for specifically to get deterministic, hardware-independent output (e.g. matching a reference render, or working around a suspect driver). GPU attempts hardware at each stage independently, probes it before committing, and falls back to software FOR THAT STAGE ONLY if the probe fails — decided in conversation: a machine with working NVENC but no GPU decode support shouldn't lose GPU encoding just because decode fell back. Every fallback is logged loudly via EditSharpConfig.Logger.Log (not LogVerbose) specifically so a sudden, unexplained slowdown on GPU is never silent — see FfmpegRunner and GpuContext for where each stage's probe and fallback actually happen. public enum HardwareAccelerator Fields GPU = 1 None = 0"
  },
  "api/EditSharp.Video.VideoCodec.html": {
    "href": "api/EditSharp.Video.VideoCodec.html",
    "title": "Enum VideoCodec | EditSharp",
    "summary": "Enum VideoCodec Namespace EditSharp.Video Assembly EditSharp.dll public enum VideoCodec Fields AV1 = 2 DNxHR = 5 Avid DNxHR — one of two codecs OptimizedMediaCache can build its persistent optimized media with (see EditSharpConfig. OptimizedMediaCodec), and the DEFAULT of the two: an open format with no licensing friction, and ffmpeg's \"dnxhd\" encoder handles it natively and cross-platform. All-intra, which is the entire point for this use — see OptimizedMediaCache's class remarks. CPU encode/decode only; no vendor has a hardware codec block for this format — decided in conversation, see OptimizedMediaCache's remarks on Vulkan Video's actual codec coverage (H.264/HEVC/AV1 only, nothing else, on any vendor). FFV1 = 4 GIF = 3 H264 = 0 H265 = 1 ProRes = 6 Apple ProRes — the other codec OptimizedMediaCache can build optimized media with. Common in pro NLE ecosystems; ffmpeg's \"prores_ks\" encoder (not the older, lower-quality \"prores\") is what this project uses for it. Same all-intra, CPU-only reasoning as DNxHR — see its own remarks."
  },
  "api/EditSharp.Video.VideoUtils.html": {
    "href": "api/EditSharp.Video.VideoUtils.html",
    "title": "Class VideoUtils | EditSharp",
    "summary": "Class VideoUtils Namespace EditSharp.Video Assembly EditSharp.dll Small, generic ffmpeg-backed video building blocks — muxing, re-encoding with an optional resize — meant to be reusable both by EditSharp's own pipeline and by a consumer doing an ordinary video task that has nothing to do with the timeline renderer. Deliberately kept free of pipeline- specific filter-graph knowledge — a caller with clip/effect-specific needs (e.g. OptimizedMediaCache's own encode, which needs its own -profile:v handling) builds its own ffmpeg args using the shared lookups here (PixelFormatFor, ContainerExtensionFor, ProfileArgsFor, CodecNames.VideoCodecNames) rather than this file growing pipeline- specific branches of its own. public static class VideoUtils Inheritance object VideoUtils Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods MuxAudioVideoAsync(MediaVideoSource, MediaAudioSource, string) Muxes a video source and an audio source into one file at outputPath with a single ffmpeg process — no InputGraph, no filter_complex, no pass through the timeline pipeline. Both streams are stream-COPIED, not re-encoded, so this is cheap but only works when the container at outputPath can legally hold both codecs as-is. public static Task<MediaVideoSource> MuxAudioVideoAsync(MediaVideoSource video, MediaAudioSource audio, string outputPath) Parameters video MediaVideoSource audio MediaAudioSource outputPath string Returns Task<MediaVideoSource> ReencodeVideoAsync(MediaVideoSource, VideoCodec, (int Width, int Height)?) Re-encodes a source's video stream to a different codec via a single ffmpeg process — decode input, encode output, an optional resize, no audio. A general-purpose helper, not specific to any one part of the pipeline; any caller re-encoding a video source to a different codec, with an optional resize, can use it. (OptimizedMediaCache does NOT use this method directly — it needs a -profile:v argument this method has no parameter for, so it builds its own equivalent ffmpeg invocation instead, sharing PixelFormatFor/ ContainerExtensionFor/ProfileArgsFor with this method rather than duplicating those lookups.) Unlike AddTrimmedInput's use in MuxAudioVideoAsync, the input seek here IS frame-accurate. -ss before -i only lands on the nearest keyframe when the stream is stream-copied — there's nothing to trim mid-GOP without decoding. This method re-encodes, so ffmpeg decodes forward from the nearest keyframe to the exact requested timestamp before the encoder ever sees a frame. Only VideoCodec.FFV1 has a pixel format wired up (Ffv1PixelFormat). See PixelFormatFor. Other codecs fall back to whatever ffmpeg negotiates on its own; there's no current caller that needs them. scaleTo, when given, downscales to that exact size — the caller is responsible for having already worked out an aspect-correct, never-upscaling target; this method just applies whatever box it's handed. Baking a clip's effects into a re-encode is deliberately NOT a feature of this method — nothing in the current pipeline needs that (SkClipVideoChain/ClipEffects apply a clip's effects live, per frame, against whatever this method or OptimizedMediaCache hands back), so this stays a small building block rather than growing pipeline-specific knowledge. public static Task<string> ReencodeVideoAsync(MediaVideoSource source, VideoCodec codec, (int Width, int Height)? scaleTo = null) Parameters source MediaVideoSource codec VideoCodec scaleTo (int Width, int Height)? Returns Task<string>"
  },
  "api/EditSharp.Video.html": {
    "href": "api/EditSharp.Video.html",
    "title": "Namespace EditSharp.Video | EditSharp",
    "summary": "Namespace EditSharp.Video Classes VideoUtils Small, generic ffmpeg-backed video building blocks — muxing, re-encoding with an optional resize — meant to be reusable both by EditSharp's own pipeline and by a consumer doing an ordinary video task that has nothing to do with the timeline renderer. Deliberately kept free of pipeline- specific filter-graph knowledge — a caller with clip/effect-specific needs (e.g. OptimizedMediaCache's own encode, which needs its own -profile:v handling) builds its own ffmpeg args using the shared lookups here (PixelFormatFor, ContainerExtensionFor, ProfileArgsFor, CodecNames.VideoCodecNames) rather than this file growing pipeline- specific branches of its own. Enums AudioCodec HardwareAccelerator A single top-level switch covering every stage of the pipeline that has a hardware path at all: source DECODE (SourceDecoder's ffmpeg subprocesses), in-process COMPOSITE (the Skia GRContext the compositor's surfaces are backed by, see GpuContext/SurfacePool), and final ENCODE (FfmpegRunner's mux/encode step). Replaces the old Nvenc-only enum, which named one specific vendor encoder rather than describing an intent — this one says \"use hardware wherever it's available\" and leaves resolving that to the fastest option ACTUALLY present on the machine at each of the three stages independently. None forces software at every single stage, deliberately and unconditionally — not \"prefer software\", an absolute guarantee, since this is the value a consumer reaches for specifically to get deterministic, hardware-independent output (e.g. matching a reference render, or working around a suspect driver). GPU attempts hardware at each stage independently, probes it before committing, and falls back to software FOR THAT STAGE ONLY if the probe fails — decided in conversation: a machine with working NVENC but no GPU decode support shouldn't lose GPU encoding just because decode fell back. Every fallback is logged loudly via EditSharpConfig.Logger.Log (not LogVerbose) specifically so a sudden, unexplained slowdown on GPU is never silent — see FfmpegRunner and GpuContext for where each stage's probe and fallback actually happen. VideoCodec"
  },
  "api/EditSharp.html": {
    "href": "api/EditSharp.html",
    "title": "Namespace EditSharp | EditSharp",
    "summary": "Namespace EditSharp Namespaces EditSharp.Audio EditSharp.Caching.Proxy EditSharp.Components EditSharp.Editing EditSharp.History EditSharp.Playback EditSharp.Rendering EditSharp.Video Classes EditSharpConfig Process-wide configuration for EditSharp. Set these once at startup, before the first call into TimelineAssembler — nothing here is per-render, so a value changed mid-flight applies to every render sharing the process from that point on, not just the next one. Interfaces IEditSharpLogger Minimal logging seam so EditSharp doesn't assume Console, a specific logging framework, or any other ambient logger exists in the host process. Adapt to Microsoft.Extensions.Logging, Serilog, etc. with a one-line wrapper — e.g. msg => _logger.LogInformation(msg)."
  },
  "index.html": {
    "href": "index.html",
    "title": "EditSharp | EditSharp",
    "summary": "EditSharp A C# timeline video editing library: clips are node graphs, sources are pluggable, and rendering runs through ffmpeg. Start with the History namespace (the reviewed pilot) or browse the API from the menu."
  }
}